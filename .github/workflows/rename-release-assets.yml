name: Rename Release Assets

on:
  release:
    types:
      - published
      - edited

permissions:
  contents: write

jobs:
  rename:
    if: github.event.release.tag_name != 'rivet-2-stable-feed' && github.event.release.tag_name != 'rivet-2-developer-feed'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22.21.1'
      - name: Run rename script
        run: node .github/scripts/rename-release-files.mjs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_RELEASE_ID: ${{ github.event.release.id }}
