// Generated by packages/node/scripts/build-web-app-client.cjs. Do not edit directly.
export const RIVET_WEB_APP_CLIENT_JS =
  '"use strict";\n(() => {\n  // ../core/src/model/AgentResponseTrace.ts\n  var AGENT_RESPONSE_TRACE_SCHEMA_VERSION = 1;\n  var AGENT_RESPONSE_TRACE_MAX_MODEL_CALLS = 250;\n  var AGENT_RESPONSE_TRACE_MAX_PROFILE_ATTEMPTS = 1e3;\n  var AGENT_RESPONSE_TRACE_MAX_TOOL_CALLS = 500;\n  function isAgentResponseTrace(value) {\n    if (!isRecord(value) || value.schemaVersion !== AGENT_RESPONSE_TRACE_SCHEMA_VERSION) return false;\n    if (!hasOnlyKeys(value, [\n      "schemaVersion",\n      "traceId",\n      "scope",\n      "rootRunId",\n      "graphRunId",\n      "graphId",\n      "nodeId",\n      "processId",\n      "startedAt",\n      "responseReadyAt",\n      "finishedAt",\n      "durationMs",\n      "status",\n      "backgroundWorkPending",\n      "summary",\n      "modelCalls",\n      "profileAttempts",\n      "toolCalls",\n      "omittedModelCallCount",\n      "omittedProfileAttemptCount",\n      "omittedToolCallCount"\n    ]))\n      return false;\n    if (typeof value.traceId !== "string" || typeof value.rootRunId !== "string") return false;\n    if (typeof value.graphRunId !== "string" || typeof value.graphId !== "string") return false;\n    if (value.scope !== "response" && value.scope !== "llm-invocation") return false;\n    if (!isRecord(value.summary) || !Array.isArray(value.modelCalls) || !Array.isArray(value.toolCalls)) return false;\n    if (value.profileAttempts !== void 0 && !Array.isArray(value.profileAttempts)) return false;\n    if (value.modelCalls.length > AGENT_RESPONSE_TRACE_MAX_MODEL_CALLS) return false;\n    if (Array.isArray(value.profileAttempts) && value.profileAttempts.length > AGENT_RESPONSE_TRACE_MAX_PROFILE_ATTEMPTS)\n      return false;\n    if (value.toolCalls.length > AGENT_RESPONSE_TRACE_MAX_TOOL_CALLS) return false;\n    return isOptionalString(value.nodeId) && isOptionalString(value.processId) && isOptionalNonNegativeFiniteNumber(value.startedAt) && isOptionalNonNegativeFiniteNumber(value.responseReadyAt) && isOptionalNonNegativeFiniteNumber(value.finishedAt) && isOptionalNonNegativeFiniteNumber(value.durationMs) && isOptionalBoolean(value.backgroundWorkPending) && ["running", "response-ready", "completed", "error", "aborted", "unavailable"].includes(String(value.status)) && isNonNegativeInteger(value.omittedModelCallCount) && (value.omittedProfileAttemptCount === void 0 || isNonNegativeInteger(value.omittedProfileAttemptCount)) && isNonNegativeInteger(value.omittedToolCallCount) && isAgentTraceSummary(value.summary) && value.modelCalls.every(isAgentModelCallTrace) && (value.profileAttempts === void 0 || value.profileAttempts.every(isAgentLLMProfileAttemptTrace)) && value.toolCalls.every(isAgentToolCallTrace);\n  }\n  function isAgentModelCallTrace(value) {\n    return isRecord(value) && hasOnlyKeys(value, [\n      "callId",\n      "nodeId",\n      "processId",\n      "provider",\n      "model",\n      "customProviderApi",\n      "outcome",\n      "attemptIndex",\n      "profileIndex",\n      "profileName",\n      "profileHealthKey",\n      "profileHealthState",\n      "roundIndex",\n      "startedAt",\n      "durationMs",\n      "finishReason",\n      "usage",\n      "pricing"\n    ]) && typeof value.callId === "string" && typeof value.nodeId === "string" && typeof value.processId === "string" && typeof value.provider === "string" && typeof value.model === "string" && (value.customProviderApi === void 0 || value.customProviderApi === "completions" || value.customProviderApi === "responses") && (value.outcome === "success" || value.outcome === "provider-failure" || value.outcome === "aborted") && isNonNegativeInteger(value.attemptIndex) && isOptionalNonNegativeInteger(value.profileIndex) && isOptionalString(value.profileName) && isOptionalString(value.profileHealthKey) && (value.profileHealthState === void 0 || value.profileHealthState === "closed" || value.profileHealthState === "open" || value.profileHealthState === "half-open") && isOptionalNonNegativeInteger(value.roundIndex) && isOptionalNonNegativeFiniteNumber(value.startedAt) && isOptionalNonNegativeFiniteNumber(value.durationMs) && isOptionalString(value.finishReason) && (value.usage === void 0 || isAgentTraceUsage(value.usage)) && isAgentTracePricing(value.pricing);\n  }\n  function isAgentLLMProfileAttemptTrace(value) {\n    return isRecord(value) && hasOnlyKeys(value, [\n      "eventId",\n      "roundIndex",\n      "profileIndex",\n      "profileName",\n      "nodeId",\n      "processId",\n      "provider",\n      "model",\n      "customProviderApi",\n      "stage",\n      "outcome",\n      "attemptIndex",\n      "status",\n      "error",\n      "profileHealthKey",\n      "healthState",\n      "healthDisposition",\n      "healthOutcome",\n      "retryAt",\n      "timeoutKind"\n    ]) && typeof value.eventId === "string" && isNonNegativeInteger(value.roundIndex) && isOptionalNonNegativeInteger(value.profileIndex) && isOptionalString(value.profileName) && typeof value.nodeId === "string" && typeof value.processId === "string" && typeof value.provider === "string" && typeof value.model === "string" && (value.customProviderApi === void 0 || value.customProviderApi === "completions" || value.customProviderApi === "responses") && ["configuration", "request", "response-validation", "health-gate", "health-update"].includes(\n      String(value.stage)\n    ) && ["success", "failure", "aborted", "skipped"].includes(String(value.outcome)) && isOptionalNonNegativeInteger(value.attemptIndex) && isOptionalNonNegativeFiniteNumber(value.status) && isOptionalString(value.error) && isOptionalString(value.profileHealthKey) && (value.healthState === void 0 || value.healthState === "closed" || value.healthState === "open" || value.healthState === "half-open") && (value.healthDisposition === void 0 || value.healthDisposition === "allow" || value.healthDisposition === "deny" || value.healthDisposition === "fail-open") && (value.healthOutcome === void 0 || value.healthOutcome === "healthy" || value.healthOutcome === "unhealthy" || value.healthOutcome === "ignored") && isOptionalNonNegativeFiniteNumber(value.retryAt) && (value.timeoutKind === void 0 || value.timeoutKind === "first-output" || value.timeoutKind === "stream-inactivity");\n  }\n  function isAgentToolCallTrace(value) {\n    const hasKnownOutcome = isRecord(value) && (value.outcome === "success" || value.outcome === "passthrough-error" || value.outcome === "failure" || value.outcome === "aborted");\n    return isRecord(value) && hasOnlyKeys(value, [\n      "toolCallId",\n      "toolName",\n      "sourceNodeId",\n      "sourceProcessId",\n      "resultOwner",\n      "handlerKind",\n      "handlerGraphId",\n      "handlerName",\n      "outcome",\n      "startedAt",\n      "durationMs"\n    ]) && isOptionalString(value.toolCallId) && typeof value.toolName === "string" && typeof value.sourceNodeId === "string" && typeof value.sourceProcessId === "string" && (value.resultOwner === void 0 || isAgentToolResultOutcome(value.outcome) && isAgentToolResultOwner(value.resultOwner)) && (value.handlerKind === "graph" || value.handlerKind === "external" || value.handlerKind === "unknown") && isOptionalString(value.handlerGraphId) && isOptionalString(value.handlerName) && hasKnownOutcome && isOptionalNonNegativeFiniteNumber(value.startedAt) && isOptionalNonNegativeFiniteNumber(value.durationMs);\n  }\n  function isAgentToolResultOutcome(value) {\n    return value === "success" || value === "passthrough-error";\n  }\n  function isAgentToolResultOwner(value) {\n    return isRecord(value) && hasOnlyKeys(value, ["nodeId", "processId", "outputPortId"]) && typeof value.nodeId === "string" && typeof value.processId === "string" && typeof value.outputPortId === "string";\n  }\n  function isAgentTraceSummary(value) {\n    return hasOnlyKeys(value, [\n      "modelCallCount",\n      "toolCallCount",\n      "retryCount",\n      "fallbackCount",\n      "promptTokens",\n      "completionTokens",\n      "totalTokens",\n      "cachedTokens",\n      "reasoningTokens",\n      "knownCostUsd",\n      "costStatus"\n    ]) && isNonNegativeInteger(value.modelCallCount) && isNonNegativeInteger(value.toolCallCount) && isNonNegativeInteger(value.retryCount) && isNonNegativeInteger(value.fallbackCount) && isOptionalNonNegativeFiniteNumber(value.promptTokens) && isOptionalNonNegativeFiniteNumber(value.completionTokens) && isOptionalNonNegativeFiniteNumber(value.totalTokens) && isOptionalNonNegativeFiniteNumber(value.cachedTokens) && isOptionalNonNegativeFiniteNumber(value.reasoningTokens) && isNonNegativeFiniteNumber(value.knownCostUsd) && (value.costStatus === "known" || value.costStatus === "partial" || value.costStatus === "unknown");\n  }\n  function isAgentTraceUsage(value) {\n    return isRecord(value) && hasOnlyKeys(value, ["promptTokens", "completionTokens", "totalTokens", "cachedTokens", "reasoningTokens"]) && Object.values(value).every(isNonNegativeFiniteNumber);\n  }\n  function isAgentTracePricing(value) {\n    return isRecord(value) && hasOnlyKeys(value, ["status", "costUsd"]) && (value.status === "known" || value.status === "unknown") && isOptionalNonNegativeFiniteNumber(value.costUsd);\n  }\n  function hasOnlyKeys(value, allowed) {\n    const allowedKeys = new Set(allowed);\n    return Object.keys(value).every((key) => allowedKeys.has(key));\n  }\n  function isFiniteNumber(value) {\n    return typeof value === "number" && Number.isFinite(value);\n  }\n  function isNonNegativeFiniteNumber(value) {\n    return isFiniteNumber(value) && value >= 0;\n  }\n  function isOptionalNonNegativeFiniteNumber(value) {\n    return value === void 0 || isNonNegativeFiniteNumber(value);\n  }\n  function isNonNegativeInteger(value) {\n    return Number.isInteger(value) && value >= 0;\n  }\n  function isOptionalNonNegativeInteger(value) {\n    return value === void 0 || isNonNegativeInteger(value);\n  }\n  function isOptionalString(value) {\n    return value === void 0 || typeof value === "string";\n  }\n  function isOptionalBoolean(value) {\n    return value === void 0 || typeof value === "boolean";\n  }\n  function isRecord(value) {\n    return typeof value === "object" && value != null && !Array.isArray(value);\n  }\n\n  // ../core/src/model/StoredValueStore.ts\n  function normalizeRivetStoredValueKey(value) {\n    if (typeof value !== "string" || !value.trim()) {\n      throw new Error("Stored value key must be a non-empty string.");\n    }\n    return value;\n  }\n  function cloneRivetStoredValue(value, label = "Stored value") {\n    return clonePortableValue(value, label, /* @__PURE__ */ new Set());\n  }\n  function cloneRivetStoredValueRecord(value, label = "Stored value record") {\n    const cloned = cloneRivetStoredValue(value, label);\n    if (!isPlainRecord(cloned)) throw new Error(`${label} must be a JSON object.`);\n    for (const key of Object.keys(cloned)) normalizeRivetStoredValueKey(key);\n    return cloned;\n  }\n  function clonePortableValue(value, label, ancestors) {\n    if (value === null || typeof value === "string" || typeof value === "boolean") return value;\n    if (typeof value === "number") {\n      if (!Number.isFinite(value)) throw new Error(`${label} must contain only finite numbers.`);\n      return value;\n    }\n    if (typeof value !== "object") {\n      throw new Error(`${label} must contain only portable JSON values.`);\n    }\n    if (ancestors.has(value)) throw new Error(`${label} must not contain cycles.`);\n    ancestors.add(value);\n    try {\n      if (Array.isArray(value)) {\n        const cloned = [];\n        for (let index = 0; index < value.length; index += 1) {\n          if (!Object.prototype.hasOwnProperty.call(value, index)) {\n            throw new Error(`${label} must not contain sparse arrays.`);\n          }\n          cloned.push(clonePortableValue(value[index], label, ancestors));\n        }\n        return cloned;\n      }\n      if (!isPlainRecord(value)) {\n        throw new Error(`${label} must contain only plain JSON objects and arrays.`);\n      }\n      return Object.fromEntries(\n        Object.entries(value).map(([key, item]) => [key, clonePortableValue(item, label, ancestors)])\n      );\n    } finally {\n      ancestors.delete(value);\n    }\n  }\n  function isPlainRecord(value) {\n    if (typeof value !== "object" || value === null || Array.isArray(value)) return false;\n    const prototype = Object.getPrototypeOf(value);\n    return prototype === Object.prototype || prototype === null;\n  }\n\n  // ../core/src/model/WebAppBrowserStorage.ts\n  var RIVET_WEB_APP_BROWSER_STORAGE_DATABASE = "rivet-web-app-browser-storage";\n  var RIVET_WEB_APP_BROWSER_STORAGE_SCHEMA_VERSION = 1;\n  var RIVET_WEB_APP_BROWSER_STORAGE_CHUNK_BYTES = 256 * 1024;\n  var RIVET_WEB_APP_LEGACY_RETENTION_MS = 30 * 24 * 60 * 60 * 1e3;\n  var METADATA_STORE = "metadata";\n  var CHUNKS_STORE = "chunks";\n  var MIGRATIONS_STORE = "migrations";\n  var STAGING_STORE = "staging";\n  var RECORD_GENERATION_INDEX = "record-generation";\n  var SCHEMA_METADATA_ID = "__rivet_schema__";\n  var BROADCAST_CHANNEL_NAME = "rivet-web-app-browser-storage:v1";\n  var IndexedDbWebAppBrowserStorage = class {\n    #databaseName;\n    #factory;\n    #now;\n    #storageManager;\n    #broadcastChannelFactory;\n    #listeners = /* @__PURE__ */ new Set();\n    #memory = /* @__PURE__ */ new Map();\n    #memoryDeleted = /* @__PURE__ */ new Set();\n    #memoryOnly = /* @__PURE__ */ new Set();\n    #instanceId = createGeneration();\n    #knownRevisions = /* @__PURE__ */ new Map();\n    #focusListener = () => void this.#recheckRevisions();\n    #channel;\n    #database;\n    #openPromise;\n    #scope;\n    #scopeId;\n    #status = { durable: false, reason: "indexeddb-unavailable" };\n    #writeQueue = Promise.resolve();\n    #lastRevision = 0;\n    constructor(options = {}) {\n      this.#databaseName = options.databaseName ?? RIVET_WEB_APP_BROWSER_STORAGE_DATABASE;\n      this.#factory = options.indexedDB === void 0 ? getDefaultIndexedDbFactory() : options.indexedDB ?? void 0;\n      this.#now = options.now ?? Date.now;\n      this.#storageManager = options.storageManager ?? getDefaultStorageManager();\n      this.#broadcastChannelFactory = options.broadcastChannelFactory ?? getDefaultBroadcastChannelFactory();\n    }\n    async initialize(scope) {\n      const normalized = normalizeScope(scope);\n      const nextScopeId = serializeScope(normalized);\n      if (this.#scopeId && this.#scopeId !== nextScopeId) {\n        throw new Error("A browser storage instance cannot be reused for another web app scope.");\n      }\n      this.#scope = normalized;\n      this.#scopeId = nextScopeId;\n      this.#ensureBroadcastChannel();\n      if (!this.#factory) return this.#status;\n      try {\n        this.#database = await this.#openDatabase();\n        await this.#ensureSchemaMetadata();\n        this.#status = { durable: true };\n        await this.#removeAbandonedStaging();\n        globalThis.addEventListener?.("focus", this.#focusListener);\n      } catch {\n        this.#status = { durable: false, reason: "open-failed" };\n      }\n      return this.#status;\n    }\n    async get(namespace, key) {\n      const id = this.#recordId(namespace, key);\n      if (this.#memoryDeleted.has(id)) return void 0;\n      const memoryValue = this.#memory.get(id);\n      if (memoryValue !== void 0) return cloneRivetStoredValue(memoryValue, "Browser stored value");\n      if (!this.#database) return void 0;\n      const transaction = this.#database.transaction([METADATA_STORE, CHUNKS_STORE], "readonly");\n      const metadata = await requestResult(\n        transaction.objectStore(METADATA_STORE).get(id)\n      );\n      if (!metadata) {\n        await transactionDone(transaction);\n        this.#knownRevisions.set(id, 0);\n        return void 0;\n      }\n      if (metadata.deleted) {\n        await transactionDone(transaction);\n        this.#memory.delete(id);\n        this.#memoryDeleted.add(id);\n        this.#knownRevisions.set(id, metadata.revision);\n        return void 0;\n      }\n      const chunks = await requestResult(\n        transaction.objectStore(CHUNKS_STORE).index(RECORD_GENERATION_INDEX).getAll(IDBKeyRange.only([id, metadata.generation]))\n      );\n      await transactionDone(transaction);\n      const value = decodeChunks(metadata, chunks);\n      this.#memory.set(id, value);\n      this.#knownRevisions.set(id, metadata.revision);\n      return cloneRivetStoredValue(value, "Browser stored value");\n    }\n    async list(namespace) {\n      const scopeId = this.#requireScope();\n      const entries = /* @__PURE__ */ new Map();\n      const database = this.#database;\n      if (database) {\n        const transaction = database.transaction([METADATA_STORE, CHUNKS_STORE], "readonly");\n        const records = (await requestResult(transaction.objectStore(METADATA_STORE).getAll())).filter(isStoredRecordMetadata).filter((metadata) => metadata.scopeId === scopeId && metadata.namespace === namespace && !metadata.deleted).sort((left, right) => left.key.localeCompare(right.key));\n        for (const metadata of records) {\n          if (this.#memoryDeleted.has(metadata.id)) continue;\n          const memoryValue = this.#memory.get(metadata.id);\n          if (memoryValue !== void 0) {\n            entries.set(metadata.key, cloneRivetStoredValue(memoryValue, "Browser stored value"));\n            continue;\n          }\n          const chunks = await requestResult(\n            transaction.objectStore(CHUNKS_STORE).index(RECORD_GENERATION_INDEX).getAll(IDBKeyRange.only([metadata.id, metadata.generation]))\n          );\n          const value = decodeChunks(metadata, chunks);\n          this.#memory.set(metadata.id, value);\n          this.#knownRevisions.set(metadata.id, metadata.revision);\n          entries.set(metadata.key, cloneRivetStoredValue(value, "Browser stored value"));\n        }\n        await transactionDone(transaction);\n      }\n      const prefix = `${scopeId}\\0${namespace}\\0`;\n      for (const [id, value] of this.#memory) {\n        if (!id.startsWith(prefix) || this.#memoryDeleted.has(id)) continue;\n        entries.set(id.slice(prefix.length), cloneRivetStoredValue(value, "Browser stored value"));\n      }\n      return [...entries].sort(([left], [right]) => left.localeCompare(right)).map(([key, value]) => ({ key, value }));\n    }\n    async set(namespace, key, value) {\n      return await this.commitBatch([{ key, namespace, value }]);\n    }\n    async delete(namespace, key) {\n      return await this.commitBatch([{ key, namespace }]);\n    }\n    async commitBatch(changes) {\n      this.#requireScope();\n      if (changes.length === 0) return { ...this.#status, revision: this.#now() };\n      const prepared = changes.map((change) => prepareChange(this.#recordId(change.namespace, change.key), change));\n      if (new Set(prepared.map((change) => change.id)).size !== prepared.length) {\n        throw new Error("A browser storage batch cannot change the same scoped key more than once.");\n      }\n      for (const change of prepared) {\n        if (change.serialized) {\n          const parsed = cloneRivetStoredValue(\n            JSON.parse(new TextDecoder().decode(change.serialized)),\n            "Browser stored value"\n          );\n          this.#memory.set(change.id, parsed);\n          this.#memoryDeleted.delete(change.id);\n        } else {\n          this.#memory.delete(change.id);\n          this.#memoryDeleted.add(change.id);\n        }\n      }\n      const revision = this.#nextRevision();\n      try {\n        let committedRevision = revision;\n        await this.#enqueueWrite(async () => {\n          if (!this.#database) throw new Error("IndexedDB is unavailable.");\n          committedRevision = await this.#commitPreparedChanges(prepared, revision);\n        });\n        this.#lastRevision = Math.max(this.#lastRevision, committedRevision);\n        for (const change of prepared) {\n          this.#memoryOnly.delete(change.id);\n          this.#knownRevisions.set(change.id, committedRevision);\n          this.#publishChange(change.namespace, change.key, committedRevision);\n        }\n        this.#status = this.#memoryOnly.size === 0 ? { durable: true } : { durable: false, reason: "write-failed" };\n        return {\n          durable: true,\n          ...this.#memoryOnly.size > 0 ? { hasMemoryOnlyRecords: true } : {},\n          revision: committedRevision\n        };\n      } catch (error) {\n        for (const change of prepared) this.#memoryOnly.add(change.id);\n        this.#status = { durable: false, reason: "write-failed" };\n        throw new WebAppBrowserStoragePersistenceError(\n          "Browser data could not be saved. Changes will last only until this page is closed.",\n          { cause: error, revision }\n        );\n      }\n    }\n    subscribe(listener) {\n      this.#listeners.add(listener);\n      return () => this.#listeners.delete(listener);\n    }\n    hasMemoryOnlyRecord(namespace, key) {\n      return this.#memoryOnly.has(this.#recordId(namespace, key));\n    }\n    async estimateUsage() {\n      if (!this.#storageManager) return {};\n      try {\n        const estimate = await this.#storageManager.estimate();\n        return {\n          ...typeof estimate.quota === "number" ? { quota: estimate.quota } : {},\n          ...typeof estimate.usage === "number" ? { usage: estimate.usage } : {}\n        };\n      } catch {\n        return {};\n      }\n    }\n    dispose() {\n      this.#channel?.close();\n      globalThis.removeEventListener?.("focus", this.#focusListener);\n      this.#channel = void 0;\n      this.#database?.close();\n      this.#database = void 0;\n      this.#openPromise = void 0;\n      this.#listeners.clear();\n      this.#knownRevisions.clear();\n      this.#memoryDeleted.clear();\n      this.#memoryOnly.clear();\n    }\n    async getMigration(legacyKey) {\n      const scopeId = this.#requireScope();\n      if (!this.#database) return void 0;\n      const id = migrationId(scopeId, legacyKey);\n      const transaction = this.#database.transaction(MIGRATIONS_STORE, "readonly");\n      const migration = await requestResult(\n        transaction.objectStore(MIGRATIONS_STORE).get(id)\n      );\n      await transactionDone(transaction);\n      if (!migration) return void 0;\n      return {\n        cleanupAfter: migration.cleanupAfter,\n        completedAt: migration.completedAt,\n        key: migration.key,\n        legacyKey: migration.legacyKey,\n        namespace: migration.namespace\n      };\n    }\n    async recordMigration(migration) {\n      const scopeId = this.#requireScope();\n      if (!this.#database) throw new Error("IndexedDB is unavailable.");\n      const transaction = this.#database.transaction(MIGRATIONS_STORE, "readwrite");\n      transaction.objectStore(MIGRATIONS_STORE).put({\n        ...migration,\n        id: migrationId(scopeId, migration.legacyKey),\n        scopeId\n      });\n      await transactionDone(transaction);\n    }\n    async listMigrations() {\n      const scopeId = this.#requireScope();\n      if (!this.#database) return [];\n      const transaction = this.#database.transaction(MIGRATIONS_STORE, "readonly");\n      const migrations = await requestResult(transaction.objectStore(MIGRATIONS_STORE).getAll());\n      await transactionDone(transaction);\n      return migrations.filter((migration) => migration.scopeId === scopeId).map(({ cleanupAfter, completedAt, key, legacyKey, namespace }) => ({\n        cleanupAfter,\n        completedAt,\n        key,\n        legacyKey,\n        namespace\n      }));\n    }\n    #recordId(namespace, key) {\n      const scopeId = this.#requireScope();\n      if (!key) throw new Error("Browser storage key cannot be empty.");\n      return `${scopeId}\\0${namespace}\\0${key}`;\n    }\n    #requireScope() {\n      if (!this.#scopeId || !this.#scope) throw new Error("Browser storage must be initialized before use.");\n      return this.#scopeId;\n    }\n    async #openDatabase() {\n      if (this.#database) return this.#database;\n      if (this.#openPromise) return await this.#openPromise;\n      if (!this.#factory) throw new Error("IndexedDB is unavailable.");\n      this.#openPromise = new Promise((resolve, reject) => {\n        const request = this.#factory.open(this.#databaseName, RIVET_WEB_APP_BROWSER_STORAGE_SCHEMA_VERSION);\n        request.onupgradeneeded = () => {\n          const database = request.result;\n          if (!database.objectStoreNames.contains(METADATA_STORE)) {\n            database.createObjectStore(METADATA_STORE, { keyPath: "id" });\n          }\n          if (!database.objectStoreNames.contains(CHUNKS_STORE)) {\n            const chunks = database.createObjectStore(CHUNKS_STORE, {\n              keyPath: ["recordId", "generation", "index"]\n            });\n            chunks.createIndex(RECORD_GENERATION_INDEX, ["recordId", "generation"]);\n          }\n          if (!database.objectStoreNames.contains(MIGRATIONS_STORE)) {\n            database.createObjectStore(MIGRATIONS_STORE, { keyPath: "id" });\n          }\n          if (!database.objectStoreNames.contains(STAGING_STORE)) {\n            database.createObjectStore(STAGING_STORE, { keyPath: "id" });\n          }\n        };\n        request.onerror = () => reject(request.error ?? new Error("Failed to open IndexedDB."));\n        request.onblocked = () => reject(new Error("IndexedDB upgrade was blocked by another page."));\n        request.onsuccess = () => {\n          const database = request.result;\n          database.onversionchange = () => database.close();\n          resolve(database);\n        };\n      });\n      try {\n        return await this.#openPromise;\n      } finally {\n        this.#openPromise = void 0;\n      }\n    }\n    async #ensureSchemaMetadata() {\n      if (!this.#database) return;\n      const transaction = this.#database.transaction(METADATA_STORE, "readwrite");\n      transaction.objectStore(METADATA_STORE).put({\n        id: SCHEMA_METADATA_ID,\n        schemaVersion: RIVET_WEB_APP_BROWSER_STORAGE_SCHEMA_VERSION,\n        updatedAt: this.#now()\n      });\n      await transactionDone(transaction);\n    }\n    async #commitPreparedChanges(prepared, revision) {\n      const database = this.#database;\n      const generation = createGeneration();\n      const staged = prepared.filter((change) => change.serialized != null);\n      if (staged.length > 0) {\n        const stageTransaction = database.transaction([CHUNKS_STORE, STAGING_STORE], "readwrite");\n        const chunksStore = stageTransaction.objectStore(CHUNKS_STORE);\n        const stagingStore2 = stageTransaction.objectStore(STAGING_STORE);\n        for (const change of staged) {\n          const stagingId = `${change.id}\\0${generation}`;\n          stagingStore2.put({\n            createdAt: this.#now(),\n            generation,\n            id: stagingId,\n            recordId: change.id\n          });\n          change.chunks.forEach((bytes, index2) => {\n            chunksStore.put({ bytes, generation, index: index2, recordId: change.id });\n          });\n        }\n        await transactionDone(stageTransaction);\n        const validationTransaction = database.transaction(CHUNKS_STORE, "readonly");\n        const index = validationTransaction.objectStore(CHUNKS_STORE).index(RECORD_GENERATION_INDEX);\n        for (const change of staged) {\n          const chunks = await requestResult(index.getAll(IDBKeyRange.only([change.id, generation])));\n          decodeChunks(\n            {\n              byteLength: change.serialized.byteLength,\n              chunkCount: change.chunks.length,\n              generation,\n              id: change.id,\n              key: change.key,\n              namespace: change.namespace,\n              revision,\n              scopeId: this.#scopeId,\n              updatedAt: this.#now()\n            },\n            chunks\n          );\n        }\n        await transactionDone(validationTransaction);\n      }\n      const switchTransaction = database.transaction([METADATA_STORE, STAGING_STORE], "readwrite");\n      const metadataStore = switchTransaction.objectStore(METADATA_STORE);\n      const stagingStore = switchTransaction.objectStore(STAGING_STORE);\n      const previous = [];\n      const priorRecords = [];\n      for (const change of prepared) {\n        const prior = await requestResult(metadataStore.get(change.id));\n        if (prior) {\n          priorRecords.push(prior);\n          if (!prior.deleted) previous.push(prior);\n        }\n      }\n      const committedRevision = Math.max(revision, ...priorRecords.map((prior) => prior.revision + 1));\n      for (const change of prepared) {\n        const base = {\n          byteLength: change.serialized?.byteLength ?? 0,\n          chunkCount: change.serialized?.byteLength == null ? 0 : change.chunks.length,\n          deleted: change.serialized == null,\n          generation,\n          id: change.id,\n          key: change.key,\n          namespace: change.namespace,\n          revision: committedRevision,\n          scopeId: this.#scopeId,\n          updatedAt: this.#now()\n        };\n        metadataStore.put(base);\n        if (change.serialized) stagingStore.delete(`${change.id}\\0${generation}`);\n      }\n      await transactionDone(switchTransaction);\n      void Promise.all(previous.map((metadata) => this.#deleteGeneration(metadata.id, metadata.generation))).catch(\n        () => void 0\n      );\n      return committedRevision;\n    }\n    async #deleteGeneration(recordId, generation) {\n      if (!this.#database) return;\n      const transaction = this.#database.transaction(CHUNKS_STORE, "readwrite");\n      const index = transaction.objectStore(CHUNKS_STORE).index(RECORD_GENERATION_INDEX);\n      const keys = await requestResult(index.getAllKeys(IDBKeyRange.only([recordId, generation])));\n      const store = transaction.objectStore(CHUNKS_STORE);\n      for (const key of keys) store.delete(key);\n      await transactionDone(transaction);\n    }\n    async #removeAbandonedStaging() {\n      if (!this.#database) return;\n      const transaction = this.#database.transaction(STAGING_STORE, "readwrite");\n      const staging = await requestResult(transaction.objectStore(STAGING_STORE).getAll());\n      const store = transaction.objectStore(STAGING_STORE);\n      const abandoned = staging.filter((record) => this.#now() - record.createdAt >= 5 * 60 * 1e3);\n      for (const record of abandoned) store.delete(record.id);\n      await transactionDone(transaction);\n      await Promise.all(abandoned.map((record) => this.#deleteGeneration(record.recordId, record.generation)));\n    }\n    #ensureBroadcastChannel() {\n      if (this.#channel || !this.#broadcastChannelFactory) return;\n      try {\n        this.#channel = this.#broadcastChannelFactory(BROADCAST_CHANNEL_NAME);\n        this.#channel.onmessage = (event) => {\n          const message = parseBroadcastMessage(event.data);\n          if (!message || message.sourceId === this.#instanceId || message.scopeId !== this.#scopeId) return;\n          void this.#enqueueWrite(async () => {\n            const id = this.#recordId(message.namespace, message.key);\n            if (this.#memoryOnly.has(id)) return;\n            const knownRevision = this.#knownRevisions.get(id);\n            if (knownRevision !== void 0 && message.revision <= knownRevision) return;\n            this.#memory.delete(id);\n            this.#memoryDeleted.delete(id);\n            this.#knownRevisions.set(id, message.revision);\n            const change = { key: message.key, namespace: message.namespace, revision: message.revision };\n            for (const listener of this.#listeners) listener(change);\n          });\n        };\n      } catch {\n        this.#channel = void 0;\n      }\n    }\n    #publishChange(namespace, key, revision) {\n      const change = { key, namespace, revision };\n      try {\n        this.#channel?.postMessage({\n          ...change,\n          scopeId: this.#scopeId,\n          sourceId: this.#instanceId,\n          type: "record-changed"\n        });\n      } catch {\n      }\n    }\n    async #enqueueWrite(operation) {\n      const result = this.#writeQueue.catch(() => void 0).then(operation);\n      this.#writeQueue = result.catch(() => void 0);\n      await result;\n    }\n    #nextRevision() {\n      this.#lastRevision = Math.max(this.#lastRevision + 1, this.#now());\n      return this.#lastRevision;\n    }\n    async #recheckRevisions() {\n      if (!this.#database || !this.#scopeId) return;\n      await this.#enqueueWrite(async () => {\n        if (!this.#database || !this.#scopeId) return;\n        const transaction = this.#database.transaction(METADATA_STORE, "readonly");\n        const records = (await requestResult(transaction.objectStore(METADATA_STORE).getAll())).filter(\n          isStoredRecordMetadata\n        );\n        await transactionDone(transaction);\n        for (const metadata of records) {\n          if (metadata.scopeId !== this.#scopeId) continue;\n          if (this.#memoryOnly.has(metadata.id)) continue;\n          const knownRevision = this.#knownRevisions.get(metadata.id);\n          if (knownRevision !== void 0 && metadata.revision <= knownRevision) continue;\n          this.#memory.delete(metadata.id);\n          this.#memoryDeleted.delete(metadata.id);\n          this.#knownRevisions.set(metadata.id, metadata.revision);\n          const change = {\n            key: metadata.key,\n            namespace: metadata.namespace,\n            revision: metadata.revision\n          };\n          for (const listener of this.#listeners) listener(change);\n        }\n      }).catch(() => void 0);\n    }\n  };\n  var WebAppBrowserStoragePersistenceError = class extends Error {\n    revision;\n    constructor(message, options) {\n      super(message, options);\n      this.name = "WebAppBrowserStoragePersistenceError";\n      this.revision = options.revision;\n    }\n  };\n  function prepareChange(id, change) {\n    if (!change.key) throw new Error("Browser storage key cannot be empty.");\n    if (change.value === void 0) {\n      return { chunks: [], id, key: change.key, namespace: change.namespace };\n    }\n    const portable = cloneRivetStoredValue(change.value, "Browser stored value");\n    const serialized = new TextEncoder().encode(JSON.stringify(portable));\n    const chunks = [];\n    for (let offset = 0; offset < serialized.byteLength; offset += RIVET_WEB_APP_BROWSER_STORAGE_CHUNK_BYTES) {\n      const chunk = serialized.slice(\n        offset,\n        Math.min(serialized.byteLength, offset + RIVET_WEB_APP_BROWSER_STORAGE_CHUNK_BYTES)\n      );\n      chunks.push(chunk.buffer);\n    }\n    if (chunks.length === 0) chunks.push(new ArrayBuffer(0));\n    return { chunks, id, key: change.key, namespace: change.namespace, serialized };\n  }\n  function decodeChunks(metadata, chunks) {\n    if (chunks.length !== metadata.chunkCount) throw new Error("Browser storage record is incomplete.");\n    chunks.sort((left, right) => left.index - right.index);\n    const bytes = new Uint8Array(metadata.byteLength);\n    let offset = 0;\n    for (let index = 0; index < chunks.length; index += 1) {\n      const chunk = chunks[index];\n      if (chunk.index !== index) throw new Error("Browser storage record has invalid chunk ordering.");\n      const source = new Uint8Array(chunk.bytes);\n      if (offset + source.byteLength > bytes.byteLength) throw new Error("Browser storage record exceeds its metadata.");\n      bytes.set(source, offset);\n      offset += source.byteLength;\n    }\n    if (offset !== metadata.byteLength)\n      throw new Error("Browser storage record byte length does not match its metadata.");\n    return cloneRivetStoredValue(JSON.parse(new TextDecoder().decode(bytes)), "Browser stored value");\n  }\n  function isStoredRecordMetadata(value) {\n    if (!value || typeof value !== "object" || Array.isArray(value)) return false;\n    const metadata = value;\n    return typeof metadata.id === "string" && metadata.id !== SCHEMA_METADATA_ID && typeof metadata.scopeId === "string" && typeof metadata.key === "string" && (metadata.namespace === "chat-state" || metadata.namespace === "response-traces" || metadata.namespace === "stored-values") && typeof metadata.revision === "number" && typeof metadata.generation === "string" && typeof metadata.byteLength === "number" && typeof metadata.chunkCount === "number";\n  }\n  function normalizeScope(scope) {\n    if (!scope.origin) throw new Error("Browser storage origin cannot be empty.");\n    if (!scope.uiGraphId) throw new Error("Browser storage UI graph id cannot be empty.");\n    const pathname = scope.pathname.replace(/\\/+$/, "") || "/";\n    return { origin: scope.origin, pathname, uiGraphId: scope.uiGraphId };\n  }\n  function serializeScope(scope) {\n    return [\n      encodeURIComponent(scope.origin),\n      encodeURIComponent(scope.pathname),\n      encodeURIComponent(scope.uiGraphId)\n    ].join(":");\n  }\n  function migrationId(scopeId, legacyKey) {\n    return `${scopeId}\\0${legacyKey}`;\n  }\n  function createGeneration() {\n    return globalThis.crypto?.randomUUID?.() ?? `rivet-${Date.now()}-${Math.random().toString(36).slice(2)}`;\n  }\n  function getDefaultIndexedDbFactory() {\n    try {\n      return globalThis.indexedDB ?? globalThis.window?.indexedDB;\n    } catch {\n      return void 0;\n    }\n  }\n  function getDefaultStorageManager() {\n    try {\n      return globalThis.navigator?.storage;\n    } catch {\n      return void 0;\n    }\n  }\n  function getDefaultBroadcastChannelFactory() {\n    try {\n      return typeof globalThis.BroadcastChannel === "function" ? (name) => new BroadcastChannel(name) : void 0;\n    } catch {\n      return void 0;\n    }\n  }\n  function parseBroadcastMessage(value) {\n    if (!value || typeof value !== "object" || Array.isArray(value)) return void 0;\n    const message = value;\n    return message.type === "record-changed" && typeof message.scopeId === "string" && typeof message.sourceId === "string" && typeof message.key === "string" && (message.namespace === "chat-state" || message.namespace === "response-traces" || message.namespace === "stored-values") && Number.isSafeInteger(message.revision) && Number(message.revision) > 0 ? message : void 0;\n  }\n  function requestResult(request) {\n    return new Promise((resolve, reject) => {\n      request.onsuccess = () => resolve(request.result);\n      request.onerror = () => reject(request.error ?? new Error("IndexedDB request failed."));\n    });\n  }\n  function transactionDone(transaction) {\n    return new Promise((resolve, reject) => {\n      transaction.oncomplete = () => resolve();\n      transaction.onerror = () => reject(transaction.error ?? new Error("IndexedDB transaction failed."));\n      transaction.onabort = () => reject(transaction.error ?? new Error("IndexedDB transaction was aborted."));\n    });\n  }\n\n  // ../core/src/model/WebAppBrowserStorageRpc.ts\n  var RIVET_WEB_APP_BROWSER_STORAGE_RPC_CAPABILITY = "browser-storage-rpc-v2";\n  var RIVET_WEB_APP_BROWSER_STORAGE_SAFE_FALLBACK_BYTES = 4 * 1024 * 1024;\n  var RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_MAX_VALUE_BYTES = 256 * 1024 * 1024;\n  var RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_MAX_ACTION_BYTES = 512 * 1024 * 1024;\n  var RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_MAX_ACTIVE_BYTES = 512 * 1024 * 1024;\n  var RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_TRANSFER_TIMEOUT_MS = 6e4;\n  var RIVET_WEB_APP_BROWSER_STORAGE_TRANSFER_CHUNK_BYTES = 256 * 1024;\n  var RIVET_WEB_APP_BROWSER_STORAGE_BINARY_FRAME_HEADER_BYTES = 40;\n  var FRAME_MAGIC = new Uint8Array([82, 86, 66, 83]);\n  function parseRivetWebAppBrowserStorageServerMessage(value) {\n    if (!isRecord2(value) || !hasActionIds(value) || typeof value.type !== "string") return void 0;\n    if (!isUuid(value.storageSessionId)) return void 0;\n    if (value.type === "storage.get") {\n      return isNonEmptyString(value.storageRequestId) && typeof value.key === "string" && value.key.length > 0 ? {\n        type: value.type,\n        requestId: value.requestId,\n        runId: value.runId,\n        storageSessionId: value.storageSessionId,\n        storageRequestId: value.storageRequestId,\n        key: value.key\n      } : void 0;\n    }\n    if (value.type === "storage.transfer.ack") {\n      return isNonEmptyString(value.storageRequestId) && isUuid(value.transferId) ? {\n        type: value.type,\n        requestId: value.requestId,\n        runId: value.runId,\n        storageSessionId: value.storageSessionId,\n        storageRequestId: value.storageRequestId,\n        transferId: value.transferId\n      } : void 0;\n    }\n    if (value.type === "storage.commit.start") {\n      return isUuid(value.transferId) && isSafeByteLength(value.byteLength) && isSafeChunkCount(value.chunkCount) ? {\n        type: value.type,\n        requestId: value.requestId,\n        runId: value.runId,\n        storageSessionId: value.storageSessionId,\n        transferId: value.transferId,\n        byteLength: value.byteLength,\n        chunkCount: value.chunkCount\n      } : void 0;\n    }\n    if (value.type === "storage.error") return parseStorageError(value);\n    return void 0;\n  }\n  function serializeRivetWebAppStoredValue(value) {\n    return new TextEncoder().encode(JSON.stringify(value));\n  }\n  function deserializeRivetWebAppStoredValuePatch(bytes) {\n    return cloneRivetStoredValueRecord(\n      JSON.parse(new TextDecoder().decode(bytes)),\n      "Browser Stored Value commit"\n    );\n  }\n  function splitRivetWebAppStorageTransfer(bytes) {\n    if (bytes.byteLength === 0) return [];\n    const chunks = [];\n    for (let offset = 0; offset < bytes.byteLength; offset += RIVET_WEB_APP_BROWSER_STORAGE_TRANSFER_CHUNK_BYTES) {\n      chunks.push(\n        bytes.slice(offset, Math.min(bytes.byteLength, offset + RIVET_WEB_APP_BROWSER_STORAGE_TRANSFER_CHUNK_BYTES))\n      );\n    }\n    return chunks;\n  }\n  function encodeRivetWebAppStorageBinaryFrame(storageSessionId, transferId, chunkIndex, bytes) {\n    const sessionUuid = uuidToBytes(storageSessionId);\n    const transferUuid = uuidToBytes(transferId);\n    if (!Number.isSafeInteger(chunkIndex) || chunkIndex < 0 || chunkIndex > 4294967295) {\n      throw new RangeError("Storage transfer chunk index is invalid.");\n    }\n    const frame = new Uint8Array(RIVET_WEB_APP_BROWSER_STORAGE_BINARY_FRAME_HEADER_BYTES + bytes.byteLength);\n    frame.set(FRAME_MAGIC, 0);\n    frame.set(sessionUuid, 4);\n    frame.set(transferUuid, 20);\n    new DataView(frame.buffer).setUint32(36, chunkIndex, false);\n    frame.set(bytes, RIVET_WEB_APP_BROWSER_STORAGE_BINARY_FRAME_HEADER_BYTES);\n    return frame;\n  }\n  function decodeRivetWebAppStorageBinaryFrame(frame) {\n    const source = frame instanceof ArrayBuffer ? new Uint8Array(frame) : new Uint8Array(frame.buffer, frame.byteOffset, frame.byteLength);\n    if (source.byteLength < RIVET_WEB_APP_BROWSER_STORAGE_BINARY_FRAME_HEADER_BYTES) return void 0;\n    for (let index = 0; index < FRAME_MAGIC.length; index += 1) {\n      if (source[index] !== FRAME_MAGIC[index]) return void 0;\n    }\n    return {\n      storageSessionId: bytesToUuid(source.slice(4, 20)),\n      transferId: bytesToUuid(source.slice(20, 36)),\n      chunkIndex: new DataView(\n        source.buffer,\n        source.byteOffset,\n        RIVET_WEB_APP_BROWSER_STORAGE_BINARY_FRAME_HEADER_BYTES\n      ).getUint32(36, false),\n      bytes: source.slice(RIVET_WEB_APP_BROWSER_STORAGE_BINARY_FRAME_HEADER_BYTES)\n    };\n  }\n  function parseStorageError(value) {\n    return isStorageErrorCode(value.code) && typeof value.error === "string" && typeof value.retryable === "boolean" ? {\n      type: "storage.error",\n      requestId: value.requestId,\n      runId: value.runId,\n      storageSessionId: value.storageSessionId,\n      code: value.code,\n      error: value.error,\n      retryable: value.retryable,\n      ...isNonEmptyString(value.storageRequestId) ? { storageRequestId: value.storageRequestId } : {},\n      ...isUuid(value.transferId) ? { transferId: value.transferId } : {}\n    } : void 0;\n  }\n  function hasActionIds(value) {\n    return isNonEmptyString(value.requestId) && isNonEmptyString(value.runId);\n  }\n  function isStorageErrorCode(value) {\n    return value === "storage_unavailable" || value === "storage_invalid" || value === "storage_too_large" || value === "storage_capacity";\n  }\n  function isSafeByteLength(value) {\n    return typeof value === "number" && Number.isSafeInteger(value) && value >= 0;\n  }\n  function isSafeChunkCount(value) {\n    return typeof value === "number" && Number.isSafeInteger(value) && value >= 0;\n  }\n  function isUuid(value) {\n    return typeof value === "string" && /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value);\n  }\n  function uuidToBytes(value) {\n    if (!isUuid(value)) throw new Error("Storage transfer ID must be a UUID.");\n    return Uint8Array.from(\n      value.replaceAll("-", "").match(/.{2}/g).map((part) => Number.parseInt(part, 16))\n    );\n  }\n  function bytesToUuid(value) {\n    const hex = [...value].map((byte) => byte.toString(16).padStart(2, "0")).join("");\n    return `${hex.slice(0, 8)}-${hex.slice(8, 12)}-${hex.slice(12, 16)}-${hex.slice(16, 20)}-${hex.slice(20)}`;\n  }\n  function isRecord2(value) {\n    return typeof value === "object" && value !== null && !Array.isArray(value);\n  }\n  function isNonEmptyString(value) {\n    return typeof value === "string" && value.length > 0;\n  }\n\n  // ../core/src/model/UiGraph.ts\n  function getUiGraphInitialState(uiGraph) {\n    const state = {};\n    for (const component of uiGraph.components) {\n      if (component.type === "input" || component.type === "textarea") {\n        state[component.stateKey] = component.defaultValue ?? "";\n      } else if (component.type === "dropdown") {\n        state[component.stateKey] = "";\n      } else if (component.type === "chat") {\n        state[getUiGraphChatDraftStateKey(component.id)] = "";\n        state[getUiGraphChatMessagesStateKey(component.id)] = [];\n        state[getUiGraphChatPinsStateKey(component.id)] = [];\n      }\n    }\n    return state;\n  }\n  function getUiGraphChatDraftStateKey(componentId) {\n    return `__rivet_chat_${componentId}_draft`;\n  }\n  function getUiGraphChatMessagesStateKey(componentId) {\n    return `__rivet_chat_${componentId}_messages`;\n  }\n  function getUiGraphChatPinsStateKey(componentId) {\n    return `__rivet_chat_${componentId}_pins`;\n  }\n  function getUiGraphChatMessages(componentId, state) {\n    const messages = state[getUiGraphChatMessagesStateKey(componentId)];\n    return Array.isArray(messages) ? messages.filter(isUiGraphChatMessage) : [];\n  }\n  function getUiGraphChatPins(componentId, state) {\n    const messages = getUiGraphChatMessages(componentId, state);\n    const storedPins = state[getUiGraphChatPinsStateKey(componentId)];\n    const messageIndexes = Array.isArray(storedPins) ? [\n      ...new Set(\n        storedPins.filter(\n          (value) => typeof value === "number" && Number.isSafeInteger(value) && value >= 0\n        )\n      )\n    ].sort((left, right) => left - right) : [];\n    const pinnedIndexes = new Set(messageIndexes);\n    const pins = [];\n    let prompt;\n    let promptMessageIndex;\n    messages.forEach((message, messageIndex) => {\n      if (message.role === "user") {\n        prompt = message;\n        promptMessageIndex = messageIndex;\n      } else if (pinnedIndexes.has(messageIndex)) {\n        pins.push({ messageIndex, prompt, promptMessageIndex, response: message });\n      }\n    });\n    return pins;\n  }\n  function createUiGraphChatPinStatePatch(componentId, state, messageIndex) {\n    const messages = getUiGraphChatMessages(componentId, state);\n    if (messages[messageIndex]?.role !== "assistant") {\n      return void 0;\n    }\n    const pinsStateKey = getUiGraphChatPinsStateKey(componentId);\n    const pinnedIndexes = getUiGraphChatPins(componentId, state).map((pin) => pin.messageIndex);\n    return {\n      [pinsStateKey]: pinnedIndexes.includes(messageIndex) ? pinnedIndexes.filter((index) => index !== messageIndex) : [...pinnedIndexes, messageIndex].sort((left, right) => left - right)\n    };\n  }\n  function createUiGraphChatMessageRemovalStatePatch(componentId, state, messageIndex) {\n    const messages = getUiGraphChatMessages(componentId, state);\n    if (!Number.isSafeInteger(messageIndex) || messageIndex < 0 || messageIndex >= messages.length) {\n      return void 0;\n    }\n    const pinsStateKey = getUiGraphChatPinsStateKey(componentId);\n    const pinnedIndexes = getUiGraphChatPins(componentId, state).map((pin) => pin.messageIndex);\n    const remainingPinnedIndexes = pinnedIndexes.flatMap((pinnedIndex) => {\n      if (pinnedIndex === messageIndex) return [];\n      return [pinnedIndex > messageIndex ? pinnedIndex - 1 : pinnedIndex];\n    });\n    return {\n      [getUiGraphChatMessagesStateKey(componentId)]: messages.filter((_message, index) => index !== messageIndex),\n      [pinsStateKey]: remainingPinnedIndexes\n    };\n  }\n  function createUiGraphChatHistoryFlushStatePatch(componentId) {\n    return {\n      [getUiGraphChatMessagesStateKey(componentId)]: [],\n      [getUiGraphChatPinsStateKey(componentId)]: []\n    };\n  }\n  function createUiGraphChatSubmissionStatePatch(componentId, state, timestamp = (/* @__PURE__ */ new Date()).toISOString()) {\n    const draftStateKey = getUiGraphChatDraftStateKey(componentId);\n    const content = `${state[draftStateKey] ?? ""}`.trim();\n    if (!content) {\n      return void 0;\n    }\n    return {\n      [draftStateKey]: "",\n      [getUiGraphChatMessagesStateKey(componentId)]: [\n        ...getUiGraphChatMessages(componentId, state),\n        { role: "user", content, timestamp }\n      ]\n    };\n  }\n  function getUiGraphComponentActionState(component, state) {\n    if (component.type === "button") {\n      return getUiGraphActionState(component.action, state);\n    }\n    const messagesStateKey = getUiGraphChatMessagesStateKey(component.id);\n    return {\n      ...getUiGraphActionState(component.action, state),\n      [messagesStateKey]: getUiGraphChatMessages(component.id, state)\n    };\n  }\n  function getUiGraphComponentActionOutputStateKeys(component) {\n    return component.type === "button" ? getUiGraphActionOutputBindings(component.action).map((binding) => binding.stateKey.trim()).filter(Boolean) : [getUiGraphChatMessagesStateKey(component.id)];\n  }\n  function getUiGraphActionState(action, state) {\n    return Object.fromEntries(\n      [...new Set(getUiGraphActionInputBindings(action).map((binding) => binding.stateKey))].filter(Boolean).filter((stateKey) => Object.prototype.hasOwnProperty.call(state, stateKey)).map((stateKey) => [stateKey, state[stateKey]])\n    );\n  }\n  function getUiGraphActionOutputBindings(action) {\n    if (action.outputs) {\n      return action.outputs;\n    }\n    return action.outputStateKey ? [{ outputKey: action.outputKey, stateKey: action.outputStateKey }] : [];\n  }\n  function getUiGraphActionInputBindings(action) {\n    if (action.inputMappings) {\n      return action.inputMappings;\n    }\n    return Object.entries(action.inputs ?? {}).map(([inputKey, binding]) => ({\n      inputKey,\n      stateKey: binding.type === "state" ? binding.key : ""\n    }));\n  }\n  function isUiGraphChatMessage(value) {\n    return value != null && typeof value === "object" && (value.role === "user" || value.role === "assistant") && typeof value.content === "string";\n  }\n\n  // ../core/src/model/GraphProgress.ts\n  var MAX_PROGRESS_MESSAGE_LENGTH = 500;\n  function normalizeGraphProgress(progress) {\n    const message = typeof progress.message === "string" ? progress.message.trim().slice(0, MAX_PROGRESS_MESSAGE_LENGTH) : "";\n    const percent = typeof progress.percent === "number" && Number.isFinite(progress.percent) ? Math.min(100, Math.max(0, progress.percent)) : void 0;\n    if (!message && percent == null) {\n      return void 0;\n    }\n    return {\n      ...message ? { message } : {},\n      ...percent == null ? {} : { percent }\n    };\n  }\n\n  // ../core/src/model/UiGraphRuntimeModel.ts\n  var IMAGE_DATA_URL_PATTERN = /^data:(image\\/(?:avif|bmp|gif|jpe?g|png|webp));base64,([\\s\\S]+)$/i;\n  var IMAGE_URL_PATTERN = /^(?:https?:\\/\\/|blob:|\\/\\/|\\/|\\.\\.?\\/)/i;\n  var RELATIVE_IMAGE_PATH_PATTERN = /\\.(?:avif|bmp|gif|jpe?g|png|webp)(?:[?#].*)?$/i;\n  var BASE64_PATTERN = /^[A-Za-z0-9+/]+={0,2}$/;\n  var UI_GRAPH_PROGRESSIVE_JSON_OUTPUT_THRESHOLD = 128 * 1024;\n  var UI_GRAPH_PROGRESSIVE_JSON_OUTPUT_INITIAL_CHARS = 16 * 1024;\n  var UI_GRAPH_PROGRESSIVE_JSON_OUTPUT_CHUNK_CHARS = 16 * 1024;\n  var ACTION_LOADING_DELAY_MS = 300;\n  function getUiGraphActionComponentsById(uiGraph) {\n    return new Map(\n      uiGraph.components.filter(\n        (component) => component.type === "button" || component.type === "chat"\n      ).map((component) => [component.id, component])\n    );\n  }\n  function areUiGraphActionValuesEqual(left, right) {\n    if (Object.is(left, right)) {\n      return true;\n    }\n    if (!left || !right || typeof left !== "object" || typeof right !== "object") {\n      return false;\n    }\n    if (Array.isArray(left) || Array.isArray(right)) {\n      return Array.isArray(left) && Array.isArray(right) && left.length === right.length && left.every((value, index) => areUiGraphActionValuesEqual(value, right[index]));\n    }\n    const leftRecord = left;\n    const rightRecord = right;\n    const leftKeys = Object.keys(leftRecord);\n    const rightKeys = Object.keys(rightRecord);\n    return leftKeys.length === rightKeys.length && leftKeys.every(\n      (key) => Object.prototype.hasOwnProperty.call(rightRecord, key) && areUiGraphActionValuesEqual(leftRecord[key], rightRecord[key])\n    );\n  }\n  function areUiGraphActionComponentsExecutionEquivalent(left, right) {\n    return left.type === right.type && areUiGraphActionValuesEqual(left.action, right.action);\n  }\n  function createUiGraphActionExecutionController() {\n    let nextVersion = 0;\n    const activeExecutionByComponent = /* @__PURE__ */ new Map();\n    const latestWriterByStateKey = /* @__PURE__ */ new Map();\n    return {\n      begin(component) {\n        if (activeExecutionByComponent.has(component.id)) {\n          return void 0;\n        }\n        const execution = { componentId: component.id, id: ++nextVersion };\n        activeExecutionByComponent.set(component.id, execution.id);\n        for (const stateKey of getUiGraphComponentActionOutputStateKeys(component)) {\n          latestWriterByStateKey.set(stateKey, execution.id);\n        }\n        return execution;\n      },\n      finish(execution) {\n        if (activeExecutionByComponent.get(execution.componentId) !== execution.id) {\n          return false;\n        }\n        activeExecutionByComponent.delete(execution.componentId);\n        return true;\n      },\n      isCurrent(execution) {\n        return activeExecutionByComponent.get(execution.componentId) === execution.id;\n      },\n      isRunning(componentId) {\n        return activeExecutionByComponent.has(componentId);\n      },\n      noteStateWrite(stateKey) {\n        const normalizedStateKey = stateKey.trim();\n        if (normalizedStateKey) {\n          latestWriterByStateKey.set(normalizedStateKey, ++nextVersion);\n        }\n      },\n      resolveStatePatch(execution, statePatch) {\n        if (!statePatch) {\n          return void 0;\n        }\n        const applicableEntries = Object.entries(statePatch).filter(\n          ([stateKey]) => latestWriterByStateKey.get(stateKey) === execution.id\n        );\n        return applicableEntries.length > 0 ? Object.fromEntries(applicableEntries) : void 0;\n      },\n      reset() {\n        activeExecutionByComponent.clear();\n        latestWriterByStateKey.clear();\n      }\n    };\n  }\n  function createUiGraphInteractionController(initialUiGraph, options = {}) {\n    let uiGraphId = initialUiGraph.id;\n    let actionComponentsById = getUiGraphActionComponentsById(initialUiGraph);\n    let outputComponentIds = getUiGraphOutputComponentIds(initialUiGraph);\n    let outputComponentsByStateKey = getUiGraphOutputComponentsByStateKey(initialUiGraph);\n    const initialStateOverride = options.initialState ? { ...options.initialState } : void 0;\n    let initialState = normalizeUiGraphDropdownState(\n      initialUiGraph,\n      initialStateOverride ?? getUiGraphInitialState(initialUiGraph)\n    );\n    let state = { ...initialState };\n    let actionErrors = {};\n    let actionProgress = {};\n    const collapsedOutputComponentIds = /* @__PURE__ */ new Set();\n    let snapshot;\n    const actionController = createUiGraphActionExecutionController();\n    const activeActions = /* @__PURE__ */ new Map();\n    const loadingActionIds = /* @__PURE__ */ new Set();\n    const loadingDelayTimers = /* @__PURE__ */ new Map();\n    const listeners = /* @__PURE__ */ new Set();\n    const updateSnapshot = () => {\n      snapshot = {\n        actionErrors,\n        actionProgress,\n        collapsedOutputComponentIds: new Set(collapsedOutputComponentIds),\n        loadingComponentIds: new Set(\n          [...activeActions].flatMap(\n            ([executionId, { execution }]) => loadingActionIds.has(executionId) ? [execution.componentId] : []\n          )\n        ),\n        runningComponentIds: new Set([...activeActions.values()].map(({ execution }) => execution.componentId)),\n        state\n      };\n    };\n    const publish = (change) => {\n      updateSnapshot();\n      for (const listener of listeners) {\n        listener(change);\n      }\n    };\n    const clearActionLoadingPresentation = (executionId) => {\n      const timer = loadingDelayTimers.get(executionId);\n      if (timer != null) {\n        clearTimeout(timer);\n        loadingDelayTimers.delete(executionId);\n      }\n      loadingActionIds.delete(executionId);\n    };\n    const delayActionLoadingPresentation = (execution) => {\n      loadingDelayTimers.set(\n        execution.id,\n        setTimeout(() => {\n          loadingDelayTimers.delete(execution.id);\n          if (!activeActions.has(execution.id) || !actionController.isCurrent(execution)) return;\n          loadingActionIds.add(execution.id);\n          publish("action");\n        }, ACTION_LOADING_DELAY_MS)\n      );\n    };\n    const abortMatchingActions = (predicate, notify) => {\n      let changed = false;\n      for (const [executionId, activeAction] of activeActions) {\n        if (!predicate(activeAction, executionId)) {\n          continue;\n        }\n        activeAction.abortController.abort();\n        activeActions.delete(executionId);\n        clearActionLoadingPresentation(executionId);\n        actionController.finish(activeAction.execution);\n        changed = true;\n      }\n      if (changed && notify) {\n        publish("action");\n      }\n      return changed;\n    };\n    const abortAllActions = (notify) => {\n      let changed = abortMatchingActions(() => true, false);\n      actionController.reset();\n      if (Object.keys(actionProgress).length > 0) {\n        actionProgress = {};\n        changed = true;\n      }\n      if (changed && notify) {\n        publish("action");\n      }\n    };\n    const removeActionPresentation = (componentId) => {\n      let changed = false;\n      if (Object.prototype.hasOwnProperty.call(actionErrors, componentId)) {\n        actionErrors = { ...actionErrors };\n        delete actionErrors[componentId];\n        changed = true;\n      }\n      if (Object.prototype.hasOwnProperty.call(actionProgress, componentId)) {\n        actionProgress = { ...actionProgress };\n        delete actionProgress[componentId];\n        changed = true;\n      }\n      return changed;\n    };\n    const expandOutputsForUpdatedState = (stateKeys, nextState) => {\n      for (const stateKey of stateKeys) {\n        for (const component of outputComponentsByStateKey.get(stateKey) ?? []) {\n          if (getUiGraphOutputRenderModel(nextState, stateKey, component.renderAs ?? "text").hasValue) {\n            collapsedOutputComponentIds.delete(component.id);\n          }\n        }\n      }\n    };\n    const stampChatResponseAtBrowserReceipt = (component, statePatch, responseTrace) => {\n      if (component.type !== "chat" || !statePatch) {\n        return statePatch;\n      }\n      const messagesStateKey = getUiGraphChatMessagesStateKey(component.id);\n      const messages = statePatch[messagesStateKey];\n      if (!Array.isArray(messages) || messages.length === 0) {\n        return statePatch;\n      }\n      const lastMessage = messages[messages.length - 1];\n      if (!lastMessage || typeof lastMessage !== "object" || lastMessage.role !== "assistant" || typeof lastMessage.content !== "string") {\n        return statePatch;\n      }\n      return {\n        ...statePatch,\n        [messagesStateKey]: [\n          ...messages.slice(0, -1),\n          {\n            ...lastMessage,\n            timestamp: (/* @__PURE__ */ new Date()).toISOString(),\n            ...component.allowResponseInspection && responseTrace != null ? { responseTraceId: responseTrace.traceId } : {}\n          }\n        ]\n      };\n    };\n    updateSnapshot();\n    return {\n      abortActions() {\n        abortAllActions(true);\n      },\n      cancelAction(componentId) {\n        if (abortMatchingActions(({ execution }) => execution.componentId === componentId, false)) {\n          removeActionPresentation(componentId);\n          publish("action");\n        }\n      },\n      detachActions() {\n        for (const executionId of activeActions.keys()) {\n          clearActionLoadingPresentation(executionId);\n        }\n        activeActions.clear();\n        actionController.reset();\n        actionProgress = {};\n        updateSnapshot();\n      },\n      getSnapshot() {\n        return snapshot;\n      },\n      reset() {\n        abortAllActions(false);\n        state = { ...initialState };\n        actionErrors = {};\n        actionProgress = {};\n        collapsedOutputComponentIds.clear();\n        publish("state");\n      },\n      async runAction(component, runner) {\n        const execution = actionController.begin(component);\n        if (!execution) {\n          return;\n        }\n        const abortController = new AbortController();\n        activeActions.set(execution.id, { abortController, execution });\n        delayActionLoadingPresentation(execution);\n        removeActionPresentation(component.id);\n        publish("action");\n        try {\n          const result = await runner({\n            abortOtherActions: () => abortMatchingActions((_activeAction, executionId) => executionId !== execution.id, true),\n            componentId: component.id,\n            reportProgress: (progress) => {\n              if (!actionController.isCurrent(execution)) return;\n              const normalized = normalizeGraphProgress(progress);\n              if (!normalized) return;\n              actionProgress = { ...actionProgress, [component.id]: normalized };\n              publish("action");\n            },\n            signal: abortController.signal,\n            state: getUiGraphComponentActionState(component, state)\n          });\n          if (!actionController.isCurrent(execution)) {\n            return;\n          }\n          const statePatch = actionController.resolveStatePatch(\n            execution,\n            stampChatResponseAtBrowserReceipt(component, result.statePatch, result.responseTrace)\n          );\n          if (statePatch) {\n            const nextState = applyUiGraphStatePatch(state, statePatch);\n            expandOutputsForUpdatedState(Object.keys(statePatch), nextState);\n            state = nextState;\n          }\n        } catch (error) {\n          if (!abortController.signal.aborted && actionController.isCurrent(execution)) {\n            actionErrors = {\n              ...actionErrors,\n              [component.id]: error instanceof Error ? error.message : String(error)\n            };\n          }\n        } finally {\n          if (activeActions.delete(execution.id)) {\n            clearActionLoadingPresentation(execution.id);\n            actionController.finish(execution);\n            if (Object.prototype.hasOwnProperty.call(actionProgress, component.id)) {\n              actionProgress = { ...actionProgress };\n              delete actionProgress[component.id];\n            }\n            publish("action");\n          }\n        }\n      },\n      setUiGraph(nextUiGraph) {\n        if (nextUiGraph.id !== uiGraphId) {\n          abortAllActions(false);\n          uiGraphId = nextUiGraph.id;\n          initialState = initialStateOverride ?? getUiGraphInitialState(nextUiGraph);\n          state = { ...initialState };\n          actionErrors = {};\n          actionProgress = {};\n          outputComponentIds = getUiGraphOutputComponentIds(nextUiGraph);\n          outputComponentsByStateKey = getUiGraphOutputComponentsByStateKey(nextUiGraph);\n          actionComponentsById = getUiGraphActionComponentsById(nextUiGraph);\n          collapsedOutputComponentIds.clear();\n          publish("graph");\n          return;\n        }\n        if (!initialStateOverride) {\n          initialState = getUiGraphInitialState(nextUiGraph);\n        }\n        const normalizedState = normalizeUiGraphDropdownState(nextUiGraph, state);\n        const stateChanged = normalizedState !== state;\n        if (stateChanged) {\n          state = normalizedState;\n        }\n        outputComponentIds = getUiGraphOutputComponentIds(nextUiGraph);\n        outputComponentsByStateKey = getUiGraphOutputComponentsByStateKey(nextUiGraph);\n        const collapsedOutputIdsBeforeCleanup = collapsedOutputComponentIds.size;\n        for (const componentId of collapsedOutputComponentIds) {\n          if (!outputComponentIds.has(componentId)) {\n            collapsedOutputComponentIds.delete(componentId);\n          }\n        }\n        const nextActionComponentsById = getUiGraphActionComponentsById(nextUiGraph);\n        const actionComponentIds = new Set(nextActionComponentsById.keys());\n        const changedActionComponentIds = new Set(\n          [...nextActionComponentsById].flatMap(([componentId, component]) => {\n            const previousComponent = actionComponentsById.get(componentId);\n            return previousComponent && !areUiGraphActionComponentsExecutionEquivalent(previousComponent, component) ? [componentId] : [];\n          })\n        );\n        let changed = collapsedOutputComponentIds.size !== collapsedOutputIdsBeforeCleanup || stateChanged || abortMatchingActions(\n          ({ execution }) => !actionComponentIds.has(execution.componentId) || changedActionComponentIds.has(execution.componentId),\n          false\n        );\n        for (const componentId of changedActionComponentIds) {\n          changed = removeActionPresentation(componentId) || changed;\n        }\n        actionComponentsById = nextActionComponentsById;\n        const remainingErrors = Object.fromEntries(\n          Object.entries(actionErrors).filter(([componentId]) => actionComponentIds.has(componentId))\n        );\n        const remainingProgress = Object.fromEntries(\n          Object.entries(actionProgress).filter(([componentId]) => actionComponentIds.has(componentId))\n        );\n        if (Object.keys(remainingErrors).length !== Object.keys(actionErrors).length) {\n          actionErrors = remainingErrors;\n          changed = true;\n        }\n        if (Object.keys(remainingProgress).length !== Object.keys(actionProgress).length) {\n          actionProgress = remainingProgress;\n          changed = true;\n        }\n        if (changed) {\n          publish("graph");\n        }\n      },\n      subscribe(listener) {\n        listeners.add(listener);\n        return () => listeners.delete(listener);\n      },\n      toggleOutputCollapsed(componentId) {\n        if (!outputComponentIds.has(componentId)) {\n          return;\n        }\n        if (collapsedOutputComponentIds.has(componentId)) {\n          collapsedOutputComponentIds.delete(componentId);\n        } else {\n          collapsedOutputComponentIds.add(componentId);\n        }\n        publish("presentation");\n      },\n      updateState(stateKey, value) {\n        actionController.noteStateWrite(stateKey);\n        const nextState = { ...state, [stateKey]: value };\n        expandOutputsForUpdatedState([stateKey], nextState);\n        state = nextState;\n        publish("state");\n      },\n      updateStatePatch(statePatch) {\n        for (const stateKey of Object.keys(statePatch)) {\n          actionController.noteStateWrite(stateKey);\n        }\n        const nextState = applyUiGraphStatePatch(state, statePatch);\n        expandOutputsForUpdatedState(Object.keys(statePatch), nextState);\n        state = nextState;\n        publish("state");\n      }\n    };\n  }\n  function getUiGraphOutputComponentIds(uiGraph) {\n    return new Set(\n      uiGraph.components.filter((component) => component.type === "output").map((component) => component.id)\n    );\n  }\n  function getUiGraphOutputComponentsByStateKey(uiGraph) {\n    const componentsByStateKey = /* @__PURE__ */ new Map();\n    for (const component of uiGraph.components) {\n      if (component.type !== "output") continue;\n      const components = componentsByStateKey.get(component.stateKey) ?? [];\n      components.push(component);\n      componentsByStateKey.set(component.stateKey, components);\n    }\n    return componentsByStateKey;\n  }\n  function normalizeUiGraphDropdownState(uiGraph, state) {\n    let normalizedState;\n    for (const component of uiGraph.components) {\n      if (component.type !== "dropdown") {\n        continue;\n      }\n      const selectedValue = `${state[component.stateKey] ?? ""}`;\n      if (selectedValue === "" || component.items.some((item) => item.value === selectedValue)) {\n        continue;\n      }\n      normalizedState ??= { ...state };\n      normalizedState[component.stateKey] = "";\n    }\n    return normalizedState ?? state;\n  }\n  function getUiGraphComponentRenderModel(component, state) {\n    switch (component.type) {\n      case "text":\n        return { component, text: component.text, type: "text" };\n      case "markdown":\n        return { component, markdown: component.markdown, type: "markdown" };\n      case "gap":\n        return { component, size: component.size, type: "gap" };\n      case "input":\n      case "textarea":\n        return {\n          component,\n          label: getUiGraphComponentLabel(component),\n          type: component.type,\n          value: `${state[component.stateKey] ?? component.defaultValue ?? ""}`\n        };\n      case "dropdown": {\n        const selectedValue = `${state[component.stateKey] ?? ""}`;\n        return {\n          component,\n          items: component.items,\n          label: getUiGraphComponentLabel(component),\n          type: "dropdown",\n          value: component.items.some((item) => item.value === selectedValue) ? selectedValue : ""\n        };\n      }\n      case "button":\n        return { component, label: component.label, type: "button" };\n      case "chat":\n        return {\n          component,\n          draft: `${state[getUiGraphChatDraftStateKey(component.id)] ?? ""}`,\n          messages: getUiGraphChatMessages(component.id, state),\n          pins: getUiGraphChatPins(component.id, state),\n          type: "chat"\n        };\n      case "output":\n        return {\n          component,\n          label: getUiGraphComponentLabel(component),\n          output: getUiGraphOutputRenderModel(state, component.stateKey, component.renderAs ?? "text"),\n          type: "output"\n        };\n    }\n  }\n  function getUiGraphComponentLabel(component) {\n    return component.label || component.stateKey;\n  }\n  function getUiGraphOutputRenderModel(state, stateKey, renderAs) {\n    const value = state[stateKey];\n    const renderedValue = renderUiGraphOutputValue(value, renderAs);\n    const hasStateValue = hasUiGraphStateValue(state, stateKey);\n    const hasImageValue = hasStateValue && value != null && value !== "";\n    const imageSource = hasImageValue && renderAs === "image" ? getUiGraphImageSource(value) : void 0;\n    const imageErrorMessage = hasImageValue && renderAs === "image" && !imageSource ? "Expected an image URL or base64 image." : void 0;\n    const hasValue = renderAs === "image" ? imageSource != null || imageErrorMessage != null : renderedValue.trim().length > 0;\n    return {\n      hasValue,\n      ...imageErrorMessage ? { imageErrorMessage } : {},\n      ...imageSource ? { imageSource } : {},\n      ...hasValue && renderAs === "json" ? { jsonDownloadValue: renderedValue } : {},\n      renderedValue,\n      renderAs\n    };\n  }\n  function hasUiGraphStateValue(state, key) {\n    return Object.prototype.hasOwnProperty.call(state, key) && state[key] !== void 0;\n  }\n  function renderUiGraphOutputValue(value, renderAs) {\n    if (renderAs === "json") {\n      return stringifyUiGraphValue(value) ?? "";\n    }\n    return typeof value === "string" ? value : value == null ? "" : stringifyUiGraphValue(value) ?? "";\n  }\n  function getUiGraphProgressiveJsonOutputChunks(value) {\n    if (value.length < UI_GRAPH_PROGRESSIVE_JSON_OUTPUT_THRESHOLD) {\n      return void 0;\n    }\n    const chunks = [];\n    let offset = 0;\n    let chunkLength = UI_GRAPH_PROGRESSIVE_JSON_OUTPUT_INITIAL_CHARS;\n    while (offset < value.length) {\n      let nextOffset = Math.min(value.length, offset + chunkLength);\n      if (nextOffset < value.length && isHighSurrogate(value.charCodeAt(nextOffset - 1)) && isLowSurrogate(value.charCodeAt(nextOffset))) {\n        nextOffset += 1;\n      }\n      chunks.push(value.slice(offset, nextOffset));\n      offset = nextOffset;\n      chunkLength = UI_GRAPH_PROGRESSIVE_JSON_OUTPUT_CHUNK_CHARS;\n    }\n    return chunks;\n  }\n  function isHighSurrogate(value) {\n    return value >= 55296 && value <= 56319;\n  }\n  function isLowSurrogate(value) {\n    return value >= 56320 && value <= 57343;\n  }\n  function getUiGraphImageSource(value) {\n    if (typeof value !== "string") {\n      return void 0;\n    }\n    const source = value.trim();\n    if (!source) {\n      return void 0;\n    }\n    const dataUrl = IMAGE_DATA_URL_PATTERN.exec(source);\n    if (dataUrl) {\n      const base642 = normalizeBase64(dataUrl[2]);\n      return base642 ? `data:${normalizeImageMediaType(dataUrl[1])};base64,${base642}` : void 0;\n    }\n    const base64 = normalizeBase64(source);\n    const inferredMediaType = base64 ? inferBase64ImageMediaType(base64) : void 0;\n    if (base64 && inferredMediaType) {\n      return `data:${inferredMediaType};base64,${base64}`;\n    }\n    if (!IMAGE_URL_PATTERN.test(source) && !RELATIVE_IMAGE_PATH_PATTERN.test(source)) {\n      return void 0;\n    }\n    try {\n      const protocol = new URL(source, "https://rivet.invalid").protocol;\n      return protocol === "http:" || protocol === "https:" || protocol === "blob:" ? source : void 0;\n    } catch {\n      return void 0;\n    }\n  }\n  function normalizeBase64(value) {\n    const compact = value.replace(/\\s/g, "");\n    return compact.length >= 8 && compact.length % 4 !== 1 && BASE64_PATTERN.test(compact) ? compact : void 0;\n  }\n  function normalizeImageMediaType(mediaType) {\n    const normalized = mediaType.toLowerCase();\n    return normalized === "image/jpg" ? "image/jpeg" : normalized;\n  }\n  function inferBase64ImageMediaType(value) {\n    if (value.startsWith("iVBORw0KGgo")) return "image/png";\n    if (value.startsWith("/9j/")) return "image/jpeg";\n    if (value.startsWith("R0lGODdh") || value.startsWith("R0lGODlh")) return "image/gif";\n    return void 0;\n  }\n  function stringifyUiGraphValue(value) {\n    try {\n      return JSON.stringify(value, null, 2);\n    } catch {\n      return "[Unserializable value]";\n    }\n  }\n  function getUiGraphJsonOutputFilename(appName, date = /* @__PURE__ */ new Date()) {\n    const safeName = appName.trim().replace(/[<>:"/\\\\|?*\\u0000-\\u001F]/g, "-").replace(/\\s+/g, " ").slice(0, 80);\n    return `${safeName || "Rivet web app"} ${formatUiGraphDownloadDateTime(date)}.json`;\n  }\n  function formatUiGraphDownloadDateTime(date) {\n    const pad = (value) => `${value}`.padStart(2, "0");\n    return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())} ${pad(date.getHours())}-${pad(\n      date.getMinutes()\n    )}-${pad(date.getSeconds())}`;\n  }\n  function applyUiGraphStatePatch(state, statePatch) {\n    return statePatch ? { ...state, ...statePatch } : state;\n  }\n\n  // ../core/src/model/UiGraphBrowserRuntime.ts\n  var OUTPUT_MAX_HEIGHT_PX = 800;\n  var OUTPUT_MAX_VIEWPORT_HEIGHT_RATIO = 0.8;\n  var OUTPUT_RESIZE_MIN_HEIGHT_PROPERTY = "--rivet-web-app-output-resize-min-height";\n  var OUTPUT_RESIZE_MAX_HEIGHT_PROPERTY = "--rivet-web-app-output-resize-max-height";\n  var CHAT_SEARCH_MATCH_CLASS = "rivet-web-app-chat-search-match";\n  var CHAT_SEARCH_ACTIVE_MATCH_CLASS = "rivet-web-app-chat-search-match-active";\n  var UI_GRAPH_CHAT_STORAGE_PREFIX = "rivet-web-app-chat-state:v1";\n  var UI_GRAPH_APP_STORAGE_PREFIX = "rivet-web-app-storage:v1";\n  var UI_GRAPH_RESPONSE_TRACE_STORAGE_PREFIX = "rivet-web-app-response-traces:v1";\n  var responseTraceMemoryFallback = /* @__PURE__ */ new Map();\n  var unsyncedResponseTraceKeysByStorage = /* @__PURE__ */ new WeakMap();\n  var escapeRegularExpression = (value) => value.replace(/[.*+?^${}()|[\\]\\\\]/g, "\\\\$&");\n  var getPixelValue = (value, fallback) => {\n    const parsed = Number.parseFloat(value);\n    return Number.isFinite(parsed) ? parsed : fallback;\n  };\n  var setStyleProperty = (element, property, value) => {\n    if (element.style.getPropertyValue(property) !== value) {\n      element.style.setProperty(property, value);\n    }\n  };\n  var isRecord3 = (value) => typeof value === "object" && value !== null && !Array.isArray(value);\n  var getDefaultUiGraphBrowserStorage = () => {\n    try {\n      return globalThis.localStorage ?? globalThis.window?.localStorage;\n    } catch {\n      return void 0;\n    }\n  };\n  var getDefaultUiGraphStorageLocation = () => {\n    try {\n      return globalThis.location ?? globalThis.window?.location;\n    } catch {\n      return void 0;\n    }\n  };\n  function getUiGraphChatMessagePresentations(messages, options = {}) {\n    const { locales, timeZone } = options;\n    const timeFormatter = new Intl.DateTimeFormat(locales, {\n      hour: "2-digit",\n      hourCycle: "h23",\n      minute: "2-digit",\n      ...timeZone ? { timeZone } : {}\n    });\n    const dateFormatter = new Intl.DateTimeFormat(locales, {\n      day: "numeric",\n      month: "long",\n      year: "numeric",\n      ...timeZone ? { timeZone } : {}\n    });\n    const dateKeyFormatter = new Intl.DateTimeFormat("en-CA", {\n      day: "2-digit",\n      month: "2-digit",\n      year: "numeric",\n      ...timeZone ? { timeZone } : {}\n    });\n    const entries = messages.map((message) => {\n      const timestamp = parseUiGraphChatTimestamp(message.timestamp);\n      if (!timestamp) return void 0;\n      return {\n        dateKey: dateKeyFormatter.format(timestamp),\n        dateTime: timestamp.toISOString(),\n        dateLabel: dateFormatter.format(timestamp),\n        timeLabel: timeFormatter.format(timestamp),\n        timestamp\n      };\n    });\n    const hasMultipleDates = new Set(entries.flatMap((entry) => entry ? [entry.dateKey] : [])).size > 1;\n    let previousDateKey;\n    let previousUserTimestamp;\n    return entries.map((entry, index) => {\n      const message = messages[index];\n      if (message.role === "user") {\n        previousUserTimestamp = entry?.timestamp;\n      }\n      if (!entry) return {};\n      const dateSeparator = hasMultipleDates && previousDateKey != null && previousDateKey !== entry.dateKey ? { dateTime: entry.dateTime, label: entry.dateLabel } : void 0;\n      previousDateKey = entry.dateKey;\n      const elapsedSincePreviousUserMessage = message.role === "assistant" && previousUserTimestamp ? formatUiGraphChatElapsedSeconds(entry.timestamp.getTime() - previousUserTimestamp.getTime()) : void 0;\n      return {\n        ...dateSeparator ? { dateSeparator } : {},\n        timestamp: {\n          dateTime: entry.dateTime,\n          ...elapsedSincePreviousUserMessage ? { elapsedSincePreviousUserMessage } : {},\n          label: entry.timeLabel\n        }\n      };\n    });\n  }\n  var formatUiGraphChatElapsedSeconds = (elapsedMilliseconds) => {\n    if (!Number.isFinite(elapsedMilliseconds) || elapsedMilliseconds < 0) return void 0;\n    const seconds = Math.round(elapsedMilliseconds / 1e3 * 10) / 10;\n    return `${Number.isInteger(seconds) ? seconds : seconds.toFixed(1)} seconds after previous user message`;\n  };\n  var parseUiGraphChatTimestamp = (value) => {\n    if (typeof value !== "string") return void 0;\n    const timestamp = new Date(value);\n    return Number.isNaN(timestamp.getTime()) ? void 0 : timestamp;\n  };\n  function getUiGraphChatStorageKey(uiGraph, location = getDefaultUiGraphStorageLocation()) {\n    if (!location?.origin) {\n      return void 0;\n    }\n    const pathname = location.pathname.replace(/\\/+$/, "") || "/";\n    return [\n      UI_GRAPH_CHAT_STORAGE_PREFIX,\n      encodeURIComponent(location.origin),\n      encodeURIComponent(pathname),\n      encodeURIComponent(uiGraph.id)\n    ].join(":");\n  }\n  function getUiGraphWebAppStorageKey(uiGraph, location = getDefaultUiGraphStorageLocation()) {\n    if (!location?.origin) {\n      return void 0;\n    }\n    const pathname = location.pathname.replace(/\\/+$/, "") || "/";\n    return [\n      UI_GRAPH_APP_STORAGE_PREFIX,\n      encodeURIComponent(location.origin),\n      encodeURIComponent(pathname),\n      encodeURIComponent(uiGraph.id)\n    ].join(":");\n  }\n  function getUiGraphResponseTraceStorageKey(uiGraph, componentId, location = getDefaultUiGraphStorageLocation()) {\n    if (!location?.origin) return void 0;\n    const pathname = location.pathname.replace(/\\/+$/, "") || "/";\n    return [\n      UI_GRAPH_RESPONSE_TRACE_STORAGE_PREFIX,\n      encodeURIComponent(location.origin),\n      encodeURIComponent(pathname),\n      encodeURIComponent(uiGraph.id),\n      encodeURIComponent(componentId)\n    ].join(":");\n  }\n  var removeUiGraphChatResponseTraceIds = (messages) => messages.map((message) => {\n    if (message.responseTraceId == null) return message;\n    const sanitized = { ...message };\n    delete sanitized.responseTraceId;\n    return sanitized;\n  });\n  var purgeDisabledUiGraphResponseTraces = (uiGraph, storage, location) => {\n    for (const component of uiGraph.components) {\n      if (component.type !== "chat" || component.allowResponseInspection === true) continue;\n      const key = getUiGraphResponseTraceStorageKey(uiGraph, component.id, location) ?? `memory:${uiGraph.id}:${component.id}`;\n      persistUiGraphResponseTraces(key, [], storage);\n    }\n  };\n  function persistUiGraphResponseTraces(key, traces, storage) {\n    responseTraceMemoryFallback.set(key, traces);\n    if (!storage || key.startsWith("memory:")) return;\n    try {\n      if (traces.length === 0) storage.removeItem(key);\n      else storage.setItem(key, JSON.stringify(traces));\n      unsyncedResponseTraceKeysByStorage.get(storage)?.delete(key);\n    } catch {\n      const unsyncedKeys = unsyncedResponseTraceKeysByStorage.get(storage) ?? /* @__PURE__ */ new Set();\n      unsyncedKeys.add(key);\n      unsyncedResponseTraceKeysByStorage.set(storage, unsyncedKeys);\n    }\n  }\n  function loadUiGraphWebAppStorage(uiGraph, storage = getDefaultUiGraphBrowserStorage(), location) {\n    const key = getUiGraphWebAppStorageKey(uiGraph, location);\n    if (!key || !storage) return {};\n    try {\n      const serialized = storage.getItem(key);\n      if (!serialized) return {};\n      const value = JSON.parse(serialized);\n      return isRecord3(value) ? value : {};\n    } catch {\n      return {};\n    }\n  }\n  async function applyUiGraphWebAppStorageActionPatchAsync(patch, actionNumber, appliedActionByKey, persist) {\n    const applicablePatch = Object.fromEntries(\n      Object.entries(patch).filter(([key]) => actionNumber >= (appliedActionByKey.get(key) ?? 0))\n    );\n    const applicableKeys = Object.keys(applicablePatch);\n    if (applicableKeys.length === 0) return applicablePatch;\n    await persist(applicablePatch);\n    for (const key of applicableKeys) appliedActionByKey.set(key, actionNumber);\n    return applicablePatch;\n  }\n  function getUiGraphChatPersistentState(uiGraph, state) {\n    const persistentState = {};\n    for (const component of uiGraph.components) {\n      if (component.type !== "chat") continue;\n      const draftStateKey = getUiGraphChatDraftStateKey(component.id);\n      const messagesStateKey = getUiGraphChatMessagesStateKey(component.id);\n      const pinsStateKey = getUiGraphChatPinsStateKey(component.id);\n      const draft = state[draftStateKey];\n      const messages = getUiGraphChatMessages(component.id, state);\n      const persistedMessages = component.allowResponseInspection === true ? messages : removeUiGraphChatResponseTraceIds(messages);\n      const pins = getUiGraphChatPins(component.id, state).map((pin) => pin.messageIndex);\n      if (typeof draft === "string" && draft) {\n        persistentState[draftStateKey] = draft;\n      }\n      if (persistedMessages.length > 0) {\n        persistentState[messagesStateKey] = persistedMessages;\n      }\n      if (pins.length > 0) {\n        persistentState[pinsStateKey] = pins;\n      }\n    }\n    return persistentState;\n  }\n  function hasUiGraphChatPersistentStateChanged(uiGraph, previousState, nextState) {\n    return uiGraph.components.some((component) => {\n      if (component.type !== "chat") return false;\n      return previousState[getUiGraphChatDraftStateKey(component.id)] !== nextState[getUiGraphChatDraftStateKey(component.id)] || previousState[getUiGraphChatMessagesStateKey(component.id)] !== nextState[getUiGraphChatMessagesStateKey(component.id)] || previousState[getUiGraphChatPinsStateKey(component.id)] !== nextState[getUiGraphChatPinsStateKey(component.id)];\n    });\n  }\n  function loadUiGraphChatPersistentState(uiGraph, storage = getDefaultUiGraphBrowserStorage(), location) {\n    const key = getUiGraphChatStorageKey(uiGraph, location);\n    if (!key || !storage) {\n      return {};\n    }\n    try {\n      const serializedState = storage.getItem(key);\n      if (!serializedState) {\n        return {};\n      }\n      const storedState = JSON.parse(serializedState);\n      return isRecord3(storedState) ? getUiGraphChatPersistentState(uiGraph, storedState) : {};\n    } catch {\n      return {};\n    }\n  }\n  function saveUiGraphChatPersistentState(uiGraph, state, storage = getDefaultUiGraphBrowserStorage(), location) {\n    purgeDisabledUiGraphResponseTraces(uiGraph, storage, location);\n    const key = getUiGraphChatStorageKey(uiGraph, location);\n    if (!key || !storage) {\n      return;\n    }\n    try {\n      const persistentState = getUiGraphChatPersistentState(uiGraph, state);\n      if (Object.keys(persistentState).length === 0) {\n        storage.removeItem(key);\n        return;\n      }\n      const serializedState = JSON.stringify(persistentState);\n      if (storage.getItem(key) !== serializedState) {\n        storage.setItem(key, serializedState);\n      }\n    } catch {\n    }\n  }\n  function observeUiGraphOutputResizeBounds(output) {\n    const ownerWindow = output.ownerDocument.defaultView;\n    let scheduledFrame;\n    const sync = () => {\n      scheduledFrame = void 0;\n      if (!ownerWindow) return;\n      const header = output.querySelector(".rivet-web-app-output-header");\n      const content = output.querySelector(".rivet-web-app-output-content");\n      const body = output.querySelector(".rivet-web-app-output-content-body");\n      if (!header || !content || !body) {\n        output.classList.remove("rivet-web-app-output-resizable");\n        output.classList.remove("rivet-web-app-output-scrollable");\n        output.style.removeProperty(OUTPUT_RESIZE_MIN_HEIGHT_PROPERTY);\n        output.style.removeProperty(OUTPUT_RESIZE_MAX_HEIGHT_PROPERTY);\n        return;\n      }\n      const bodyStyle = ownerWindow.getComputedStyle(body);\n      const lineHeight = getPixelValue(bodyStyle.lineHeight, getPixelValue(bodyStyle.fontSize, 16) * 1.5);\n      const contentStyle = ownerWindow.getComputedStyle(content);\n      const contentPadding = getPixelValue(contentStyle.paddingTop, 0) + getPixelValue(contentStyle.paddingBottom, 0);\n      const verticalBorder = Math.max(0, output.offsetHeight - output.clientHeight);\n      const minimumHeight = Math.ceil(\n        header.getBoundingClientRect().height + contentPadding + lineHeight + verticalBorder\n      );\n      const naturalHeight = Math.ceil(\n        header.getBoundingClientRect().height + contentPadding + Math.max(body.scrollHeight, lineHeight) + verticalBorder\n      );\n      const viewportHeight = ownerWindow?.innerHeight ?? OUTPUT_MAX_HEIGHT_PX / OUTPUT_MAX_VIEWPORT_HEIGHT_RATIO;\n      const maximumViewportHeight = Math.min(viewportHeight * OUTPUT_MAX_VIEWPORT_HEIGHT_RATIO, OUTPUT_MAX_HEIGHT_PX);\n      output.classList.toggle("rivet-web-app-output-resizable", naturalHeight > maximumViewportHeight);\n      output.classList.toggle("rivet-web-app-output-scrollable", body.scrollHeight > body.clientHeight);\n      setStyleProperty(output, OUTPUT_RESIZE_MIN_HEIGHT_PROPERTY, `${minimumHeight}px`);\n      setStyleProperty(output, OUTPUT_RESIZE_MAX_HEIGHT_PROPERTY, `${naturalHeight}px`);\n    };\n    const scheduleSync = () => {\n      if (scheduledFrame != null) return;\n      if (ownerWindow?.requestAnimationFrame) {\n        scheduledFrame = ownerWindow.requestAnimationFrame(sync);\n      } else {\n        sync();\n      }\n    };\n    const resizeObserver = ownerWindow?.ResizeObserver ? new ownerWindow.ResizeObserver(scheduleSync) : void 0;\n    resizeObserver?.observe(output);\n    const mutationObserver = ownerWindow?.MutationObserver ? new ownerWindow.MutationObserver(scheduleSync) : void 0;\n    mutationObserver?.observe(output, { childList: true, characterData: true, subtree: true });\n    output.addEventListener("load", scheduleSync, true);\n    ownerWindow?.addEventListener("resize", scheduleSync);\n    sync();\n    return () => {\n      if (scheduledFrame != null) ownerWindow?.cancelAnimationFrame(scheduledFrame);\n      resizeObserver?.disconnect();\n      mutationObserver?.disconnect();\n      output.removeEventListener("load", scheduleSync, true);\n      ownerWindow?.removeEventListener("resize", scheduleSync);\n    };\n  }\n  async function copyUiGraphText(value) {\n    try {\n      await navigator.clipboard.writeText(value);\n      return true;\n    } catch {\n    }\n    const textArea = document.createElement("textarea");\n    textArea.className = "rivet-web-app-clipboard-fallback";\n    textArea.value = value;\n    try {\n      document.body.append(textArea);\n      textArea.select();\n      return document.execCommand?.("copy") ?? false;\n    } catch {\n      return false;\n    } finally {\n      textArea.remove();\n    }\n  }\n  function downloadUiGraphJsonOutput(value, appName) {\n    const url = URL.createObjectURL(new Blob([value], { type: "application/json" }));\n    try {\n      const link = document.createElement("a");\n      link.href = url;\n      link.download = getUiGraphJsonOutputFilename(appName);\n      document.body.append(link);\n      link.click();\n      link.remove();\n    } finally {\n      globalThis.setTimeout(() => URL.revokeObjectURL(url), 0);\n    }\n  }\n  function enhanceUiGraphChatJsonCodeBlocks(root2, appName) {\n    const codeBlocks = root2.querySelectorAll("pre > code.language-json");\n    root2.classList.toggle("rivet-web-app-chat-markdown-has-json", codeBlocks.length > 0);\n    for (const code of codeBlocks) {\n      const pre = code.parentElement;\n      const existingWrapper = pre?.parentElement;\n      if (!pre) {\n        continue;\n      }\n      if (existingWrapper?.classList.contains("rivet-web-app-chat-json-block")) {\n        existingWrapper.dataset.rivetChatJsonAppName = appName;\n        continue;\n      }\n      const value = code.textContent ?? "";\n      const document2 = root2.ownerDocument;\n      const wrapper = document2.createElement("div");\n      wrapper.className = "rivet-web-app-chat-json-block";\n      wrapper.dataset.rivetChatJsonAppName = appName;\n      const actions = document2.createElement("div");\n      actions.className = "rivet-web-app-chat-json-actions";\n      actions.dataset.rivetChatSearchIgnore = "true";\n      const copyButton = document2.createElement("button");\n      copyButton.type = "button";\n      copyButton.className = "rivet-web-app-output-action-button rivet-web-app-output-copy-button";\n      copyButton.title = "Copy JSON";\n      copyButton.setAttribute("aria-label", "Copy JSON");\n      copyButton.addEventListener("click", (event) => {\n        event.stopPropagation();\n        void copyUiGraphText(value);\n      });\n      const downloadButton = document2.createElement("button");\n      downloadButton.type = "button";\n      downloadButton.className = "rivet-web-app-output-action-button rivet-web-app-output-download-button";\n      downloadButton.title = "Download JSON";\n      downloadButton.setAttribute("aria-label", "Download JSON");\n      downloadButton.addEventListener("click", (event) => {\n        event.stopPropagation();\n        downloadUiGraphJsonOutput(value, wrapper.dataset.rivetChatJsonAppName ?? appName);\n      });\n      actions.append(copyButton, downloadButton);\n      pre.replaceWith(wrapper);\n      wrapper.append(pre, actions);\n      const syncScrollableState = () => {\n        wrapper.classList.toggle("rivet-web-app-chat-json-block-scrollable", pre.scrollHeight > pre.clientHeight);\n      };\n      syncScrollableState();\n      root2.ownerDocument.defaultView?.requestAnimationFrame(syncScrollableState);\n    }\n  }\n  function highlightUiGraphChatSearchMatches(messagesElement, query, requestedActiveIndex = 0) {\n    clearUiGraphChatSearchMatches(messagesElement);\n    const searchText = query.trim();\n    if (!searchText) {\n      return { activeIndex: -1, matches: [] };\n    }\n    const document2 = messagesElement.ownerDocument;\n    const textNodes = [];\n    const walker = document2.createTreeWalker(messagesElement, 4);\n    let node = walker.nextNode();\n    while (node) {\n      if (node.parentElement?.closest("script, style, textarea, [data-rivet-chat-search-ignore]")) {\n        node = walker.nextNode();\n        continue;\n      }\n      textNodes.push(node);\n      node = walker.nextNode();\n    }\n    const matches = [];\n    for (const textNode of textNodes) {\n      const value = textNode.data;\n      const ranges = [];\n      const expression = new RegExp(escapeRegularExpression(searchText), "gi");\n      for (const match of value.matchAll(expression)) {\n        if (match.index != null && match[0]) {\n          ranges.push({ start: match.index, end: match.index + match[0].length });\n        }\n      }\n      if (ranges.length === 0 || !textNode.parentNode) {\n        continue;\n      }\n      const fragment = document2.createDocumentFragment();\n      let cursor = 0;\n      for (const range of ranges) {\n        fragment.append(value.slice(cursor, range.start));\n        const match = document2.createElement("mark");\n        match.className = CHAT_SEARCH_MATCH_CLASS;\n        match.textContent = value.slice(range.start, range.end);\n        fragment.append(match);\n        matches.push(match);\n        cursor = range.end;\n      }\n      fragment.append(value.slice(cursor));\n      textNode.parentNode.replaceChild(fragment, textNode);\n    }\n    if (matches.length === 0) {\n      return { activeIndex: -1, matches };\n    }\n    const activeIndex = (requestedActiveIndex % matches.length + matches.length) % matches.length;\n    matches[activeIndex]?.classList.add(CHAT_SEARCH_ACTIVE_MATCH_CLASS);\n    return { activeIndex, matches };\n  }\n  function clearUiGraphChatSearchMatches(messagesElement) {\n    for (const match of messagesElement.querySelectorAll(`.${CHAT_SEARCH_MATCH_CLASS}`)) {\n      match.replaceWith(messagesElement.ownerDocument.createTextNode(match.textContent ?? ""));\n    }\n    messagesElement.normalize();\n  }\n  function revealUiGraphChatElement(messagesElement, element, alignment = "center") {\n    if (!element || !messagesElement.contains(element)) {\n      return;\n    }\n    const containerRect = messagesElement.getBoundingClientRect();\n    const elementRect = element.getBoundingClientRect();\n    const maxScrollTop = Math.max(0, messagesElement.scrollHeight - messagesElement.clientHeight);\n    const offset = elementRect.top - containerRect.top - (alignment === "center" ? (messagesElement.clientHeight - elementRect.height) / 2 : 0);\n    messagesElement.scrollTop = Math.max(0, Math.min(maxScrollTop, messagesElement.scrollTop + offset));\n  }\n  var revealUiGraphChatSearchMatch = revealUiGraphChatElement;\n\n  // ../core/src/model/UiGraphIndexedDbPersistence.ts\n  var CHAT_STATE_RECORD_KEY = "state";\n  var RESPONSE_TRACE_LIMIT = 100;\n  var UiGraphBrowserPersistence = class {\n    #store;\n    #uiGraph;\n    #legacyStorage;\n    #location;\n    #now;\n    #onDiagnostic;\n    #listeners = /* @__PURE__ */ new Set();\n    #warningListeners = /* @__PURE__ */ new Set();\n    #status = { durable: false, reason: "indexeddb-unavailable" };\n    #warning;\n    #disposeStoreSubscription;\n    #responseTraceMutationQueue = Promise.resolve();\n    #useLegacyStorage = true;\n    constructor(uiGraph, options = {}) {\n      this.#uiGraph = uiGraph;\n      this.#legacyStorage = options.legacyStorage ?? getDefaultLegacyStorage();\n      this.#now = options.now ?? Date.now;\n      this.#onDiagnostic = options.onDiagnostic ?? defaultBrowserStorageDiagnostic;\n      this.#location = options.location ?? getDefaultLocation();\n      this.#store = new IndexedDbWebAppBrowserStorage(options);\n    }\n    async initialize() {\n      const location = this.#location;\n      if (!location?.origin) {\n        this.#reportDiagnostic({ operation: "open", outcome: "fallback" });\n        this.#setWarning({\n          code: "indexeddb-unavailable",\n          message: "Saved browser data is unavailable. Changes will last only until this page is closed."\n        });\n        return this.#status;\n      }\n      this.#status = await this.#store.initialize({\n        origin: location.origin,\n        pathname: location.pathname,\n        uiGraphId: String(this.#uiGraph.id)\n      });\n      this.#reportDiagnostic({ operation: "open", outcome: this.#status.durable ? "success" : "fallback" });\n      this.#disposeStoreSubscription = this.#store.subscribe((change) => {\n        for (const listener of this.#listeners) listener(change);\n      });\n      if (!this.#status.durable) {\n        this.#setWarning({\n          code: "indexeddb-unavailable",\n          message: "Saved browser data is unavailable. Changes will use limited legacy storage for this page."\n        });\n        return this.#status;\n      }\n      this.#useLegacyStorage = false;\n      const migratedLegacyRecords = await this.#tryMigrateLegacyRecords();\n      if (migratedLegacyRecords) {\n        try {\n          await this.#cleanupExpiredLegacyRecords();\n          this.#reportDiagnostic({ operation: "cleanup", outcome: "success" });\n        } catch {\n          this.#reportDiagnostic({ operation: "cleanup", outcome: "failure" });\n        }\n        this.#setWarning(void 0);\n      }\n      return this.#status;\n    }\n    get status() {\n      return this.#status;\n    }\n    get warning() {\n      return this.#warning;\n    }\n    subscribe(listener) {\n      this.#listeners.add(listener);\n      return () => this.#listeners.delete(listener);\n    }\n    subscribeWarning(listener) {\n      this.#warningListeners.add(listener);\n      return () => this.#warningListeners.delete(listener);\n    }\n    async loadChatState() {\n      if (this.#useLegacyStorage) {\n        return loadUiGraphChatPersistentState(this.#uiGraph, this.#legacyStorage, this.#location);\n      }\n      const value = await this.#store.get("chat-state", CHAT_STATE_RECORD_KEY);\n      if (isRecord4(value)) return getUiGraphChatPersistentState(this.#uiGraph, value);\n      return await this.#loadLegacyChatStateIfUnmigrated();\n    }\n    async saveChatState(state) {\n      if (this.#useLegacyStorage) {\n        saveUiGraphChatPersistentState(this.#uiGraph, state, this.#legacyStorage, this.#location);\n        return;\n      }\n      await this.#tryMigrateLegacyRecords();\n      const persistentState = getUiGraphChatPersistentState(this.#uiGraph, state);\n      await this.#persist(\n        () => Object.keys(persistentState).length === 0 ? this.#store.delete("chat-state", CHAT_STATE_RECORD_KEY) : this.#store.set("chat-state", CHAT_STATE_RECORD_KEY, cloneRivetStoredValue(persistentState))\n      );\n    }\n    async loadStoredValues() {\n      if (this.#useLegacyStorage) {\n        return cloneRivetStoredValueRecord(\n          loadUiGraphWebAppStorage(this.#uiGraph, this.#legacyStorage, this.#location),\n          "Stored Value browser snapshot"\n        );\n      }\n      const indexedDbValues = Object.fromEntries(\n        (await this.#store.list("stored-values")).map(({ key, value }) => [key, value])\n      );\n      const legacyValues = await this.#loadLegacyStoredValuesIfUnmigrated();\n      return cloneRivetStoredValueRecord({ ...legacyValues, ...indexedDbValues }, "Stored Value browser snapshot");\n    }\n    async loadStoredValue(key) {\n      if (this.#useLegacyStorage) {\n        const values = await this.loadStoredValues();\n        return Object.prototype.hasOwnProperty.call(values, key) ? cloneRivetStoredValue(values[key], "Stored Value browser value") : void 0;\n      }\n      const value = await this.#store.get("stored-values", key);\n      if (value !== void 0) return cloneRivetStoredValue(value, "Stored Value browser value");\n      const legacyValues = await this.#loadLegacyStoredValuesIfUnmigrated();\n      return Object.prototype.hasOwnProperty.call(legacyValues, key) ? cloneRivetStoredValue(legacyValues[key], "Stored Value browser value") : void 0;\n    }\n    async applyStoredValuePatch(patch, options = {}) {\n      const portablePatch = cloneRivetStoredValueRecord(patch, "Stored Value browser patch");\n      if (this.#useLegacyStorage) {\n        const legacyKey = getUiGraphWebAppStorageKey(this.#uiGraph, this.#location);\n        if (!legacyKey || !this.#legacyStorage) throw new Error("Browser storage is unavailable for this web app.");\n        const current = await this.loadStoredValues();\n        const next = cloneRivetStoredValueRecord({ ...current, ...portablePatch }, "Stored Value browser snapshot");\n        this.#legacyStorage.setItem(legacyKey, JSON.stringify(next));\n        return;\n      }\n      await this.#tryMigrateLegacyRecords();\n      await this.#persist(\n        () => this.#store.commitBatch(\n          Object.entries(portablePatch).map(([key, value]) => ({ key, namespace: "stored-values", value }))\n        ),\n        options.requireDurable === true\n      );\n    }\n    async loadResponseTrace(componentId, traceId) {\n      const traces = await this.#loadResponseTraces(componentId);\n      return traces.find((trace) => trace.traceId === traceId);\n    }\n    async saveResponseTrace(componentId, trace) {\n      if (!isAgentResponseTrace(trace) || !this.#isResponseInspectionEnabled(componentId)) return;\n      await this.#enqueueResponseTraceMutation(async () => {\n        const traces = (await this.#loadResponseTraces(componentId)).filter(\n          (candidate) => candidate.traceId !== trace.traceId\n        );\n        traces.push(trace);\n        await this.#saveResponseTraces(componentId, traces.slice(-RESPONSE_TRACE_LIMIT));\n      });\n    }\n    async pruneResponseTraces(state) {\n      await this.#enqueueResponseTraceMutation(async () => {\n        for (const component of this.#uiGraph.components) {\n          if (component.type !== "chat") continue;\n          const referenced = component.allowResponseInspection === true ? new Set(\n            getUiGraphChatMessages(component.id, state).flatMap(\n              (message) => typeof message.responseTraceId === "string" ? [message.responseTraceId] : []\n            )\n          ) : /* @__PURE__ */ new Set();\n          const retained = (await this.#loadResponseTraces(String(component.id))).filter(\n            (trace) => referenced.has(trace.traceId)\n          );\n          await this.#saveResponseTraces(String(component.id), retained);\n        }\n      });\n    }\n    reportTransportIncompatibility(message) {\n      this.#reportDiagnostic({ operation: "transport", outcome: "failure" });\n      if (this.#warning && this.#warning.code !== "transport-incompatible") return;\n      this.#setWarning({ code: "transport-incompatible", message });\n    }\n    clearTransportIncompatibility() {\n      if (this.#warning?.code === "transport-incompatible") {\n        this.#setWarning(void 0);\n      }\n    }\n    async estimateUsage() {\n      return await this.#store.estimateUsage();\n    }\n    dispose() {\n      this.#disposeStoreSubscription?.();\n      this.#disposeStoreSubscription = void 0;\n      this.#listeners.clear();\n      this.#warningListeners.clear();\n      this.#store.dispose();\n    }\n    async #loadResponseTraces(componentId) {\n      if (!this.#isResponseInspectionEnabled(componentId)) return [];\n      if (this.#useLegacyStorage) return this.#loadLegacyResponseTraces(componentId);\n      const value = await this.#store.get("response-traces", componentId);\n      return Array.isArray(value) ? value.filter(isAgentResponseTrace).slice(-RESPONSE_TRACE_LIMIT) : [];\n    }\n    async #saveResponseTraces(componentId, traces) {\n      if (this.#useLegacyStorage) {\n        const legacyKey = getUiGraphResponseTraceStorageKey(this.#uiGraph, componentId, this.#location);\n        if (!legacyKey || !this.#legacyStorage) return;\n        if (traces.length === 0) this.#legacyStorage.removeItem(legacyKey);\n        else this.#legacyStorage.setItem(legacyKey, JSON.stringify(traces));\n        return;\n      }\n      await this.#persist(\n        () => traces.length === 0 ? this.#store.delete("response-traces", componentId) : this.#store.set("response-traces", componentId, cloneRivetStoredValue(traces))\n      );\n    }\n    #loadLegacyResponseTraces(componentId) {\n      const legacyKey = getUiGraphResponseTraceStorageKey(this.#uiGraph, componentId, this.#location);\n      if (!legacyKey || !this.#legacyStorage) return [];\n      try {\n        const raw = this.#legacyStorage.getItem(legacyKey);\n        const value = raw ? JSON.parse(raw) : void 0;\n        return Array.isArray(value) ? value.filter(isAgentResponseTrace).slice(-RESPONSE_TRACE_LIMIT) : [];\n      } catch {\n        return [];\n      }\n    }\n    #isResponseInspectionEnabled(componentId) {\n      return this.#uiGraph.components.some(\n        (component) => component.type === "chat" && String(component.id) === componentId && component.allowResponseInspection === true\n      );\n    }\n    async #enqueueResponseTraceMutation(operation) {\n      const result = this.#responseTraceMutationQueue.catch(() => void 0).then(operation);\n      this.#responseTraceMutationQueue = result.then(\n        () => void 0,\n        () => void 0\n      );\n      return await result;\n    }\n    async #tryMigrateLegacyRecords() {\n      try {\n        if (!await this.#migrateLegacyRecords()) {\n          this.#reportDiagnostic({ operation: "migration", outcome: "success" });\n          if (this.#warning?.code === "migration-failed") this.#setWarning(void 0);\n          return true;\n        }\n      } catch {\n      }\n      this.#reportMigrationFailure(\n        "Existing browser data could not be migrated safely. Rivet kept the original copy and will try again later."\n      );\n      return false;\n    }\n    #reportMigrationFailure(message) {\n      this.#reportDiagnostic({ operation: "migration", outcome: "failure" });\n      this.#setWarning({ code: "migration-failed", message });\n    }\n    async #loadLegacyChatStateIfUnmigrated() {\n      const legacyKey = getUiGraphChatStorageKey(this.#uiGraph, this.#location);\n      if (!legacyKey || await this.#store.getMigration(legacyKey)) return {};\n      const raw = this.#legacyStorage?.getItem(legacyKey);\n      if (raw == null) return {};\n      try {\n        const value = JSON.parse(raw);\n        assertValidLegacyChatState(this.#uiGraph, value);\n        const state = getUiGraphChatPersistentState(this.#uiGraph, value);\n        await this.#tryMigrateLegacyRecords();\n        return state;\n      } catch {\n        this.#reportMigrationFailure(\n          "Some existing browser data was invalid and could not be migrated. Rivet kept its original copy."\n        );\n        return {};\n      }\n    }\n    async #loadLegacyStoredValuesIfUnmigrated() {\n      const legacyKey = getUiGraphWebAppStorageKey(this.#uiGraph, this.#location);\n      if (!legacyKey || await this.#store.getMigration(legacyKey)) return {};\n      const raw = this.#legacyStorage?.getItem(legacyKey);\n      if (raw == null) return {};\n      try {\n        const values = cloneRivetStoredValueRecord(\n          JSON.parse(raw),\n          "Legacy Stored Value snapshot"\n        );\n        await this.#tryMigrateLegacyRecords();\n        return values;\n      } catch {\n        this.#reportMigrationFailure(\n          "Some existing browser data was invalid and could not be migrated. Rivet kept its original copy."\n        );\n        return {};\n      }\n    }\n    async #migrateLegacyRecords() {\n      const legacyStorage = this.#legacyStorage;\n      if (!legacyStorage) return false;\n      let hasUnmigratedLegacyRecords = false;\n      const now = this.#now();\n      const migrations = [\n        {\n          key: CHAT_STATE_RECORD_KEY,\n          legacyKey: getUiGraphChatStorageKey(this.#uiGraph, this.#location),\n          namespace: "chat-state",\n          parse: (raw) => {\n            const value = JSON.parse(raw);\n            assertValidLegacyChatState(this.#uiGraph, value);\n            const state = getUiGraphChatPersistentState(this.#uiGraph, value);\n            return Object.keys(state).length > 0 ? [{ key: CHAT_STATE_RECORD_KEY, namespace: "chat-state", value: cloneRivetStoredValue(state) }] : [];\n          }\n        },\n        {\n          key: "legacy-snapshot",\n          legacyKey: getUiGraphWebAppStorageKey(this.#uiGraph, this.#location),\n          namespace: "stored-values",\n          parse: (raw) => Object.entries(\n            cloneRivetStoredValueRecord(JSON.parse(raw), "Legacy Stored Value snapshot")\n          ).map(([key, value]) => ({ key, namespace: "stored-values", value }))\n        },\n        ...this.#uiGraph.components.flatMap(\n          (component) => component.type !== "chat" ? [] : [\n            {\n              key: String(component.id),\n              legacyKey: getUiGraphResponseTraceStorageKey(this.#uiGraph, String(component.id), this.#location),\n              namespace: "response-traces",\n              parse: (raw) => {\n                const value = JSON.parse(raw);\n                if (!Array.isArray(value) || !value.every(isAgentResponseTrace)) {\n                  throw new Error("Legacy Chat response traces are invalid.");\n                }\n                const traces = value.slice(-RESPONSE_TRACE_LIMIT);\n                return traces.length > 0 ? [\n                  {\n                    key: String(component.id),\n                    namespace: "response-traces",\n                    value: cloneRivetStoredValue(traces)\n                  }\n                ] : [];\n              }\n            }\n          ]\n        )\n      ];\n      for (const migration of migrations) {\n        if (!migration.legacyKey || await this.#store.getMigration(migration.legacyKey)) continue;\n        const raw = legacyStorage.getItem(migration.legacyKey);\n        if (raw == null) continue;\n        let parsed;\n        try {\n          parsed = migration.parse(raw);\n        } catch {\n          hasUnmigratedLegacyRecords = true;\n          continue;\n        }\n        const missing = [];\n        let hasMemoryOnlyRecord = false;\n        for (const change of parsed) {\n          if (this.#store.hasMemoryOnlyRecord(change.namespace, change.key)) {\n            hasMemoryOnlyRecord = true;\n            break;\n          }\n          if (await this.#store.get(change.namespace, change.key) === void 0) missing.push(change);\n        }\n        if (hasMemoryOnlyRecord) {\n          hasUnmigratedLegacyRecords = true;\n          continue;\n        }\n        if (missing.length > 0) await this.#store.commitBatch(missing);\n        await this.#store.recordMigration({\n          cleanupAfter: now + RIVET_WEB_APP_LEGACY_RETENTION_MS,\n          completedAt: now,\n          key: migration.key,\n          legacyKey: migration.legacyKey,\n          namespace: migration.namespace\n        });\n      }\n      return hasUnmigratedLegacyRecords;\n    }\n    async #cleanupExpiredLegacyRecords() {\n      if (!this.#legacyStorage) return;\n      const now = this.#now();\n      for (const migration of await this.#store.listMigrations()) {\n        if (migration.cleanupAfter <= now) this.#legacyStorage.removeItem(migration.legacyKey);\n      }\n    }\n    async #persist(operation, requireDurable = false) {\n      try {\n        const result = await operation();\n        if (isStorageStatus(result) && !result.durable) {\n          this.#reportDiagnostic({ operation: "write", outcome: "failure" });\n          this.#status = result;\n          const warning = {\n            code: "persistence-failed",\n            message: "Some browser data exists only in memory and will not survive a reload."\n          };\n          this.#setWarning(warning);\n          if (requireDurable) throw new Error(warning.message);\n        } else if (isStorageCommitResult(result) && result.hasMemoryOnlyRecords) {\n          this.#reportDiagnostic({ operation: "write", outcome: "failure" });\n          this.#status = { durable: false, reason: "write-failed" };\n          this.#setWarning({\n            code: "persistence-failed",\n            message: "Some browser data exists only in memory and will not survive a reload."\n          });\n        } else {\n          this.#status = { durable: true };\n          if (this.#warning?.code === "persistence-failed") this.#setWarning(void 0);\n        }\n      } catch (error) {\n        if (error instanceof WebAppBrowserStoragePersistenceError) {\n          this.#reportDiagnostic({ operation: "write", outcome: "failure" });\n          this.#status = { durable: false, reason: "write-failed" };\n          const warning = isQuotaExceededError(error) ? {\n            code: "quota-exhausted",\n            message: "Browser storage is full. Changes will last only until this page is closed."\n          } : { code: "persistence-failed", message: error.message };\n          this.#setWarning(warning);\n          if (requireDurable) throw new Error(warning.message, { cause: error });\n          return;\n        }\n        throw error;\n      }\n    }\n    #reportDiagnostic(diagnostic) {\n      try {\n        this.#onDiagnostic(diagnostic);\n      } catch {\n      }\n    }\n    #setWarning(warning) {\n      if (this.#warning?.code === warning?.code && this.#warning?.message === warning?.message) return;\n      this.#warning = warning;\n      for (const listener of this.#warningListeners) listener(warning);\n    }\n  };\n  function defaultBrowserStorageDiagnostic(diagnostic) {\n    globalThis.console?.debug?.("[rivet-web-app-storage]", diagnostic);\n  }\n  function isQuotaExceededError(error) {\n    let current = error;\n    for (let depth = 0; depth < 4 && current instanceof Error; depth += 1) {\n      if (current.name === "QuotaExceededError") return true;\n      current = current.cause;\n    }\n    return false;\n  }\n  function isStorageStatus(value) {\n    return isRecord4(value) && typeof value.durable === "boolean";\n  }\n  function isStorageCommitResult(value) {\n    return isStorageStatus(value) && ("revision" in value || "hasMemoryOnlyRecords" in value);\n  }\n  function getDefaultLegacyStorage() {\n    try {\n      return globalThis.localStorage ?? globalThis.window?.localStorage;\n    } catch {\n      return void 0;\n    }\n  }\n  function getDefaultLocation() {\n    try {\n      return globalThis.location ?? globalThis.window?.location;\n    } catch {\n      return void 0;\n    }\n  }\n  function isRecord4(value) {\n    return typeof value === "object" && value !== null && !Array.isArray(value);\n  }\n  function assertValidLegacyChatState(uiGraph, value) {\n    if (!isRecord4(value)) throw new Error("Legacy Chat state must be a JSON object.");\n    for (const component of uiGraph.components) {\n      if (component.type !== "chat") continue;\n      const draft = value[`__rivet_chat_${component.id}_draft`];\n      const messages = value[`__rivet_chat_${component.id}_messages`];\n      const pins = value[`__rivet_chat_${component.id}_pins`];\n      if (draft !== void 0 && typeof draft !== "string") {\n        throw new Error("Legacy Chat draft is invalid.");\n      }\n      if (messages !== void 0 && (!Array.isArray(messages) || !messages.every(isValidLegacyChatMessage))) {\n        throw new Error("Legacy Chat messages are invalid.");\n      }\n      if (pins !== void 0 && (!Array.isArray(pins) || !pins.every((pin) => typeof pin === "number" && Number.isSafeInteger(pin) && pin >= 0))) {\n        throw new Error("Legacy Chat pins are invalid.");\n      }\n    }\n  }\n  function isValidLegacyChatMessage(value) {\n    if (!isRecord4(value)) return false;\n    return (value.role === "user" || value.role === "assistant") && typeof value.content === "string" && (value.timestamp === void 0 || typeof value.timestamp === "string") && (value.responseTraceId === void 0 || typeof value.responseTraceId === "string");\n  }\n\n  // ../core/src/model/UiGraphActionProtocol.ts\n  var RIVET_WEB_APP_ACTION_PROTOCOL_VERSION = 1;\n  function parseRivetWebAppServerMessage(value) {\n    if (!isRecord5(value) || typeof value.type !== "string") return void 0;\n    const storageMessage = parseRivetWebAppBrowserStorageServerMessage(value);\n    if (storageMessage) return storageMessage;\n    if (value.type === "server.ready") {\n      if (value.protocolVersion !== RIVET_WEB_APP_ACTION_PROTOCOL_VERSION) return void 0;\n      const browserStorageRpcLimits = parseBrowserStorageRpcLimits(value.browserStorageRpcLimits);\n      if (value.browserStorageRpcLimits != null && !browserStorageRpcLimits) return void 0;\n      return {\n        type: value.type,\n        protocolVersion: value.protocolVersion,\n        ...isStringArray(value.capabilities) ? { capabilities: value.capabilities } : {},\n        ...browserStorageRpcLimits ? { browserStorageRpcLimits } : {}\n      };\n    }\n    if (value.type === "server.draining") return { type: value.type };\n    if (value.type === "action.rejected") {\n      return isNonEmptyString2(value.requestId) && isNonEmptyString2(value.error) ? {\n        type: value.type,\n        requestId: value.requestId,\n        error: value.error,\n        ...typeof value.code === "string" ? { code: value.code } : {}\n      } : void 0;\n    }\n    if (value.type === "run.rejected") {\n      return isNonEmptyString2(value.runId) && isNonEmptyString2(value.error) ? {\n        type: value.type,\n        runId: value.runId,\n        error: value.error,\n        ...typeof value.code === "string" ? { code: value.code } : {}\n      } : void 0;\n    }\n    if (!hasRunEventBase(value)) return void 0;\n    const base = {\n      requestId: value.requestId,\n      runId: value.runId,\n      sequence: value.sequence\n    };\n    switch (value.type) {\n      case "action.accepted":\n      case "action.cancelled":\n        return { type: value.type, ...base };\n      case "action.progress": {\n        if (!isRecord5(value.progress)) return void 0;\n        const progress = normalizeGraphProgress({\n          message: typeof value.progress.message === "string" ? value.progress.message : void 0,\n          percent: typeof value.progress.percent === "number" ? value.progress.percent : void 0\n        });\n        return progress ? { type: value.type, ...base, progress } : void 0;\n      }\n      case "action.completed":\n        return isRecord5(value.statePatch) && (value.storagePatch == null || isRecord5(value.storagePatch)) ? {\n          type: value.type,\n          ...base,\n          statePatch: value.statePatch,\n          ...isRecord5(value.storagePatch) ? { storagePatch: value.storagePatch } : {},\n          ...isAgentResponseTrace(value.responseTrace) ? { responseTrace: value.responseTrace } : {}\n        } : void 0;\n      case "action.failed":\n        return isNonEmptyString2(value.error) ? {\n          type: value.type,\n          ...base,\n          error: value.error,\n          ...typeof value.code === "string" ? { code: value.code } : {},\n          ...isAgentResponseTrace(value.responseTrace) ? { responseTrace: value.responseTrace } : {}\n        } : void 0;\n      case "action.interrupted":\n        return isNonEmptyString2(value.error) ? { type: value.type, ...base, error: value.error } : void 0;\n      default:\n        return void 0;\n    }\n  }\n  function hasRunEventBase(value) {\n    return isNonEmptyString2(value.requestId) && isNonEmptyString2(value.runId) && typeof value.sequence === "number" && Number.isSafeInteger(value.sequence) && value.sequence > 0;\n  }\n  function isRecord5(value) {\n    return typeof value === "object" && value != null && !Array.isArray(value);\n  }\n  function parseBrowserStorageRpcLimits(value) {\n    if (!isRecord5(value)) return void 0;\n    if (!isPositiveSafeInteger(value.maxActionBytes) || !isPositiveSafeInteger(value.maxValueBytes) || value.maxValueBytes > value.maxActionBytes) {\n      return void 0;\n    }\n    if (value.transferTimeoutMs != null && !isPositiveSafeInteger(value.transferTimeoutMs)) return void 0;\n    return {\n      maxActionBytes: value.maxActionBytes,\n      maxValueBytes: value.maxValueBytes,\n      transferTimeoutMs: value.transferTimeoutMs ?? RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_TRANSFER_TIMEOUT_MS\n    };\n  }\n  function isPositiveSafeInteger(value) {\n    return typeof value === "number" && Number.isSafeInteger(value) && value > 0;\n  }\n  function isNonEmptyString2(value) {\n    return typeof value === "string" && value.length > 0;\n  }\n  function isStringArray(value) {\n    return Array.isArray(value) && value.length <= 16 && value.every((item) => typeof item === "string" && item.length <= 100);\n  }\n\n  // src/webAppClientDom.ts\n  function createWebAppElement(tagName, attributes = {}, children = []) {\n    const element = document.createElement(tagName);\n    for (const [key, value] of Object.entries(attributes)) {\n      if (key === "className") element.className = String(value ?? "");\n      else if (key === "text") element.textContent = String(value ?? "");\n      else if (key.startsWith("on") && typeof value === "function") {\n        element.addEventListener(key.slice(2).toLowerCase(), value);\n      } else if (value != null) element.setAttribute(key, String(value));\n    }\n    element.append(...children);\n    return element;\n  }\n  function observeOutputResizeBounds(container) {\n    const disposers = [...container.querySelectorAll(".rivet-web-app-output")].map(\n      observeUiGraphOutputResizeBounds\n    );\n    return () => disposers.forEach((dispose) => dispose());\n  }\n  function captureFocusedTextControl(root2) {\n    const activeElement = document.activeElement;\n    if (!(activeElement instanceof HTMLInputElement || activeElement instanceof HTMLTextAreaElement) || !root2.contains(activeElement)) {\n      return void 0;\n    }\n    const componentId = activeElement.dataset.rivetFocusComponentId;\n    if (!componentId) return void 0;\n    return {\n      componentId,\n      scrollLeft: activeElement.scrollLeft,\n      scrollTop: activeElement.scrollTop,\n      selectionDirection: activeElement.selectionDirection ?? "none",\n      selectionEnd: activeElement.selectionEnd ?? activeElement.value.length,\n      selectionStart: activeElement.selectionStart ?? activeElement.value.length\n    };\n  }\n  function restoreFocusedTextControl(root2, focused) {\n    const control = [\n      ...root2.querySelectorAll("[data-rivet-focus-component-id]")\n    ].find((candidate) => candidate.dataset.rivetFocusComponentId === focused.componentId);\n    if (!control) return;\n    control.focus();\n    control.setSelectionRange(focused.selectionStart, focused.selectionEnd, focused.selectionDirection);\n    control.scrollLeft = focused.scrollLeft;\n    control.scrollTop = focused.scrollTop;\n  }\n  function renderActionProgress(progress) {\n    if (!progress) return void 0;\n    const children = [];\n    if (progress.message) children.push(createWebAppElement("span", { text: progress.message }));\n    if (progress.percent != null) {\n      children.push(\n        createWebAppElement("progress", { "aria-label": "Action progress", max: "100", value: progress.percent })\n      );\n    }\n    return createWebAppElement("div", { "aria-live": "polite", className: "rivet-web-app-progress" }, children);\n  }\n  function createProgressiveJsonOutput(value) {\n    const pre = createWebAppElement("pre", { className: "rivet-web-app-output-json" });\n    const chunks = getUiGraphProgressiveJsonOutputChunks(value);\n    if (!chunks) {\n      pre.textContent = value;\n      return pre;\n    }\n    pre.append(document.createTextNode(chunks[0]));\n    let nextChunkIndex = 1;\n    const appendNextChunk = () => {\n      if (!pre.isConnected || nextChunkIndex >= chunks.length) return;\n      pre.append(document.createTextNode(chunks[nextChunkIndex]));\n      nextChunkIndex += 1;\n      scheduleAnimationFrame(appendNextChunk);\n    };\n    scheduleAnimationFrame(appendNextChunk);\n    return pre;\n  }\n  function scheduleAnimationFrame(callback) {\n    if (typeof requestAnimationFrame === "function") requestAnimationFrame(callback);\n    else window.setTimeout(callback, 0);\n  }\n\n  // src/webAppClientStorageRpc.ts\n  var BrowserStorageClientLimitError = class extends Error {\n    constructor(message) {\n      super(message);\n      this.name = "BrowserStorageClientLimitError";\n    }\n  };\n  var WebAppClientStorageRpc = class {\n    #bridge;\n    #requestId;\n    #runId;\n    #sendJson;\n    #sendBinary;\n    #onFatal;\n    #limits;\n    #incomingCommit;\n    #storageSessionId;\n    #transferredBytes = 0;\n    #disposed = false;\n    constructor(options) {\n      this.#bridge = options.bridge;\n      this.#requestId = options.requestId;\n      this.#runId = options.runId;\n      this.#sendJson = options.sendJson;\n      this.#sendBinary = options.sendBinary;\n      this.#onFatal = options.onFatal;\n      this.#limits = options.limits ?? {\n        maxActionBytes: RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_MAX_ACTION_BYTES,\n        maxValueBytes: RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_MAX_VALUE_BYTES,\n        transferTimeoutMs: RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_TRANSFER_TIMEOUT_MS\n      };\n      assertPositiveSafeInteger(this.#limits.maxActionBytes, "maxActionBytes");\n      assertPositiveSafeInteger(this.#limits.maxValueBytes, "maxValueBytes");\n      assertPositiveSafeInteger(this.#limits.transferTimeoutMs, "transferTimeoutMs");\n      if (this.#limits.maxValueBytes > this.#limits.maxActionBytes) {\n        throw new RangeError("maxValueBytes cannot exceed maxActionBytes.");\n      }\n    }\n    async handleMessage(message) {\n      const runId = this.#runId();\n      if (this.#disposed || !runId || message.requestId !== this.#requestId || message.runId !== runId) return;\n      if (this.#storageSessionId && this.#storageSessionId !== message.storageSessionId) {\n        return this.#fail("The server sent browser storage data for another action.");\n      }\n      this.#storageSessionId = message.storageSessionId;\n      if (message.type === "storage.get") {\n        await this.#sendValue(message.storageRequestId, message.key);\n        return;\n      }\n      if (message.type === "storage.commit.start") {\n        if (this.#incomingCommit) return this.#fail("The server started overlapping browser storage transfers.");\n        if (message.byteLength < 1 || message.byteLength > this.#limits.maxActionBytes || message.chunkCount !== expectedChunkCount(message.byteLength)) {\n          return this.#fail("The server declared an invalid browser storage commit.");\n        }\n        try {\n          this.#reserveBytes(message.byteLength);\n        } catch (error) {\n          const failure = error instanceof Error ? error : new BrowserStorageClientLimitError("Browser storage transfer limit exceeded.");\n          this.#sendError("storage_too_large", failure.message, false, message.transferId);\n          this.#fail(failure.message);\n          return;\n        }\n        this.#incomingCommit = {\n          byteLength: message.byteLength,\n          chunks: [],\n          expectedChunkIndex: 0,\n          timer: this.#createTimer(() => {\n            this.#sendError("storage_unavailable", "Browser storage commit timed out.", true, message.transferId);\n            this.#fail("Browser storage commit timed out.");\n          }),\n          transferId: message.transferId\n        };\n        return;\n      }\n      if (message.type === "storage.error") {\n        this.#fail(message.error || "The server rejected a browser storage transfer.");\n      }\n    }\n    async handleBinary(frame) {\n      if (this.#disposed) return false;\n      const decoded = decodeRivetWebAppStorageBinaryFrame(frame);\n      if (!decoded) return false;\n      if (!this.#storageSessionId || decoded.storageSessionId !== this.#storageSessionId) return false;\n      const incoming = this.#incomingCommit;\n      if (!incoming || incoming.transferId !== decoded.transferId) {\n        return false;\n      }\n      const receivedBefore = incoming.chunks.reduce((total, chunk) => total + chunk.byteLength, 0);\n      if (decoded.chunkIndex !== incoming.expectedChunkIndex || decoded.bytes.byteLength > RIVET_WEB_APP_BROWSER_STORAGE_TRANSFER_CHUNK_BYTES || receivedBefore + decoded.bytes.byteLength > incoming.byteLength) {\n        this.#fail("The server sent missing, out-of-order, or oversized browser storage chunks.");\n        return true;\n      }\n      incoming.chunks.push(decoded.bytes);\n      incoming.expectedChunkIndex += 1;\n      const received = receivedBefore + decoded.bytes.byteLength;\n      if (incoming.expectedChunkIndex === expectedChunkCount(incoming.byteLength)) {\n        if (received !== incoming.byteLength) {\n          this.#fail("The browser storage commit did not match its declared size.");\n          return true;\n        }\n        try {\n          const patch = deserializeRivetWebAppStoredValuePatch(concatenateChunks(incoming.chunks, incoming.byteLength));\n          for (const value of Object.values(patch)) {\n            if (serializeRivetWebAppStoredValue(value).byteLength > this.#limits.maxValueBytes) {\n              throw new BrowserStorageClientLimitError("A browser Stored Value exceeds the supported maximum size.");\n            }\n          }\n          await this.#bridge.commit(patch);\n          if (this.#disposed || this.#incomingCommit !== incoming) return true;\n          this.#clearIncomingCommit();\n          if (!this.#sendJson({\n            type: "storage.commit.ack",\n            requestId: this.#requestId,\n            runId: this.#runId(),\n            storageSessionId: this.#storageSessionId,\n            transferId: incoming.transferId\n          })) {\n            this.#fail("The browser storage connection closed before commit acknowledgement.");\n          }\n        } catch (error) {\n          if (this.#disposed || this.#incomingCommit !== incoming) return true;\n          const isLimitError = error instanceof BrowserStorageClientLimitError;\n          const message = isLimitError ? error.message : "Browser storage could not durably commit this action.";\n          this.#clearIncomingCommit();\n          this.#sendError(\n            isLimitError ? "storage_too_large" : "storage_unavailable",\n            message,\n            !isLimitError,\n            incoming.transferId\n          );\n          this.#fail(message);\n        }\n      }\n      return true;\n    }\n    dispose() {\n      this.#disposed = true;\n      this.#clearIncomingCommit();\n    }\n    async #sendValue(storageRequestId, key) {\n      const runId = this.#runId();\n      if (!runId) return;\n      try {\n        const value = await this.#bridge.get(key);\n        const found = value !== void 0;\n        const bytes = found ? serializeRivetWebAppStoredValue(value) : new Uint8Array();\n        if (bytes.byteLength > this.#limits.maxValueBytes) {\n          this.#sendError(\n            "storage_too_large",\n            "A browser Stored Value exceeds the supported maximum size.",\n            false,\n            void 0,\n            storageRequestId\n          );\n          return;\n        }\n        this.#reserveBytes(bytes.byteLength);\n        const transferId = crypto.randomUUID();\n        const chunks = splitRivetWebAppStorageTransfer(bytes);\n        if (!this.#sendJson({\n          type: "storage.transfer.start",\n          requestId: this.#requestId,\n          runId,\n          storageSessionId: this.#storageSessionId,\n          storageRequestId,\n          transferId,\n          byteLength: bytes.byteLength,\n          chunkCount: chunks.length,\n          found\n        }) || chunks.some(\n          (chunk, index) => !this.#sendBinary(encodeRivetWebAppStorageBinaryFrame(this.#storageSessionId, transferId, index, chunk))\n        )) {\n          this.#fail("The browser storage connection closed during transfer.");\n        }\n      } catch (error) {\n        if (error instanceof BrowserStorageClientLimitError) {\n          this.#sendError("storage_too_large", error.message, false, void 0, storageRequestId);\n          return;\n        }\n        this.#sendError(\n          "storage_unavailable",\n          "Browser storage could not read the requested value.",\n          true,\n          void 0,\n          storageRequestId\n        );\n      }\n    }\n    #reserveBytes(byteLength) {\n      if (this.#transferredBytes + byteLength > this.#limits.maxActionBytes) {\n        throw new BrowserStorageClientLimitError("Browser storage transfers exceeded the per-action limit.");\n      }\n      this.#transferredBytes += byteLength;\n    }\n    #sendError(code, error, retryable, transferId, storageRequestId) {\n      const runId = this.#runId();\n      if (!runId) return;\n      this.#sendJson({\n        type: "storage.error",\n        requestId: this.#requestId,\n        runId,\n        storageSessionId: this.#storageSessionId,\n        code,\n        error,\n        retryable,\n        ...transferId ? { transferId } : {},\n        ...storageRequestId ? { storageRequestId } : {}\n      });\n    }\n    #fail(message) {\n      if (this.#disposed) return;\n      this.#disposed = true;\n      this.#clearIncomingCommit();\n      this.#onFatal(new Error(message));\n    }\n    #clearIncomingCommit() {\n      const incoming = this.#incomingCommit;\n      this.#incomingCommit = void 0;\n      if (incoming) clearTimeout(incoming.timer);\n    }\n    #createTimer(onTimeout) {\n      return setTimeout(onTimeout, this.#limits.transferTimeoutMs);\n    }\n  };\n  function expectedChunkCount(byteLength) {\n    return byteLength === 0 ? 0 : Math.ceil(byteLength / RIVET_WEB_APP_BROWSER_STORAGE_TRANSFER_CHUNK_BYTES);\n  }\n  function concatenateChunks(chunks, byteLength) {\n    const result = new Uint8Array(byteLength);\n    let offset = 0;\n    for (const chunk of chunks) {\n      result.set(chunk, offset);\n      offset += chunk.byteLength;\n    }\n    return result;\n  }\n  function assertPositiveSafeInteger(value, name) {\n    if (!Number.isSafeInteger(value) || value < 1) throw new RangeError(`${name} must be a positive safe integer.`);\n  }\n\n  // src/webAppClientTransport.ts\n  var DEFAULT_CLIENT_STORAGE_RPC_LIMITS = {\n    maxActionBytes: RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_MAX_ACTION_BYTES,\n    maxValueBytes: RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_MAX_VALUE_BYTES,\n    transferTimeoutMs: RIVET_WEB_APP_BROWSER_STORAGE_DEFAULT_TRANSFER_TIMEOUT_MS\n  };\n  var HostedActionError = class extends Error {\n    constructor(message, code, responseTrace) {\n      super(message);\n      this.code = code;\n      this.responseTrace = responseTrace;\n    }\n  };\n  function createHostedActionRunner(config2) {\n    const transport = config2.actionTransport ?? (config2.actionPath ? { type: "http", actionPath: config2.actionPath } : void 0);\n    if (!transport) {\n      return {\n        survivesPageDetach: false,\n        dispose: () => void 0,\n        run: async () => {\n          throw new Error("Rivet web app action transport is not configured.");\n        }\n      };\n    }\n    return transport.type === "websocket" ? createWebSocketActionRunner(transport.socketPath) : createHttpActionRunner(transport.actionPath);\n  }\n  function createHttpActionRunner(actionPath) {\n    return {\n      survivesPageDetach: false,\n      dispose: () => void 0,\n      async run({ browserStorage, componentId, revisionKey, signal, state, storage }) {\n        const snapshot = browserStorage ? await browserStorage.loadSnapshot() : storage ?? {};\n        if (browserStorage && serializedByteLength(snapshot) > RIVET_WEB_APP_BROWSER_STORAGE_SAFE_FALLBACK_BYTES) {\n          const error = new HostedActionError(\n            "This Stored Value state is too large for HTTP action transport. Use a current Studio Server WebSocket endpoint with browser-storage RPC v2.",\n            "browser_storage_rpc_required"\n          );\n          browserStorage.reportTransportIncompatibility?.(error.message);\n          throw error;\n        }\n        browserStorage?.clearTransportIncompatibility?.();\n        const response = await fetch(actionPath, {\n          body: JSON.stringify({ componentId, revisionKey, state, storage: snapshot }),\n          credentials: "same-origin",\n          headers: { "content-type": "application/json" },\n          method: "POST",\n          signal\n        });\n        const result = await readHostedActionResponse(response);\n        if (!response.ok) {\n          throw new HostedActionError(result.error || "Action failed.", result.code, result.responseTrace);\n        }\n        return {\n          statePatch: result.statePatch,\n          storagePatch: result.storagePatch,\n          responseTrace: result.responseTrace\n        };\n      }\n    };\n  }\n  function createWebSocketActionRunner(socketPath) {\n    const pendingByRequestId = /* @__PURE__ */ new Map();\n    const pendingByRunId = /* @__PURE__ */ new Map();\n    let socket;\n    let protocolReady = false;\n    let storageRpcReady = false;\n    let reconnectAttempt = 0;\n    let storageRpcLimits = DEFAULT_CLIENT_STORAGE_RPC_LIMITS;\n    let reconnectTimer;\n    let disposed = false;\n    const settlePending = (pending, settle) => {\n      if (pending.settled) return;\n      pending.settled = true;\n      pending.signal.removeEventListener("abort", pending.abortListener);\n      pending.storageRpc?.dispose();\n      pendingByRequestId.delete(pending.message.requestId);\n      if (pending.runId) pendingByRunId.delete(pending.runId);\n      settle();\n    };\n    const sendRaw = (message) => {\n      if (socket?.readyState !== WebSocket.OPEN) return false;\n      try {\n        socket.send(JSON.stringify(message));\n        return true;\n      } catch {\n        return false;\n      }\n    };\n    const sendBinary = (frame) => {\n      if (socket?.readyState !== WebSocket.OPEN) return false;\n      try {\n        socket.send(frame);\n        return true;\n      } catch {\n        return false;\n      }\n    };\n    const send = (message) => protocolReady && sendRaw(message);\n    const sendPending = (pending) => {\n      if (pending.runId) {\n        send({ type: "run.resume", runId: pending.runId, lastSequence: pending.lastSequence });\n        if (pending.signal.aborted) send({ type: "action.cancel", runId: pending.runId });\n      } else if ((!pending.signal.aborted || pending.startSent) && !pending.sendingStart) {\n        pending.sendingStart = true;\n        void (async () => {\n          try {\n            const useRpc = storageRpcReady && pending.browserStorage != null;\n            if (useRpc) pending.browserStorage?.clearTransportIncompatibility?.();\n            const storage = useRpc ? void 0 : pending.browserStorage ? await pending.browserStorage.loadSnapshot() : pending.legacyStorage ?? {};\n            if (storage != null && serializedByteLength(storage) > RIVET_WEB_APP_BROWSER_STORAGE_SAFE_FALLBACK_BYTES) {\n              const error = new HostedActionError(\n                "This Stored Value state requires browser-storage RPC v2, but the server or proxy did not negotiate it. Update the server before running this action.",\n                "browser_storage_rpc_required"\n              );\n              pending.browserStorage?.reportTransportIncompatibility?.(error.message);\n              settlePending(pending, () => pending.reject(error));\n              return;\n            }\n            if (!useRpc) pending.browserStorage?.clearTransportIncompatibility?.();\n            pending.startSent = send({\n              ...pending.message,\n              ...useRpc ? { storageRpcVersion: 2 } : { storage: storage ?? {} }\n            }) || pending.startSent;\n          } catch (error) {\n            settlePending(pending, () => pending.reject(error));\n          } finally {\n            pending.sendingStart = false;\n          }\n        })();\n      }\n    };\n    const scheduleReconnect = () => {\n      if (reconnectTimer || disposed) return;\n      const delay = Math.min(1e4, 250 * 2 ** reconnectAttempt) + Math.floor(Math.random() * 200);\n      reconnectAttempt += 1;\n      reconnectTimer = setTimeout(() => {\n        reconnectTimer = void 0;\n        connect();\n      }, delay);\n    };\n    const connect = () => {\n      if (disposed || socket?.readyState === WebSocket.OPEN || socket?.readyState === WebSocket.CONNECTING) return;\n      try {\n        const url = new URL(socketPath, window.location.href);\n        if (url.protocol === "http:") url.protocol = "ws:";\n        else if (url.protocol === "https:") url.protocol = "wss:";\n        else if (url.protocol !== "ws:" && url.protocol !== "wss:") {\n          throw new Error(`Unsupported web app WebSocket protocol: ${url.protocol}`);\n        }\n        socket = new WebSocket(url);\n        socket.binaryType = "arraybuffer";\n        protocolReady = false;\n        storageRpcReady = false;\n      } catch (error) {\n        for (const pending of [...pendingByRequestId.values()]) {\n          settlePending(pending, () => pending.reject(error));\n        }\n        return;\n      }\n      socket.addEventListener("open", () => {\n        sendRaw({\n          type: "client.hello",\n          protocolVersion: RIVET_WEB_APP_ACTION_PROTOCOL_VERSION,\n          capabilities: [RIVET_WEB_APP_BROWSER_STORAGE_RPC_CAPABILITY]\n        });\n      });\n      socket.addEventListener("message", (event) => {\n        if (event.data instanceof ArrayBuffer) {\n          const sourceSocket = socket;\n          void (async () => {\n            for (const pending2 of pendingByRequestId.values()) {\n              if (pending2.storageRpc && await pending2.storageRpc.handleBinary(event.data)) return;\n            }\n            if (socket === sourceSocket && sourceSocket?.readyState === WebSocket.OPEN) {\n              sourceSocket.close(1002, "Unexpected browser storage frame");\n            }\n          })();\n          return;\n        }\n        let value;\n        try {\n          value = JSON.parse(String(event.data));\n        } catch {\n          return;\n        }\n        const message = parseRivetWebAppServerMessage(value);\n        if (!message) return;\n        if (message.type === "server.ready") {\n          protocolReady = true;\n          storageRpcReady = message.capabilities?.includes(RIVET_WEB_APP_BROWSER_STORAGE_RPC_CAPABILITY) === true;\n          reconnectAttempt = 0;\n          storageRpcLimits = message.browserStorageRpcLimits ?? DEFAULT_CLIENT_STORAGE_RPC_LIMITS;\n          for (const pending2 of pendingByRequestId.values()) sendPending(pending2);\n          return;\n        }\n        if (message.type === "server.draining") return;\n        if (isStorageServerMessage(message)) {\n          const pending2 = pendingByRunId.get(message.runId) ?? pendingByRequestId.get(message.requestId);\n          if (pending2?.storageRpc) {\n            void pending2.storageRpc.handleMessage(message);\n          }\n          return;\n        }\n        if (message.type === "action.rejected") {\n          const pending2 = pendingByRequestId.get(message.requestId);\n          if (pending2) settlePending(pending2, () => pending2.reject(new HostedActionError(message.error, message.code)));\n          return;\n        }\n        if (message.type === "run.rejected") {\n          const pending2 = pendingByRunId.get(message.runId);\n          if (pending2) settlePending(pending2, () => pending2.reject(new HostedActionError(message.error, message.code)));\n          return;\n        }\n        const pending = pendingByRunId.get(message.runId) ?? pendingByRequestId.get(message.requestId);\n        if (!pending || message.sequence <= pending.lastSequence) return;\n        pending.lastSequence = message.sequence;\n        if (message.type === "action.accepted") {\n          pending.runId = message.runId;\n          pendingByRunId.set(message.runId, pending);\n          if (storageRpcReady && pending.browserStorage) {\n            pending.storageRpc = new WebAppClientStorageRpc({\n              bridge: pending.browserStorage,\n              requestId: pending.message.requestId,\n              limits: storageRpcLimits,\n              runId: () => pending.runId,\n              sendBinary,\n              sendJson: send,\n              onFatal(error) {\n                if (pending.runId) send({ type: "action.cancel", runId: pending.runId });\n                settlePending(pending, () => pending.reject(error));\n              }\n            });\n          }\n          if (pending.signal.aborted) send({ type: "action.cancel", runId: message.runId });\n        } else if (message.type === "action.progress") {\n          pending.onProgress(message.progress);\n        } else if (message.type === "action.completed") {\n          settlePending(\n            pending,\n            () => pending.resolve({\n              statePatch: message.statePatch,\n              storagePatch: message.storagePatch,\n              responseTrace: message.responseTrace\n            })\n          );\n        } else if (message.type === "action.failed") {\n          settlePending(\n            pending,\n            () => pending.reject(new HostedActionError(message.error, message.code, message.responseTrace))\n          );\n        } else if (message.type === "action.cancelled") {\n          settlePending(pending, () => pending.reject(new DOMException("The action was cancelled.", "AbortError")));\n        } else if (message.type === "action.interrupted") {\n          settlePending(pending, () => pending.reject(new HostedActionError(message.error, "action_interrupted")));\n        }\n      });\n      socket.addEventListener("close", (event) => {\n        socket = void 0;\n        protocolReady = false;\n        storageRpcReady = false;\n        if (isNonRetryableWebSocketClose(event.code)) {\n          const message = event.reason.trim() || `Web app action connection closed (${event.code}).`;\n          for (const pending of [...pendingByRequestId.values()]) {\n            settlePending(pending, () => pending.reject(new HostedActionError(message, "websocket_closed")));\n          }\n          return;\n        }\n        if (!disposed && pendingByRequestId.size > 0) scheduleReconnect();\n      });\n    };\n    return {\n      survivesPageDetach: true,\n      dispose() {\n        disposed = true;\n        if (reconnectTimer) clearTimeout(reconnectTimer);\n        socket?.close(1e3, "Page detached");\n        socket = void 0;\n        const error = new DOMException("The page detached from the action.", "AbortError");\n        for (const pending of [...pendingByRequestId.values()]) {\n          settlePending(pending, () => pending.reject(error));\n        }\n      },\n      run({ browserStorage, componentId, onProgress, revisionKey, signal, state, storage }) {\n        if (disposed) return Promise.reject(new Error("The web app action transport is closed."));\n        signal.throwIfAborted();\n        const requestId = globalThis.crypto?.randomUUID?.() ?? `rivet-${Date.now()}-${Math.random()}`;\n        return new Promise((resolve, reject) => {\n          const pending = {\n            abortListener: () => {\n              if (pending.runId) send({ type: "action.cancel", runId: pending.runId });\n              else if (!pending.startSent) settlePending(pending, () => reject(signal.reason));\n            },\n            lastSequence: 0,\n            message: {\n              type: "action.start",\n              requestId,\n              componentId,\n              state,\n              ...revisionKey == null ? {} : { revisionKey }\n            },\n            onProgress,\n            browserStorage,\n            legacyStorage: storage,\n            reject,\n            resolve,\n            settled: false,\n            signal,\n            sendingStart: false,\n            startSent: false\n          };\n          pendingByRequestId.set(requestId, pending);\n          signal.addEventListener("abort", pending.abortListener, { once: true });\n          connect();\n          sendPending(pending);\n        });\n      }\n    };\n  }\n  function serializedByteLength(value) {\n    return new TextEncoder().encode(JSON.stringify(value)).byteLength;\n  }\n  function isStorageServerMessage(message) {\n    return message?.type === "storage.get" || message?.type === "storage.transfer.ack" || message?.type === "storage.commit.start" || message?.type === "storage.error";\n  }\n  function isNonRetryableWebSocketClose(code) {\n    return code === 1002 || code === 1003 || code === 1007 || code === 1008 || code === 1009;\n  }\n  var getActionFailureMessage = (response) => `${response.status} ${response.statusText || "Action failed"}`;\n  async function readHostedActionResponse(response) {\n    const body = (await response.text()).trim();\n    if (body) {\n      try {\n        const result = JSON.parse(body);\n        if (typeof result === "object" && result !== null && !Array.isArray(result)) {\n          return result;\n        }\n      } catch {\n      }\n    }\n    throw new Error(response.ok ? "Action returned an invalid response." : getActionFailureMessage(response));\n  }\n\n  // src/webAppClientRenderer.ts\n  function createResponseInspectorElement(trace, onClose) {\n    const metric = (label, value) => createWebAppElement("div", {}, [createWebAppElement("dt", { text: label }), createWebAppElement("dd", { text: value })]);\n    const metricGroup = (title, metrics, description) => createWebAppElement("section", { className: "rivet-agent-response-inspector-metric-group" }, [\n      createWebAppElement("div", { className: "rivet-agent-response-inspector-group-heading" }, [\n        createWebAppElement("h3", { text: title }),\n        ...description ? [createWebAppElement("p", { text: description })] : []\n      ]),\n      createWebAppElement("dl", { className: "rivet-agent-response-inspector-metrics" }, metrics)\n    ]);\n    const body = trace == null ? createWebAppElement("div", { className: "rivet-agent-response-inspector-unavailable" }, [\n      createWebAppElement("strong", { text: "Trace unavailable" }),\n      createWebAppElement("p", {\n        text: "This response was produced by an older host, a replay, or while response inspection was disabled."\n      })\n    ]) : createWebAppElement("div", { className: "rivet-agent-response-inspector-body" }, [\n      metricGroup("Execution", [\n        metric("Status", trace.status),\n        metric("Total duration", formatTraceDuration(trace.durationMs)),\n        metric("Model calls", String(trace.summary.modelCallCount)),\n        metric("Tool calls", String(trace.summary.toolCallCount))\n      ]),\n      metricGroup(\n        "Recovery behavior",\n        [\n          metric("Provider request retries", String(trace.summary.retryCount)),\n          metric("LLM profile fallbacks", String(trace.summary.fallbackCount))\n        ],\n        "Provider request retries repeat a failed request. LLM profile fallbacks move to the next configured profile."\n      ),\n      metricGroup("Usage and cost", [\n        metric("Input tokens", formatTraceTokens(trace.summary.promptTokens)),\n        metric("Output tokens", formatTraceTokens(trace.summary.completionTokens)),\n        metric("Cached input tokens", formatTraceTokens(trace.summary.cachedTokens)),\n        metric("Reasoning tokens", formatTraceTokens(trace.summary.reasoningTokens)),\n        metric(\n          "Model cost",\n          trace.summary.costStatus === "unknown" ? "Unknown" : `$${trace.summary.knownCostUsd.toFixed(6)}${trace.summary.costStatus === "partial" ? " (partial)" : ""}`\n        )\n      ]),\n      ...trace.startedAt == null && trace.responseReadyAt == null && !trace.backgroundWorkPending ? [] : [\n        createWebAppElement("section", { className: "rivet-agent-response-inspector-metric-group" }, [\n          createWebAppElement("div", { className: "rivet-agent-response-inspector-group-heading" }, [\n            createWebAppElement("h3", { text: "Timing" })\n          ]),\n          createWebAppElement("dl", { className: "rivet-agent-response-inspector-timing" }, [\n            ...trace.startedAt == null ? [] : [\n              createWebAppElement("div", {}, [\n                createWebAppElement("dt", { text: "Started" }),\n                createWebAppElement("dd", { text: new Date(trace.startedAt).toLocaleString() })\n              ])\n            ],\n            ...trace.responseReadyAt == null ? [] : [\n              createWebAppElement("div", {}, [\n                createWebAppElement("dt", { text: "Response ready" }),\n                createWebAppElement("dd", { text: new Date(trace.responseReadyAt).toLocaleString() })\n              ])\n            ],\n            ...trace.backgroundWorkPending ? [\n              createWebAppElement("div", {}, [\n                createWebAppElement("dt", { text: "Async work" }),\n                createWebAppElement("dd", { text: "Still active when this response was delivered" })\n              ])\n            ] : []\n          ])\n        ])\n      ],\n      createTraceDetails(\n        "Model calls",\n        trace.modelCalls.map(\n          (call) => createWebAppElement("article", {}, [\n            createWebAppElement("strong", { text: formatTraceModelCallLabel(call) }),\n            createWebAppElement("span", {\n              text: `${call.outcome}${call.profileIndex == null ? "" : ` \\xB7 profile ${call.profileIndex + 1}`} \\xB7 round ${(call.roundIndex ?? 0) + 1} \\xB7 attempt ${call.attemptIndex + 1}`\n            }),\n            createWebAppElement("span", {\n              text: `${formatTraceDuration(call.durationMs)} \\xB7 ${formatTraceCallUsage(call.usage)} \\xB7 ${call.pricing.status === "known" && call.pricing.costUsd != null ? `$${call.pricing.costUsd.toFixed(6)}` : "cost unknown"}`\n            }),\n            ...call.finishReason ? [createWebAppElement("span", { text: `Finish reason: ${call.finishReason}` })] : []\n          ])\n        ),\n        trace.omittedModelCallCount\n      ),\n      createTraceDetails(\n        "Tool calls",\n        trace.toolCalls.map(\n          (call) => createWebAppElement("article", {}, [\n            createWebAppElement("strong", { text: call.toolName }),\n            createWebAppElement("span", {\n              text: `${call.outcome} \\xB7 ${call.handlerKind}${call.handlerName ? ` \\xB7 ${call.handlerName}` : ""}`\n            }),\n            createWebAppElement("span", { text: formatTraceDuration(call.durationMs) })\n          ])\n        ),\n        trace.omittedToolCallCount\n      )\n    ]);\n    return createWebAppElement(\n      "div",\n      {\n        className: "rivet-agent-response-inspector-backdrop",\n        onClick: (event) => event.target === event.currentTarget && onClose()\n      },\n      [\n        createWebAppElement(\n          "section",\n          {\n            "aria-label": "Response inspector",\n            "aria-modal": "true",\n            className: "rivet-agent-response-inspector",\n            role: "dialog"\n          },\n          [\n            createWebAppElement("header", {}, [\n              createWebAppElement("div", {}, [\n                createWebAppElement("strong", { text: "Response inspector" }),\n                createWebAppElement("span", { text: "Execution metadata only" })\n              ]),\n              createWebAppElement("button", {\n                "aria-label": "Close response inspector",\n                className: "rivet-agent-response-inspector-close",\n                onClick: onClose,\n                text: "\\xD7",\n                type: "button"\n              })\n            ]),\n            body\n          ]\n        )\n      ]\n    );\n  }\n  function formatTraceModelCallLabel(call) {\n    const profileName = call.profileName?.trim();\n    return profileName ? "Profile: " + profileName + " \\xB7 " + call.provider + " \\xB7 " + call.model : call.provider + " \\xB7 " + call.model;\n  }\n  function createTraceDetails(title, rows, omitted) {\n    const details = createWebAppElement("details", { className: "rivet-agent-response-inspector-section" }, [\n      createWebAppElement("summary", { text: title }),\n      createWebAppElement(\n        "div",\n        {},\n        rows.length ? rows : [createWebAppElement("p", { text: `No ${title.toLowerCase()} recorded.` })]\n      ),\n      ...omitted > 0 ? [createWebAppElement("p", { text: `${omitted} additional rows omitted by the trace limit.` })] : []\n    ]);\n    details.open = true;\n    return details;\n  }\n  function formatTraceDuration(value) {\n    return value == null ? "Unavailable" : `${(value / 1e3).toFixed(value >= 1e4 ? 1 : 2)} sec`;\n  }\n  function formatTraceTokens(value) {\n    return value == null ? "Not reported" : new Intl.NumberFormat().format(value);\n  }\n  function formatTraceCallUsage(usage) {\n    if (usage == null) return "usage not reported";\n    const parts = [\n      usage.promptTokens == null ? void 0 : `${formatTraceTokens(usage.promptTokens)} in`,\n      usage.completionTokens == null ? void 0 : `${formatTraceTokens(usage.completionTokens)} out`,\n      usage.cachedTokens == null ? void 0 : `${formatTraceTokens(usage.cachedTokens)} cached`,\n      usage.reasoningTokens == null ? void 0 : `${formatTraceTokens(usage.reasoningTokens)} reasoning`\n    ].filter((part) => part != null);\n    return parts.length > 0 ? parts.join(", ") : "usage not reported";\n  }\n  var browserGlobals = globalThis;\n  var CHAT_SEARCH_ICON_PATH = "m20 20-4.05-4.05m0 0a7 7 0 1 0-9.9-9.9 7 7 0 0 0 9.9 9.9z";\n  var CROSS_ICON_PATH = "M12 12 6 6m6 6 6 6m-6-6 6-6m-6 6-6 6";\n  var CHEVRON_LEFT_ICON_PATH = "m14 7-5 5 5 5";\n  var CHEVRON_RIGHT_ICON_PATH = "m10 7 5 5-5 5";\n  var CHAT_SEND_ICON_PATH = "M12 19V5m0 0 5 5m-5-5-5 5";\n  var PIN_ICON_PATH = "m4 20 5-5m0 0 3.956 3.956a1 1 0 0 0 1.626-.314l2.255-5.261a1 1 0 0 1 .548-.535l3.207-1.283a1 1 0 0 0 .336-1.635l-6.856-6.856a1 1 0 0 0-1.635.336l-1.283 3.207a1 1 0 0 1-.535.548L5.358 9.418a1 1 0 0 0-.314 1.626L9 15z";\n  var MORE_MENU_ICON_PATH = "M5 12h.01M12 12h.01M19 12h.01";\n  function isChatSearchShortcut(event) {\n    if (event.altKey || !(event.ctrlKey || event.metaKey)) {\n      return false;\n    }\n    return event.key.toLowerCase() === "f" || event.code === "KeyF";\n  }\n  function createLineIcon(pathData) {\n    const icon = document.createElementNS("http://www.w3.org/2000/svg", "svg");\n    icon.setAttribute("aria-hidden", "true");\n    icon.setAttribute("fill", "none");\n    icon.setAttribute("focusable", "false");\n    icon.setAttribute("viewBox", "0 0 24 24");\n    const path = document.createElementNS("http://www.w3.org/2000/svg", "path");\n    path.setAttribute("d", pathData);\n    path.setAttribute("stroke", "currentColor");\n    path.setAttribute("stroke-linecap", "round");\n    path.setAttribute("stroke-linejoin", "round");\n    path.setAttribute("stroke-width", "2");\n    icon.append(path);\n    return icon;\n  }\n  function createChatDateSeparator(presentation) {\n    return createWebAppElement(\n      "div",\n      { className: "rivet-web-app-chat-date-separator", "data-rivet-chat-search-ignore": "true", role: "separator" },\n      [createWebAppElement("time", { dateTime: presentation.dateTime, text: presentation.label })]\n    );\n  }\n  function mountRivetWebApp(root2, config2) {\n    root2.replaceChildren(createWebAppElement("div", { className: "rivet-web-app-loading", text: "Loading saved app data\\u2026" }));\n    void initializeRivetWebApp(root2, config2).catch(() => {\n      root2.replaceChildren(\n        createWebAppElement("div", {\n          className: "rivet-web-app-error",\n          role: "alert",\n          text: "This web app could not be initialized. Reload the page to try again."\n        })\n      );\n    });\n  }\n  async function initializeRivetWebApp(root2, config2) {\n    let revisionMismatch = false;\n    let disposeOutputResizeObservers = () => {\n    };\n    const chatPresentationStates = /* @__PURE__ */ new Map();\n    let chatScrollRenderStates = /* @__PURE__ */ new Map();\n    const browserPersistence = new UiGraphBrowserPersistence(config2.uiGraph);\n    await browserPersistence.initialize();\n    let storageWarning = browserPersistence.warning;\n    const hostedBrowserStorage = {\n      clearTransportIncompatibility: () => browserPersistence.clearTransportIncompatibility(),\n      loadSnapshot: () => browserPersistence.loadStoredValues(),\n      get: (key) => browserPersistence.loadStoredValue(key),\n      commit: async (patch) => void await browserPersistence.applyStoredValuePatch(patch, { requireDurable: true }),\n      reportTransportIncompatibility: (message) => browserPersistence.reportTransportIncompatibility(message)\n    };\n    const storedChatState = await browserPersistence.loadChatState();\n    const interactionController = createUiGraphInteractionController(config2.uiGraph, {\n      initialState: { ...config2.initialState, ...storedChatState }\n    });\n    let actionRunner = createHostedActionRunner(config2);\n    let nextStorageAction = 0;\n    const appliedStorageActionByKey = /* @__PURE__ */ new Map();\n    let isRestoringChatState = false;\n    let restoreActionRunnerFromPageCache = false;\n    const getChatPresentationState = (componentId) => {\n      let state = chatPresentationStates.get(componentId);\n      if (!state) {\n        state = {\n          activeMatchIndex: 0,\n          isMenuOpen: false,\n          isPinsOpen: false,\n          isSearchOpen: false,\n          query: ""\n        };\n        chatPresentationStates.set(componentId, state);\n      }\n      return state;\n    };\n    const schedule = (callback) => {\n      if (typeof requestAnimationFrame === "function") {\n        requestAnimationFrame(callback);\n      } else {\n        window.setTimeout(callback, 0);\n      }\n    };\n    const focusChatSearchInput = (componentId) => {\n      schedule(() => {\n        for (const input of root2.querySelectorAll(".rivet-web-app-chat-search-input")) {\n          if (input.dataset.rivetFocusComponentId === `chat-search-${componentId}`) {\n            input.focus();\n            return;\n          }\n        }\n      });\n    };\n    const focusChatSearchButton = (componentId) => {\n      schedule(() => {\n        for (const button of root2.querySelectorAll(".rivet-web-app-chat-search-button")) {\n          if (button.dataset.rivetChatSearchComponentId === componentId) {\n            button.focus();\n            return;\n          }\n        }\n      });\n    };\n    const focusChatReadingViewCloseButton = (componentId) => {\n      schedule(() => {\n        for (const button of root2.querySelectorAll(".rivet-web-app-chat-reading-view-close")) {\n          if (button.dataset.rivetChatReadingViewComponentId === componentId) {\n            button.focus();\n            return;\n          }\n        }\n      });\n    };\n    root2.addEventListener(\n      "pointerdown",\n      (event) => {\n        for (const dropdown of root2.querySelectorAll(".rivet-web-app-hosted-dropdown.open")) {\n          if (!dropdown.contains(event.target)) {\n            dropdown.classList.remove("open", "menu-open-up");\n            dropdown.querySelector(".rivet-web-app-hosted-dropdown-menu")?.remove();\n            const button = dropdown.querySelector(".rivet-web-app-hosted-dropdown-button");\n            button?.setAttribute("aria-expanded", "false");\n            button?.removeAttribute("aria-activedescendant");\n          }\n        }\n        for (const menu of root2.querySelectorAll(".rivet-web-app-chat-menu-anchor")) {\n          if (menu.contains(event.target)) continue;\n          const componentId = menu.dataset.rivetChatMenuComponentId;\n          const chatState = componentId ? chatPresentationStates.get(componentId) : void 0;\n          if (chatState?.isMenuOpen) {\n            chatState.isMenuOpen = false;\n            menu.querySelector(".rivet-web-app-chat-menu")?.remove();\n            menu.querySelector(".rivet-web-app-chat-menu-button")?.setAttribute("aria-expanded", "false");\n          }\n        }\n        for (const menu of root2.querySelectorAll(".rivet-web-app-chat-message-context-menu")) {\n          if (menu.contains(event.target)) continue;\n          const componentId = menu.dataset.rivetChatMessageMenuComponentId;\n          const chatState = componentId ? chatPresentationStates.get(componentId) : void 0;\n          if (chatState?.messageMenu) {\n            chatState.messageMenu = void 0;\n            menu.remove();\n          }\n        }\n      },\n      true\n    );\n    const escapeHtml = (value) => `${value ?? ""}`.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/\'/g, "&#039;");\n    const createSafeMarkdownRenderer = () => {\n      const Renderer = browserGlobals.marked?.Renderer;\n      if (typeof Renderer !== "function") return void 0;\n      const renderer = new Renderer();\n      renderer.html = (html) => escapeHtml(html);\n      return renderer;\n    };\n    const markdownRenderer = createSafeMarkdownRenderer();\n    const renderMarkdown = (value) => {\n      const parser = browserGlobals.marked?.parse ?? browserGlobals.marked?.marked;\n      const sanitize = browserGlobals.DOMPurify?.sanitize;\n      if (typeof parser !== "function" || typeof sanitize !== "function" || !markdownRenderer) {\n        return escapeHtml(value);\n      }\n      const policy = config2.markdownSanitizerPolicy;\n      return sanitize(parser(value, { renderer: markdownRenderer }), {\n        ALLOW_ARIA_ATTR: false,\n        ALLOW_DATA_ATTR: false,\n        ALLOW_UNKNOWN_PROTOCOLS: false,\n        ALLOWED_ATTR: policy.allowedAttributes,\n        ALLOWED_TAGS: policy.allowedTags,\n        ALLOWED_URI_REGEXP: new RegExp(policy.allowedUriRegExpSource, "i")\n      });\n    };\n    const renderMarkdownElement = (value, className, enhanceJsonBlocks = false) => {\n      const element = createWebAppElement("div", { className });\n      element.innerHTML = renderMarkdown(value);\n      if (enhanceJsonBlocks) {\n        enhanceUiGraphChatJsonCodeBlocks(element, config2.uiGraph.name);\n      }\n      return element;\n    };\n    const renderErrors = () => revisionMismatch ? [] : Object.entries(interactionController.getSnapshot().actionErrors).flatMap(\n      ([componentId, message]) => config2.uiGraph.components.some((component) => component.id === componentId && component.type === "chat") ? [] : [\n        createWebAppElement("div", {\n          className: "rivet-web-app-error",\n          "data-component-id": componentId,\n          text: message\n        })\n      ]\n    );\n    const renderStorageWarning = () => storageWarning ? [\n      createWebAppElement("div", {\n        className: "rivet-web-app-storage-warning",\n        role: "alert",\n        text: storageWarning.message\n      })\n    ] : [];\n    const renderRevisionMismatchModal = () => {\n      if (!revisionMismatch) return [];\n      return [\n        createWebAppElement("div", { className: "rivet-web-app-modal-backdrop" }, [\n          createWebAppElement(\n            "div",\n            {\n              "aria-labelledby": "rivet-web-app-revision-mismatch-title",\n              "aria-modal": "true",\n              className: "rivet-web-app-modal",\n              role: "dialog"\n            },\n            [\n              createWebAppElement("div", {\n                className: "rivet-web-app-modal-message",\n                id: "rivet-web-app-revision-mismatch-title",\n                text: "This app was updated. Reload to continue."\n              }),\n              createWebAppElement("button", {\n                className: "rivet-web-app-button rivet-web-app-modal-button",\n                onClick: () => window.location.reload(),\n                text: "Reload",\n                type: "button"\n              })\n            ]\n          )\n        ])\n      ];\n    };\n    const resetApp = () => {\n      revisionMismatch = false;\n      chatPresentationStates.clear();\n      chatScrollRenderStates.clear();\n      const chatState = getUiGraphChatPersistentState(config2.uiGraph, interactionController.getSnapshot().state);\n      isRestoringChatState = true;\n      interactionController.reset();\n      if (Object.keys(chatState).length > 0) {\n        interactionController.updateStatePatch(chatState);\n      }\n      isRestoringChatState = false;\n      void browserPersistence.saveChatState(interactionController.getSnapshot().state).catch(() => void 0);\n      render();\n    };\n    const runAction = async (component) => {\n      revisionMismatch = false;\n      await interactionController.runAction(\n        component,\n        async ({ abortOtherActions, componentId, reportProgress, signal, state }) => {\n          const storageAction = ++nextStorageAction;\n          try {\n            const result = await actionRunner.run({\n              browserStorage: hostedBrowserStorage,\n              componentId,\n              onProgress: reportProgress,\n              revisionKey: config2.revisionKey,\n              signal,\n              state\n            });\n            signal.throwIfAborted();\n            if (result.storagePatch && Object.keys(result.storagePatch).length > 0) {\n              await applyUiGraphWebAppStorageActionPatchAsync(\n                result.storagePatch,\n                storageAction,\n                appliedStorageActionByKey,\n                async (applicablePatch) => void await browserPersistence.applyStoredValuePatch(applicablePatch, { requireDurable: true })\n              );\n            }\n            if (component.type === "chat" && component.allowResponseInspection && result.responseTrace) {\n              await browserPersistence.saveResponseTrace(String(component.id), result.responseTrace);\n            }\n            return { statePatch: result.statePatch, responseTrace: result.responseTrace };\n          } catch (error) {\n            if (error instanceof HostedActionError && error.code === "revision_mismatch") {\n              revisionMismatch = true;\n              abortOtherActions();\n              return {};\n            }\n            throw error;\n          }\n        }\n      );\n    };\n    const renderComponent = (component, interaction) => {\n      const renderModel = getUiGraphComponentRenderModel(component, interaction.state);\n      let content;\n      switch (renderModel.type) {\n        case "text":\n          content = createWebAppElement("div", { className: "rivet-web-app-text", text: renderModel.text });\n          break;\n        case "markdown":\n          content = renderMarkdownElement(renderModel.markdown, "rivet-web-app-markdown markdown-body");\n          break;\n        case "gap":\n          content = createWebAppElement("div", {\n            "aria-hidden": "true",\n            className: `rivet-web-app-gap rivet-web-app-gap-${renderModel.size}`\n          });\n          break;\n        case "input":\n        case "textarea": {\n          const control = createWebAppElement(renderModel.type === "textarea" ? "textarea" : "input", {\n            className: "rivet-web-app-control inputarea",\n            "data-rivet-focus-component-id": renderModel.component.id,\n            placeholder: renderModel.component.placeholder ?? ""\n          });\n          control.value = renderModel.value;\n          control.addEventListener("input", () => {\n            interactionController.updateState(renderModel.component.stateKey, control.value);\n            refreshOutputComponents(renderModel.component.stateKey);\n          });\n          content = createWebAppElement("label", { className: "rivet-web-app-field" }, [\n            createWebAppElement("span", { text: renderModel.label }),\n            control\n          ]);\n          break;\n        }\n        case "dropdown": {\n          const wrapper = createWebAppElement("div", {\n            className: "rivet-web-app-dropdown rivet-web-app-hosted-dropdown",\n            "data-rivet-dropdown-value": renderModel.value\n          });\n          const menuId = `rivet-web-app-dropdown-${renderModel.component.id}`;\n          const button = createWebAppElement("button", {\n            "aria-controls": menuId,\n            "aria-expanded": "false",\n            "aria-haspopup": "listbox",\n            "aria-label": renderModel.label,\n            className: "rivet-web-app-hosted-dropdown-button",\n            "data-rivet-focus-component-id": renderModel.component.id,\n            "data-value": renderModel.value,\n            type: "button"\n          });\n          const selectedItem = renderModel.items.find((item) => item.value === renderModel.value);\n          button.append(\n            createWebAppElement("span", {\n              text: selectedItem?.label ?? (renderModel.items.length === 0 ? "No options available" : "Select an option")\n            }),\n            createWebAppElement("span", { "aria-hidden": "true", className: "rivet-web-app-dropdown-chevron" })\n          );\n          button.disabled = renderModel.items.length === 0;\n          const menu = createWebAppElement("div", {\n            "aria-label": renderModel.label,\n            className: "rivet-web-app-hosted-dropdown-menu",\n            id: menuId,\n            role: "listbox"\n          });\n          const selectedIndex = Math.max(\n            0,\n            renderModel.items.findIndex((item) => item.value === renderModel.value)\n          );\n          let activeIndex = selectedIndex;\n          const optionButtons = [];\n          const updateActiveOption = (index, focus = false) => {\n            activeIndex = Math.max(0, Math.min(index, optionButtons.length - 1));\n            optionButtons.forEach(\n              (option, optionIndex) => option.classList.toggle("is-focused", optionIndex === activeIndex)\n            );\n            button.setAttribute("aria-activedescendant", `${menuId}-option-${activeIndex}`);\n            const activeOption = optionButtons[activeIndex];\n            if (focus) {\n              activeOption?.focus();\n            }\n            if (typeof activeOption?.scrollIntoView === "function") {\n              activeOption.scrollIntoView({ block: "nearest" });\n            }\n          };\n          const updatePlacement = () => {\n            const rect = wrapper.getBoundingClientRect();\n            const availableBelow = window.innerHeight - rect.bottom;\n            const availableAbove = rect.top;\n            const menuHeight = Math.min(220, renderModel.items.length * 30 + 8);\n            wrapper.classList.toggle(\n              "menu-open-up",\n              availableBelow < Math.min(menuHeight, 160) && availableAbove > availableBelow\n            );\n          };\n          const setOpen = (isOpen, nextActiveIndex = selectedIndex, focus = false) => {\n            wrapper.classList.toggle("open", isOpen);\n            button.setAttribute("aria-expanded", `${isOpen}`);\n            if (isOpen) {\n              wrapper.append(menu);\n              updatePlacement();\n              updateActiveOption(nextActiveIndex, focus);\n            } else {\n              wrapper.classList.remove("menu-open-up");\n              button.removeAttribute("aria-activedescendant");\n              menu.remove();\n            }\n          };\n          const selectOption = (index) => {\n            const item = renderModel.items[index];\n            if (!item) return;\n            interactionController.updateState(renderModel.component.stateKey, item.value);\n            refreshOutputComponents(renderModel.component.stateKey);\n            setOpen(false);\n            button.focus();\n          };\n          button.addEventListener("click", () => {\n            const wasOpen = wrapper.classList.contains("open");\n            setOpen(!wasOpen, activeIndex, !wasOpen);\n          });\n          button.addEventListener("keydown", (event) => {\n            if (event.key === "ArrowDown") {\n              event.preventDefault();\n              setOpen(true, wrapper.classList.contains("open") ? activeIndex + 1 : selectedIndex + 1, true);\n            } else if (event.key === "ArrowUp") {\n              event.preventDefault();\n              setOpen(true, wrapper.classList.contains("open") ? activeIndex - 1 : selectedIndex - 1, true);\n            } else if (event.key === "Home") {\n              event.preventDefault();\n              setOpen(true, 0, true);\n            } else if (event.key === "End") {\n              event.preventDefault();\n              setOpen(true, optionButtons.length - 1, true);\n            } else if (event.key === "Escape") {\n              setOpen(false);\n            }\n          });\n          renderModel.items.forEach((item, index) => {\n            const option = createWebAppElement("button", {\n              "aria-selected": `${item.value === renderModel.value}`,\n              className: "rivet-web-app-hosted-dropdown-option",\n              "data-value": item.value,\n              id: `${menuId}-option-${index}`,\n              role: "option",\n              text: item.label,\n              type: "button"\n            });\n            option.addEventListener("focus", () => {\n              updateActiveOption(index);\n            });\n            option.addEventListener("mousemove", () => {\n              updateActiveOption(index);\n            });\n            option.addEventListener("click", () => {\n              selectOption(index);\n            });\n            option.addEventListener("keydown", (event) => {\n              if (event.key === "ArrowDown") {\n                event.preventDefault();\n                updateActiveOption(activeIndex + 1, true);\n              } else if (event.key === "ArrowUp") {\n                event.preventDefault();\n                updateActiveOption(activeIndex - 1, true);\n              } else if (event.key === "Home") {\n                event.preventDefault();\n                updateActiveOption(0, true);\n              } else if (event.key === "End") {\n                event.preventDefault();\n                updateActiveOption(optionButtons.length - 1, true);\n              } else if (event.key === "Enter" || event.key === " ") {\n                event.preventDefault();\n                selectOption(index);\n              } else if (event.key === "Escape") {\n                event.preventDefault();\n                setOpen(false);\n                button.focus();\n              }\n            });\n            optionButtons.push(option);\n            menu.append(option);\n          });\n          wrapper.append(button);\n          content = createWebAppElement("div", { className: "rivet-web-app-field" }, [\n            createWebAppElement("span", { text: renderModel.label }),\n            wrapper\n          ]);\n          break;\n        }\n        case "button": {\n          const isLoading = interaction.loadingComponentIds.has(renderModel.component.id);\n          const button = createWebAppElement(\n            "button",\n            {\n              "aria-busy": isLoading,\n              "aria-label": isLoading ? `${renderModel.label} (running)` : void 0,\n              className: "rivet-web-app-button",\n              onClick: () => void runAction(renderModel.component),\n              text: isLoading ? renderModel.label : void 0,\n              type: "button"\n            },\n            isLoading ? [createWebAppElement("span", { "aria-hidden": "true", className: "rivet-web-app-running-indicator" })] : []\n          );\n          if (!isLoading) {\n            button.textContent = renderModel.label;\n          }\n          button.disabled = isLoading;\n          const actionChildren = [button];\n          if (isLoading) {\n            actionChildren.push(\n              createWebAppElement("button", {\n                className: "rivet-web-app-abort-button",\n                onClick: () => interactionController.cancelAction(renderModel.component.id),\n                text: "Abort",\n                type: "button"\n              })\n            );\n          }\n          const progress = interaction.actionProgress[renderModel.component.id];\n          const progressElement = renderActionProgress(progress);\n          if (progressElement) actionChildren.push(progressElement);\n          content = createWebAppElement(\n            "div",\n            { className: `rivet-web-app-action-stack${isLoading ? " rivet-web-app-action-stack-running" : ""}` },\n            actionChildren\n          );\n          break;\n        }\n        case "chat": {\n          const componentId = String(renderModel.component.id);\n          const isRunning = interaction.runningComponentIds.has(renderModel.component.id);\n          const hasMessages = renderModel.messages.length > 0;\n          const searchState = getChatPresentationState(componentId);\n          if (!hasMessages) {\n            searchState.isSearchOpen = false;\n            searchState.isPinsOpen = false;\n            searchState.messageMenu = void 0;\n            searchState.readingMessageContent = void 0;\n            searchState.query = "";\n            searchState.activeMatchIndex = 0;\n          }\n          if (renderModel.pins.length === 0) {\n            searchState.isPinsOpen = false;\n          }\n          const submit = () => {\n            const statePatch = createUiGraphChatSubmissionStatePatch(\n              renderModel.component.id,\n              interactionController.getSnapshot().state\n            );\n            if (!statePatch || isRunning) return;\n            textarea.focus();\n            interactionController.updateStatePatch(statePatch);\n            void runAction(renderModel.component);\n          };\n          const pinnedMessageIndexes = new Set(renderModel.pins.map((pin) => pin.messageIndex));\n          const togglePin = (messageIndex) => {\n            const statePatch = createUiGraphChatPinStatePatch(\n              renderModel.component.id,\n              interactionController.getSnapshot().state,\n              messageIndex\n            );\n            if (statePatch) {\n              interactionController.updateStatePatch(statePatch);\n              render();\n            }\n          };\n          const openMessageContextMenu = (messageIndex, event) => {\n            event.preventDefault();\n            searchState.isMenuOpen = false;\n            searchState.isSearchOpen = false;\n            const margin = 8;\n            const menuWidth = 196;\n            const menuHeight = renderModel.messages[messageIndex]?.role === "assistant" ? renderModel.component.allowResponseInspection ? 108 : 76 : 44;\n            searchState.messageMenu = {\n              messageIndex,\n              x: Math.min(event.clientX, Math.max(margin, globalThis.innerWidth - menuWidth - margin)),\n              y: Math.min(event.clientY, Math.max(margin, globalThis.innerHeight - menuHeight - margin))\n            };\n            render();\n          };\n          const openSelectedMessageInReadingView = () => {\n            const messageIndex = searchState.messageMenu?.messageIndex;\n            searchState.messageMenu = void 0;\n            const message = messageIndex == null ? void 0 : renderModel.messages[messageIndex];\n            if (message?.role !== "assistant") return;\n            searchState.readingMessageContent = message.content;\n            render();\n            focusChatReadingViewCloseButton(componentId);\n          };\n          const closeReadingView = () => {\n            searchState.readingMessageContent = void 0;\n            render();\n          };\n          const inspectSelectedResponse = () => {\n            const messageIndex = searchState.messageMenu?.messageIndex;\n            searchState.messageMenu = void 0;\n            const message = messageIndex == null ? void 0 : renderModel.messages[messageIndex];\n            searchState.inspectedTrace = null;\n            render();\n            if (message?.role === "assistant" && typeof message.responseTraceId === "string") {\n              void browserPersistence.loadResponseTrace(String(component.id), message.responseTraceId).then((trace) => {\n                searchState.inspectedTrace = trace ?? null;\n                render();\n              }).catch(() => render());\n            }\n          };\n          const closeResponseInspector = () => {\n            searchState.inspectedTrace = void 0;\n            render();\n          };\n          const removeSelectedMessage = () => {\n            const messageIndex = searchState.messageMenu?.messageIndex;\n            searchState.messageMenu = void 0;\n            if (messageIndex == null) return;\n            const statePatch = createUiGraphChatMessageRemovalStatePatch(\n              renderModel.component.id,\n              interactionController.getSnapshot().state,\n              messageIndex\n            );\n            if (statePatch) {\n              interactionController.updateStatePatch(statePatch);\n            }\n            render();\n          };\n          const messagePresentations = getUiGraphChatMessagePresentations(renderModel.messages);\n          const messageNodes = renderModel.messages.map((message, messageIndex) => {\n            const messagePresentation = messagePresentations[messageIndex];\n            const messageContent = renderMarkdownElement(\n              message.content,\n              "rivet-web-app-chat-message-markdown markdown-body",\n              true\n            );\n            const messageElement = createWebAppElement(\n              "div",\n              {\n                className: `rivet-web-app-chat-message rivet-web-app-chat-message-${message.role}${searchState.messageMenu?.messageIndex === messageIndex ? " rivet-web-app-chat-message-context-selected" : ""}`,\n                onContextMenu: (event) => openMessageContextMenu(messageIndex, event)\n              },\n              [\n                messageContent,\n                ...messagePresentation?.timestamp ? [\n                  createWebAppElement("time", {\n                    className: "rivet-web-app-chat-message-time",\n                    "data-rivet-chat-search-ignore": "true",\n                    dateTime: messagePresentation.timestamp.dateTime,\n                    text: messagePresentation.timestamp.label,\n                    title: [\n                      messagePresentation.timestamp.dateTime,\n                      messagePresentation.timestamp.elapsedSincePreviousUserMessage\n                    ].filter(Boolean).join("\\n")\n                  })\n                ] : []\n              ]\n            );\n            if (message.role === "user") {\n              messageElement.dataset.rivetChatMessageIndex = String(messageIndex);\n              return messagePresentation?.dateSeparator ? [createChatDateSeparator(messagePresentation.dateSeparator), messageElement] : [messageElement];\n            }\n            const pinned = pinnedMessageIndexes.has(messageIndex);\n            const pinButton = createWebAppElement(\n              "button",\n              {\n                "aria-label": pinned ? "Unpin response" : "Pin response",\n                "aria-pressed": `${pinned}`,\n                className: `rivet-web-app-chat-pin-button${pinned ? " active" : ""}`,\n                onClick: () => togglePin(messageIndex),\n                title: pinned ? "Unpin response" : "Pin response",\n                type: "button"\n              },\n              [createLineIcon(PIN_ICON_PATH)]\n            );\n            const messageRow = createWebAppElement(\n              "div",\n              {\n                className: "rivet-web-app-chat-message-row",\n                "data-rivet-chat-message-index": String(messageIndex)\n              },\n              [messageElement, pinButton]\n            );\n            return messagePresentation?.dateSeparator ? [createChatDateSeparator(messagePresentation.dateSeparator), messageRow] : [messageRow];\n          }).flat();\n          if (messageNodes.length === 0) {\n            messageNodes.push(\n              createWebAppElement("div", { className: "rivet-web-app-chat-empty" }, [\n                createWebAppElement("strong", { text: "Start a conversation" })\n              ])\n            );\n          }\n          if (isRunning) {\n            messageNodes.push(\n              createWebAppElement(\n                "div",\n                {\n                  className: "rivet-web-app-chat-message rivet-web-app-chat-message-assistant rivet-web-app-chat-thinking"\n                },\n                [createWebAppElement("span"), createWebAppElement("span"), createWebAppElement("span")]\n              )\n            );\n          }\n          const messagesElement = createWebAppElement(\n            "div",\n            {\n              "aria-live": "polite",\n              "aria-relevant": "additions text",\n              className: "rivet-web-app-chat-messages",\n              "data-rivet-chat-component-id": componentId,\n              role: "log"\n            },\n            messageNodes\n          );\n          const searchResult = searchState.isSearchOpen ? highlightUiGraphChatSearchMatches(messagesElement, searchState.query, searchState.activeMatchIndex) : (clearUiGraphChatSearchMatches(messagesElement), { activeIndex: -1, matches: [] });\n          searchState.activeMatchIndex = searchResult.activeIndex < 0 ? 0 : searchResult.activeIndex;\n          const closeSearch = () => {\n            searchState.isSearchOpen = false;\n            searchState.query = "";\n            searchState.activeMatchIndex = 0;\n            render();\n            focusChatSearchButton(componentId);\n          };\n          const openSearch = () => {\n            if (!hasMessages) return;\n            searchState.isMenuOpen = false;\n            searchState.isPinsOpen = false;\n            searchState.messageMenu = void 0;\n            searchState.isSearchOpen = true;\n            searchState.activeMatchIndex = 0;\n            render();\n            focusChatSearchInput(componentId);\n          };\n          const togglePins = () => {\n            searchState.isMenuOpen = false;\n            searchState.messageMenu = void 0;\n            searchState.isSearchOpen = false;\n            searchState.isPinsOpen = !searchState.isPinsOpen;\n            render();\n          };\n          const toggleMenu = () => {\n            searchState.isSearchOpen = false;\n            searchState.messageMenu = void 0;\n            searchState.isMenuOpen = !searchState.isMenuOpen;\n            render();\n          };\n          const flushChatHistory = () => {\n            searchState.isMenuOpen = false;\n            searchState.isPinsOpen = false;\n            searchState.messageMenu = void 0;\n            interactionController.updateStatePatch(createUiGraphChatHistoryFlushStatePatch(renderModel.component.id));\n            render();\n          };\n          const moveSearchMatch = (direction) => {\n            if (searchResult.matches.length === 0) return;\n            searchState.activeMatchIndex += direction;\n            render();\n          };\n          const createSearchNavigationButton = (label, direction, iconPath) => {\n            const button = createWebAppElement(\n              "button",\n              {\n                "aria-label": label,\n                className: "rivet-web-app-chat-search-navigation-button",\n                onClick: () => moveSearchMatch(direction),\n                type: "button"\n              },\n              [createLineIcon(iconPath)]\n            );\n            button.disabled = searchResult.matches.length === 0;\n            return button;\n          };\n          const searchPanel = searchState.isSearchOpen ? createWebAppElement("div", { className: "rivet-web-app-chat-search", role: "search" }, [\n            createWebAppElement("input", {\n              "aria-label": "Search chat messages",\n              className: "rivet-web-app-chat-search-input",\n              "data-rivet-focus-component-id": `chat-search-${componentId}`,\n              placeholder: "Search chat",\n              type: "search",\n              value: searchState.query\n            }),\n            createWebAppElement("span", {\n              "aria-live": "polite",\n              className: "rivet-web-app-chat-search-count",\n              text: searchResult.matches.length === 0 ? "0\\u2009/\\u20090" : `${searchResult.activeIndex + 1}\\u2009/\\u2009${searchResult.matches.length}`\n            }),\n            createWebAppElement("span", { className: "rivet-web-app-chat-search-navigation" }, [\n              createSearchNavigationButton("Previous chat search result", -1, CHEVRON_LEFT_ICON_PATH),\n              createSearchNavigationButton("Next chat search result", 1, CHEVRON_RIGHT_ICON_PATH)\n            ]),\n            createWebAppElement(\n              "button",\n              {\n                "aria-label": "Close chat search",\n                className: "rivet-web-app-chat-search-close-button",\n                onClick: closeSearch,\n                title: "Close chat search",\n                type: "button"\n              },\n              [createLineIcon(CROSS_ICON_PATH)]\n            )\n          ]) : void 0;\n          const searchInput = searchPanel?.querySelector(".rivet-web-app-chat-search-input");\n          searchInput?.addEventListener("input", () => {\n            searchState.query = searchInput.value;\n            searchState.activeMatchIndex = 0;\n            render();\n          });\n          searchInput?.addEventListener("keydown", (event) => {\n            if (event.key === "Escape") {\n              event.preventDefault();\n              closeSearch();\n            } else if (event.key === "Enter" && searchState.query.trim()) {\n              event.preventDefault();\n              moveSearchMatch(event.shiftKey ? -1 : 1);\n            }\n          });\n          const pinsPanel = searchState.isPinsOpen ? createWebAppElement(\n            "div",\n            { "aria-label": "Pinned responses", className: "rivet-web-app-chat-pins", role: "region" },\n            renderModel.pins.map((pin) => {\n              const exchanges = [];\n              if (pin.prompt) {\n                exchanges.push(\n                  createWebAppElement("div", { className: "rivet-web-app-chat-pin-exchange" }, [\n                    createWebAppElement("strong", { text: "You asked" }),\n                    renderMarkdownElement(pin.prompt.content, "rivet-web-app-chat-pin-markdown markdown-body")\n                  ])\n                );\n              }\n              exchanges.push(\n                createWebAppElement("div", { className: "rivet-web-app-chat-pin-exchange" }, [\n                  createWebAppElement("strong", { text: "Response" }),\n                  renderMarkdownElement(pin.response.content, "rivet-web-app-chat-pin-markdown markdown-body")\n                ])\n              );\n              return createWebAppElement("div", { className: "rivet-web-app-chat-pin" }, [\n                createWebAppElement("button", {\n                  "aria-label": "Show pinned response in conversation",\n                  className: "rivet-web-app-chat-pin-reveal",\n                  onClick: () => {\n                    const messageIndex = pin.promptMessageIndex ?? pin.messageIndex;\n                    const message = messagesElement.querySelector(\n                      `[data-rivet-chat-message-index="${messageIndex}"]`\n                    );\n                    revealUiGraphChatElement(messagesElement, message ?? void 0, "start");\n                  },\n                  title: "Show pinned response in conversation",\n                  type: "button"\n                }),\n                ...exchanges\n              ]);\n            })\n          ) : void 0;\n          const textarea = createWebAppElement("textarea", {\n            "aria-label": "Message",\n            "data-rivet-chat-component-id": renderModel.component.id,\n            "data-rivet-focus-component-id": renderModel.component.id,\n            placeholder: renderModel.component.placeholder || "Message...",\n            rows: "1"\n          });\n          textarea.value = renderModel.draft;\n          const sendButton = createWebAppElement("button", {\n            "aria-label": isRunning ? "Stop response" : "Send message",\n            className: `rivet-web-app-chat-send${isRunning ? " rivet-web-app-chat-stop" : ""}`,\n            title: isRunning ? "Stop response" : "Send message",\n            type: isRunning ? "button" : "submit"\n          });\n          if (isRunning) {\n            sendButton.append(\n              createWebAppElement("span", { "aria-hidden": "true", className: "rivet-web-app-chat-stop-icon" })\n            );\n            sendButton.addEventListener("click", () => interactionController.cancelAction(renderModel.component.id));\n          } else {\n            sendButton.append(createLineIcon(CHAT_SEND_ICON_PATH));\n            sendButton.disabled = !renderModel.draft.trim();\n          }\n          textarea.addEventListener("input", () => {\n            interactionController.updateState(getUiGraphChatDraftStateKey(renderModel.component.id), textarea.value);\n            if (!isRunning) {\n              sendButton.disabled = !textarea.value.trim();\n            }\n          });\n          textarea.addEventListener("keydown", (event) => {\n            if (event.key === "Enter" && !event.shiftKey && !event.isComposing) {\n              event.preventDefault();\n              submit();\n            }\n          });\n          const composer = createWebAppElement(\n            "form",\n            {\n              className: "rivet-web-app-chat-composer",\n              onSubmit: (event) => {\n                event.preventDefault();\n                submit();\n              }\n            },\n            [textarea, sendButton]\n          );\n          const actionError = interaction.actionErrors[renderModel.component.id];\n          const headerActions = [];\n          if (renderModel.pins.length > 0) {\n            headerActions.push(\n              createWebAppElement(\n                "button",\n                {\n                  "aria-label": `${searchState.isPinsOpen ? "Hide" : "Show"} ${renderModel.pins.length} pinned ${renderModel.pins.length === 1 ? "response" : "responses"}`,\n                  "aria-pressed": `${searchState.isPinsOpen}`,\n                  className: "rivet-web-app-chat-pins-button",\n                  onClick: togglePins,\n                  title: searchState.isPinsOpen ? "Hide pinned responses" : "Show pinned responses",\n                  type: "button"\n                },\n                [createLineIcon(PIN_ICON_PATH), createWebAppElement("span", { text: String(renderModel.pins.length) })]\n              )\n            );\n          }\n          if (hasMessages) {\n            headerActions.push(\n              createWebAppElement(\n                "button",\n                {\n                  "aria-label": searchState.isSearchOpen ? "Close chat search" : "Search chat",\n                  "aria-pressed": `${searchState.isSearchOpen}`,\n                  className: "rivet-web-app-chat-search-button",\n                  "data-rivet-chat-search-component-id": componentId,\n                  onClick: () => searchState.isSearchOpen ? closeSearch() : openSearch(),\n                  title: searchState.isSearchOpen ? "Close chat search" : "Search chat",\n                  type: "button"\n                },\n                [createLineIcon(CHAT_SEARCH_ICON_PATH)]\n              )\n            );\n          }\n          const readingView = searchState.readingMessageContent == null ? void 0 : createWebAppElement(\n            "div",\n            {\n              className: "rivet-web-app-modal-backdrop rivet-web-app-chat-reading-view-backdrop",\n              onClick: (event) => {\n                if (event.target === event.currentTarget) closeReadingView();\n              }\n            },\n            [\n              createWebAppElement(\n                "section",\n                {\n                  "aria-label": "Assistant message",\n                  "aria-modal": "true",\n                  className: "rivet-web-app-chat-reading-view",\n                  role: "dialog"\n                },\n                [\n                  createWebAppElement("header", { className: "rivet-web-app-chat-reading-view-header" }, [\n                    createWebAppElement("strong", { text: "Assistant message" }),\n                    createWebAppElement(\n                      "button",\n                      {\n                        "aria-label": "Close reading view",\n                        className: "rivet-web-app-chat-reading-view-close",\n                        "data-rivet-chat-reading-view-component-id": componentId,\n                        onClick: closeReadingView,\n                        title: "Close reading view",\n                        type: "button"\n                      },\n                      [createLineIcon(CROSS_ICON_PATH)]\n                    )\n                  ]),\n                  renderMarkdownElement(\n                    searchState.readingMessageContent,\n                    "rivet-web-app-chat-message-markdown rivet-web-app-chat-reading-view-markdown markdown-body",\n                    true\n                  )\n                ]\n              )\n            ]\n          );\n          const chatChildren = [\n            createWebAppElement("div", { className: "rivet-web-app-chat-header" }, [\n              createWebAppElement("span", { className: "rivet-web-app-chat-title" }, [\n                createWebAppElement("span", { text: "Chat" }),\n                createWebAppElement(\n                  "span",\n                  {\n                    className: "rivet-web-app-chat-menu-anchor",\n                    "data-rivet-chat-menu-component-id": componentId\n                  },\n                  [\n                    createWebAppElement(\n                      "button",\n                      {\n                        "aria-expanded": `${searchState.isMenuOpen}`,\n                        "aria-haspopup": "menu",\n                        "aria-label": "Chat options",\n                        className: "rivet-web-app-chat-menu-button",\n                        onClick: toggleMenu,\n                        title: "Chat options",\n                        type: "button"\n                      },\n                      [createLineIcon(MORE_MENU_ICON_PATH)]\n                    ),\n                    ...searchState.isMenuOpen ? [\n                      createWebAppElement("span", { className: "rivet-web-app-chat-menu", role: "menu" }, [\n                        createWebAppElement("button", {\n                          onClick: flushChatHistory,\n                          role: "menuitem",\n                          text: "Flush chat history",\n                          type: "button"\n                        })\n                      ])\n                    ] : []\n                  ]\n                )\n              ]),\n              createWebAppElement("span", { className: "rivet-web-app-chat-header-actions" }, headerActions)\n            ]),\n            createWebAppElement("div", { className: "rivet-web-app-chat-history" }, [\n              ...pinsPanel ? [pinsPanel] : searchPanel ? [searchPanel] : [],\n              messagesElement\n            ]),\n            ...searchState.messageMenu ? [\n              createWebAppElement(\n                "div",\n                {\n                  "data-rivet-chat-message-menu-component-id": componentId,\n                  className: "rivet-web-app-chat-message-context-menu",\n                  role: "menu",\n                  style: `left: ${searchState.messageMenu.x}px; top: ${searchState.messageMenu.y}px;`\n                },\n                [\n                  ...renderModel.messages[searchState.messageMenu.messageIndex]?.role === "assistant" ? [\n                    createWebAppElement("button", {\n                      onClick: openSelectedMessageInReadingView,\n                      role: "menuitem",\n                      text: "Open in reading view",\n                      type: "button"\n                    })\n                  ] : [],\n                  ...renderModel.component.allowResponseInspection && renderModel.messages[searchState.messageMenu.messageIndex]?.role === "assistant" ? [\n                    createWebAppElement("button", {\n                      onClick: inspectSelectedResponse,\n                      role: "menuitem",\n                      text: "Inspect response",\n                      type: "button"\n                    })\n                  ] : [],\n                  createWebAppElement("button", {\n                    onClick: removeSelectedMessage,\n                    role: "menuitem",\n                    text: "Remove message",\n                    type: "button"\n                  })\n                ]\n              )\n            ] : [],\n            ...readingView ? [readingView] : [],\n            ...searchState.inspectedTrace !== void 0 ? [createResponseInspectorElement(searchState.inspectedTrace, closeResponseInspector)] : []\n          ];\n          if (actionError) {\n            chatChildren.push(\n              createWebAppElement("div", { className: "rivet-web-app-chat-error", role: "alert", text: actionError })\n            );\n          }\n          const chatProgress = renderActionProgress(interaction.actionProgress[renderModel.component.id]);\n          if (chatProgress) chatChildren.push(chatProgress);\n          chatChildren.push(composer);\n          const chat = createWebAppElement("section", { className: "rivet-web-app-chat" }, chatChildren);\n          chat.addEventListener("keydown", (event) => {\n            if (event.key === "Escape" && searchState.inspectedTrace !== void 0) {\n              event.preventDefault();\n              closeResponseInspector();\n              return;\n            }\n            if (event.key === "Escape" && searchState.readingMessageContent != null) {\n              event.preventDefault();\n              closeReadingView();\n              return;\n            }\n            if (event.key === "Escape" && searchState.messageMenu) {\n              event.preventDefault();\n              searchState.messageMenu = void 0;\n              render();\n              return;\n            }\n            if (event.key === "Escape" && searchState.isMenuOpen) {\n              event.preventDefault();\n              searchState.isMenuOpen = false;\n              render();\n              return;\n            }\n            if (hasMessages && isChatSearchShortcut(event)) {\n              event.preventDefault();\n              openSearch();\n            }\n          });\n          content = chat;\n          break;\n        }\n        case "output": {\n          const { output } = renderModel;\n          const isCollapsed = output.hasValue && interaction.collapsedOutputComponentIds.has(renderModel.component.id);\n          const header = output.hasValue ? createWebAppElement(\n            "button",\n            {\n              "aria-expanded": `${!isCollapsed}`,\n              "aria-label": `${isCollapsed ? "Expand" : "Collapse"} ${renderModel.label}`,\n              className: "rivet-web-app-output-header rivet-web-app-output-toggle",\n              onClick: () => interactionController.toggleOutputCollapsed(renderModel.component.id),\n              title: isCollapsed ? "Expand output" : "Collapse output",\n              type: "button"\n            },\n            [\n              createWebAppElement("span", { className: "rivet-web-app-output-title", text: renderModel.label }),\n              createWebAppElement("span", {\n                "aria-hidden": "true",\n                className: `rivet-web-app-output-toggle-icon${isCollapsed ? " collapsed" : ""}`\n              })\n            ]\n          ) : createWebAppElement("div", { className: "rivet-web-app-output-header" }, [\n            createWebAppElement("div", { className: "rivet-web-app-output-title", text: renderModel.label })\n          ]);\n          const children = [header];\n          if (output.hasValue && !isCollapsed) {\n            const contentActions = [\n              createWebAppElement("button", {\n                "aria-label": "Copy output",\n                className: "rivet-web-app-output-action-button rivet-web-app-output-copy-button",\n                onClick: (event) => {\n                  event.stopPropagation();\n                  void copyUiGraphText(output.renderedValue);\n                },\n                title: "Copy output",\n                type: "button"\n              })\n            ];\n            if (output.jsonDownloadValue != null) {\n              contentActions.push(\n                createWebAppElement("button", {\n                  "aria-label": "Download JSON",\n                  className: "rivet-web-app-output-action-button rivet-web-app-output-download-button",\n                  onClick: (event) => {\n                    event.stopPropagation();\n                    downloadUiGraphJsonOutput(output.jsonDownloadValue, config2.uiGraph.name);\n                  },\n                  title: "Download JSON",\n                  type: "button"\n                })\n              );\n            }\n            children.push(\n              createWebAppElement("div", { className: "rivet-web-app-output-content" }, [\n                createWebAppElement("div", { className: "rivet-web-app-output-content-actions" }, contentActions),\n                createWebAppElement("div", { className: "rivet-web-app-output-content-body" }, [\n                  output.renderAs === "image" ? output.imageSource ? createWebAppElement("img", {\n                    alt: renderModel.label,\n                    className: "rivet-web-app-output-image",\n                    decoding: "async",\n                    loading: "lazy",\n                    referrerpolicy: "no-referrer",\n                    src: output.imageSource\n                  }) : createWebAppElement("div", {\n                    className: "rivet-web-app-output-image-placeholder",\n                    text: output.imageErrorMessage ?? ""\n                  }) : output.renderAs === "markdown" ? renderMarkdownElement(\n                    output.renderedValue,\n                    "rivet-web-app-output-markdown markdown-body rivet-markdown-output"\n                  ) : output.renderAs === "json" ? createProgressiveJsonOutput(output.renderedValue) : createWebAppElement("pre", { text: output.renderedValue })\n                ])\n              ])\n            );\n          }\n          content = createWebAppElement(\n            "section",\n            {\n              className: `rivet-web-app-card rivet-web-app-output${output.hasValue ? " rivet-web-app-output-has-value" : ""}${isCollapsed ? " rivet-web-app-output-collapsed" : ""}`\n            },\n            children\n          );\n          break;\n        }\n      }\n      if (!content) {\n        throw new Error("Unsupported UI graph component.");\n      }\n      return createWebAppElement(\n        "div",\n        {\n          className: "rivet-web-app-component-frame",\n          "data-rivet-web-app-component-id": component.id,\n          "data-rivet-web-app-component-type": component.type\n        },\n        [content]\n      );\n    };\n    const refreshOutputComponents = (stateKey) => {\n      const interaction = interactionController.getSnapshot();\n      const frames = [...root2.querySelectorAll("[data-rivet-web-app-component-id]")];\n      for (const component of config2.uiGraph.components) {\n        if (component.type !== "output" || component.stateKey !== stateKey) continue;\n        const frame = frames.find((candidate) => candidate.dataset.rivetWebAppComponentId === component.id);\n        if (frame) {\n          frame.replaceWith(renderComponent(component, interaction));\n        }\n      }\n      disposeOutputResizeObservers();\n      disposeOutputResizeObservers = observeOutputResizeBounds(root2);\n    };\n    const render = () => {\n      const focusedControl = captureFocusedTextControl(root2);\n      const previousChatScrollTops = /* @__PURE__ */ new Map();\n      root2.querySelectorAll(".rivet-web-app-chat-messages").forEach((messages) => {\n        const componentId = messages.dataset.rivetChatComponentId;\n        if (componentId) previousChatScrollTops.set(componentId, messages.scrollTop);\n      });\n      const interaction = interactionController.getSnapshot();\n      const surface = createWebAppElement("main", { className: "rivet-web-app-surface" }, [\n        createWebAppElement("div", { className: "rivet-web-app-toolbar" }, [\n          createWebAppElement("button", {\n            "aria-label": "Reset app",\n            className: "rivet-web-app-reset-button",\n            onClick: resetApp,\n            title: "Reset app",\n            type: "button"\n          })\n        ]),\n        ...config2.uiGraph.components.map((component) => renderComponent(component, interaction)),\n        ...renderStorageWarning(),\n        ...renderErrors()\n      ]);\n      disposeOutputResizeObservers();\n      root2.replaceChildren(surface, ...renderRevisionMismatchModal());\n      root2.querySelector(".rivet-agent-response-inspector-close")?.focus();\n      disposeOutputResizeObservers = observeOutputResizeBounds(root2);\n      const chatMessagesByComponentId = /* @__PURE__ */ new Map();\n      root2.querySelectorAll(".rivet-web-app-chat-messages").forEach((messages) => {\n        const componentId = messages.dataset.rivetChatComponentId;\n        if (componentId) chatMessagesByComponentId.set(componentId, messages);\n      });\n      const nextChatScrollRenderStates = /* @__PURE__ */ new Map();\n      for (const component of config2.uiGraph.components) {\n        if (component.type !== "chat") continue;\n        const componentId = String(component.id);\n        const messages = chatMessagesByComponentId.get(componentId);\n        if (!messages) continue;\n        const renderModel = getUiGraphComponentRenderModel(component, interaction.state);\n        if (renderModel.type !== "chat") continue;\n        const searchState = getChatPresentationState(componentId);\n        const nextState = {\n          activeSearchMatchIndex: searchState.activeMatchIndex,\n          isRunning: interaction.runningComponentIds.has(component.id),\n          isSearchOpen: searchState.isSearchOpen,\n          messageCount: renderModel.messages.length,\n          messagesState: interaction.state[getUiGraphChatMessagesStateKey(component.id)],\n          searchQuery: searchState.query\n        };\n        const previousState = chatScrollRenderStates.get(componentId);\n        const searchTargetChanged = !previousState || previousState.activeSearchMatchIndex !== nextState.activeSearchMatchIndex || previousState.isSearchOpen !== nextState.isSearchOpen || previousState.messagesState !== nextState.messagesState || previousState.searchQuery !== nextState.searchQuery;\n        const shouldFollowLatest = !previousState || previousState.isRunning !== nextState.isRunning || previousState.messageCount !== nextState.messageCount;\n        if (searchState.isSearchOpen && searchState.query.trim() && searchTargetChanged) {\n          revealUiGraphChatSearchMatch(\n            messages,\n            messages.querySelector(".rivet-web-app-chat-search-match-active") ?? void 0\n          );\n        } else if (shouldFollowLatest) {\n          messages.scrollTop = messages.scrollHeight;\n        } else {\n          messages.scrollTop = previousChatScrollTops.get(componentId) ?? messages.scrollHeight;\n        }\n        nextChatScrollRenderStates.set(componentId, nextState);\n      }\n      chatScrollRenderStates = nextChatScrollRenderStates;\n      if (revisionMismatch) {\n        root2.querySelector(".rivet-web-app-modal-button")?.focus();\n      } else if (focusedControl) {\n        restoreFocusedTextControl(root2, focusedControl);\n      }\n    };\n    let persistedChatState = interactionController.getSnapshot().state;\n    interactionController.subscribe((change) => {\n      const nextState = interactionController.getSnapshot().state;\n      if (!isRestoringChatState && hasUiGraphChatPersistentStateChanged(config2.uiGraph, persistedChatState, nextState)) {\n        void browserPersistence.saveChatState(nextState).catch(() => void 0);\n        void browserPersistence.pruneResponseTraces(nextState).catch(() => void 0);\n      }\n      persistedChatState = nextState;\n      if (change !== "state") render();\n    });\n    browserPersistence.subscribeWarning((warning) => {\n      storageWarning = warning;\n      render();\n    });\n    browserPersistence.subscribe((change) => {\n      if (change.namespace !== "chat-state") return;\n      void browserPersistence.loadChatState().then((nextChatState) => {\n        const patch = {};\n        for (const component of config2.uiGraph.components) {\n          if (component.type !== "chat") continue;\n          const draftKey = getUiGraphChatDraftStateKey(component.id);\n          const messagesKey = getUiGraphChatMessagesStateKey(component.id);\n          const pinsKey = getUiGraphChatPinsStateKey(component.id);\n          patch[draftKey] = nextChatState[draftKey] ?? "";\n          patch[messagesKey] = nextChatState[messagesKey] ?? [];\n          patch[pinsKey] = nextChatState[pinsKey] ?? [];\n        }\n        isRestoringChatState = true;\n        interactionController.updateStatePatch(patch);\n        persistedChatState = interactionController.getSnapshot().state;\n        isRestoringChatState = false;\n        render();\n      }).catch(() => void 0);\n    });\n    window.addEventListener("pagehide", (event) => {\n      if (actionRunner.survivesPageDetach) {\n        interactionController.detachActions();\n      } else {\n        interactionController.abortActions();\n      }\n      actionRunner.dispose();\n      disposeOutputResizeObservers();\n      restoreActionRunnerFromPageCache = event.persisted;\n      if (!event.persisted) browserPersistence.dispose();\n    });\n    window.addEventListener("pageshow", (event) => {\n      if (!event.persisted || !restoreActionRunnerFromPageCache) return;\n      actionRunner = createHostedActionRunner(config2);\n      restoreActionRunnerFromPageCache = false;\n      disposeOutputResizeObservers = observeOutputResizeBounds(root2);\n    });\n    render();\n  }\n  function readEmbeddedConfig(root2) {\n    const serializedConfig = root2?.getAttribute("data-rivet-web-app-config");\n    if (!serializedConfig) {\n      return void 0;\n    }\n    try {\n      return JSON.parse(serializedConfig);\n    } catch {\n      return void 0;\n    }\n  }\n\n  // src/webAppClient.ts\n  var root = document.getElementById("app");\n  var config = window.__RIVET_WEB_APP__ ?? readEmbeddedConfig(root);\n  if (root && config) mountRivetWebApp(root, config);\n})();\n';
