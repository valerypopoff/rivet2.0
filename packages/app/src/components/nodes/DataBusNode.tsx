import type { NodeComponentDescriptor } from '../../hooks/useNodeTypes.js';

/** Data Bus configuration is limited to the shared node metadata controls. */
export const dataBusNodeDescriptor: NodeComponentDescriptor<'dataBus'> = {};
