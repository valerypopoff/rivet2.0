---
id: llm-chat
title: LLM Chat Node
sidebar_label: LLM Chat
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

## Overview

LLM Chat is the recommended chat node for new Rivet 2 graphs. It is a vendor-agnostic chat node built on the Vercel AI SDK and supports OpenAI, Anthropic, Google, and custom OpenAI-compatible providers from one node.

Use LLM Chat when you want to:

- switch providers without rewiring the graph
- select one or an ordered fallback chain of reusable [LLM Profile](./llm-profile.mdx) values at runtime
- use OpenAI reasoning models, Anthropic thinking, or Google thinking controls
- provide the API key from settings or from an input port
- call custom OpenAI-compatible providers by setting `Provider base URL`
- expose response status/error/request-body outputs for provider debugging
- retry non-200 provider responses and inspect per-attempt status/error arrays

The older [Chat Node](./chat.mdx) remains available for existing graphs, but new work should prefer LLM Chat.

<Tabs
  defaultValue="inputs"
  values={[
    {label: 'Inputs', value: 'inputs'},
    {label: 'Outputs', value: 'outputs'},
    {label: 'Editor Settings', value: 'settings'},
  ]
}>

<TabItem value="inputs">

## Inputs

| Title                  | Data Type                                                 | Description                                                                                                                                                                                                |
| ---------------------- | --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| System Prompt          | `string`                                                  | Optional system prompt prepended to the main prompt.                                                                                                                                                       |
| LLM Profiles           | `llm-config` or `llm-config[]`                            | Required when Configuration is From profile. One profile supplies provider, model, credential, generation, reasoning, and provider-native capability settings; an array is a left-to-right fallback chain. |
| Prompt                 | `chat-message`, `chat-message[]`, `string`, or `string[]` | Main prompt sent to the model. Strings are converted to user messages.                                                                                                                                     |
| Tools                  | `gpt-function` or `gpt-function[]`                        | Available when Tool use is enabled. Defines functions/tools the model may call.                                                                                                                            |
| API Key                | `string`                                                  | Inline mode only. Available when API key source is set to Input port. In profile mode this input belongs to LLM Profile.                                                                                   |
| Provider base URL      | `string`                                                  | Inline mode only. Available for Custom provider when Provider base URL uses an input. Accepts a base URL or full `/chat/completions` URL.                                                                  |
| Headers                | `object`                                                  | Inline mode only. Available when Headers uses an input. In profile mode this input belongs to LLM Profile.                                                                                                 |
| Extra Provider Options | `string` or `object`                                      | Inline mode only. Available when Extra provider options uses an input. In profile mode this input belongs to LLM Profile.                                                                                  |
| Response Schema        | `object` or `gpt-function`                                | Available when Response format is JSON schema.                                                                                                                                                             |

</TabItem>

Prompt messages created with the [Prompt](./prompt.mdx) or
[Assemble Message](./assemble-message.mdx) node may use a distinct
`developer` role. OpenAI and Custom OpenAI-compatible requests preserve that
role in the HTTP request body. Anthropic and Google do not expose the same role,
so LLM Chat sends developer content through their system-instruction mechanism.
Provider and model support still applies; a Custom provider that rejects
`developer` messages will return its normal provider error.

<TabItem value="outputs">

## Outputs

| Title                | Data Type                                                                 | Description                                                                                                                                                                                                                                                                                                                 |
| -------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Response             | `string`, `number`, `boolean`, `object`, `any`, or arrays of those values | Final model response. Default/Text formats output text. JSON object and JSON schema formats output the parsed structured value when parsing succeeds, or the raw string when parsing fails. It may stream visually in the editor, but downstream nodes receive the final value.                                             |
| Messages Sent        | `chat-message[]`                                                          | Messages sent to the provider.                                                                                                                                                                                                                                                                                              |
| All Messages         | `chat-message[]`                                                          | Conversation messages including the response.                                                                                                                                                                                                                                                                               |
| Tool Calls           | `object[]`                                                                | Available when Tool use is enabled. With auto-continuation, completed Rivet calls are retained as delegated records for observability; unresolved raw calls remain when continuation stops.                                                                                                                                 |
| Usage                | `object`                                                                  | Available when Output usage details is enabled. Includes provider usage metadata when available.                                                                                                                                                                                                                            |
| Reasoning            | `string` or `string[]`                                                    | Available when Output reasoning is enabled and the provider exposes reasoning/thinking output.                                                                                                                                                                                                                              |
| Response Status      | `number`, `number[]`, or `any`                                            | Available when Output response status is enabled. Retry on non-200 uses a per-attempt array. With an actual LLM Profile array, the Any value is grouped by profile: one request stays a number, while retries are a number array.                                                                                           |
| Response Error       | `string`, `string[]`, or `any`                                            | Available when Output response error is enabled. Retry on non-200 uses a per-attempt array. With an actual LLM Profile array, one error is a string, retries remain an array, and no request errors return `[]`. When multiple profiles contain an error, the Any value retains their profile positions.                    |
| LLM request body     | `object`, `object[]`, `string`, `string[]`, `any`, or `any[]`             | Available when Output request body is enabled. Shows the provider request body captured at the HTTP boundary and parsed for inspection. Multiple provider calls, such as retries or auto-continued tool rounds, are returned in call order as an array. Request headers and Rivet-managed API key headers are not included. |
| LLM Profile Summary  | `string`                                                                  | Available in From profile mode. Human-readable outcome for every configured profile: succeeded, failed, or not attempted.                                                                                                                                                                                                   |
| LLM Profile Attempts | `object[]`                                                                | Available in From profile mode. Chronological safe history of profile-chain configuration/request attempts, retry indexes, outcomes, statuses, and errors with known API-key and header values redacted.                                                                                                                    |

</TabItem>

<TabItem value="settings">

## Editor Settings

### Configuration

**Inline** is the backward-compatible default and keeps provider, model, credential,
generation, reasoning, and provider-specific settings in LLM Chat.

**From profile** hides those settings and adds a required **LLM Profiles** input.
Connect one [LLM Profile](./llm-profile.mdx) node to switch the inference
configuration without duplicating or editing the rest of the Chat node, or
connect an ordered `llm-config[]` value for provider/model fallback. Prompt,
response format, Rivet tools, auto-continuation, outputs, request diagnostics,
retry behavior, and cache behavior remain settings of the LLM Chat node.

With a fallback chain, LLM Chat tries profiles from left to right. It performs
all configured **Retry on non-200** attempts for one profile before advancing to
the next. A profile that completes a model round remains active for later
auto-continued tool rounds; only a later failure can advance to another backup.
`LLM Profile Summary` makes recovered failures visible in the ordinary node
output. `LLM Profile Attempts` contains the complete safe history. With **Output
response status** enabled, an actual profile array changes **Response Status**
into an Any value grouped by input profile: one physical request is a number and
multiple attempts are a number array. **Response Error**, when enabled, remains
a profile-indexed array of chronological error arrays. An empty array means that
profile made no HTTP request. Build a
profile array with an [Array](./array.mdx) node when you need more than one
profile. The array is kept intact for each **Run per item** invocation—it does not create one
graph run per profile.

When **Inline** is selected, choose **Export LLM settings to profile node** at the right of the switcher row to
convert the current setup in one step. Rivet creates an LLM Profile node to the
left, copies the provider and inference settings into it, moves any connected
configuration inputs, connects the profile to this LLM Chat, and changes the
Chat node to **From profile**. Prompt, tool, response-format, and downstream
connections stay where they are. You can undo the whole conversion with one
Undo action.

### Model

This section is available in Inline configuration mode.

| Setting                           | Description                                                                                                                                          |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| Provider                          | OpenAI, Anthropic, Google, or Custom provider.                                                                                                       |
| Provider base URL                 | Custom provider only. OpenAI-compatible hosts belong here by choosing Custom provider, not by overriding built-in OpenAI.                            |
| Model                             | Provider model. For Custom provider, enter the model ID expected by that provider.                                                                   |
| API key source                    | Use the configured provider key or expose an API Key input port.                                                                                     |
| Alternative programmatic key name | Custom provider only when using a configured key. Programmatic runs can pass this key as a top-level run option. If blank, `customAiApiKey` is used. |
| Alternative API key env var       | Custom provider only when using a configured key. Defaults to `CUSTOM_PROVIDER_API_KEY`.                                                             |

### Parameters

Inline LLM Chat supports common generation parameters such as Temperature, Max output tokens, Top P, Top K, presence penalty, frequency penalty, stop sequences, and seed. In From profile mode these settings and their optional input ports belong to LLM Profile. New nodes leave Top P unset unless you fill it in or drive it from an input port. Provider support varies; unsupported settings may be ignored by the provider or model.

### Reasoning

Reasoning settings are provider-specific. They are configured on LLM Chat in
Inline mode and on LLM Profile in From profile mode:

- OpenAI: Reasoning effort and optional reasoning summary.
- Anthropic: Thinking mode, effort, thinking budget, and cache breakpoint TTL.
- Google: Thinking level, thinking budget, and Include thoughts.

**Output reasoning** is always in LLM Chat's **Outputs** section because it
controls whether that Chat exposes the `Reasoning` output.

The node body shows the selected reasoning effort for built-in providers.

### Response Format

Response format supports Default, Text, JSON object, and JSON schema. JSON object and JSON schema make the Response output emit the parsed structured value instead of a JSON string when parsing succeeds. If the provider returns text that cannot be parsed into the requested structure, Rivet falls back to the raw string Response output instead of failing the node. JSON object asks OpenAI-compatible Custom providers for `response_format: { type: "json_object" }`. JSON schema adds a Response Schema input port and optional schema name/description settings. For Custom provider, JSON schema sends the same plain JSON-compatible schema as an OpenAI-compatible `response_format` request field so compatible endpoints receive the schema instead of only JSON object mode.

### Tools

Tool use enables the Tools input and optional tool-choice controls. Auto-continue mode lets Rivet execute tool calls, send tool results back to the model, and repeat until a normal answer is produced or the maximum tool-round limit is reached. A round is one model response containing one or more eligible tool calls, so a single round can contain multiple parallel calls. After Rivet executes the last permitted round, it makes one final provider request so the model can turn those results into an answer. If that request returns more tool calls, Rivet leaves them unresolved on the normal **Tool Calls** output and does not auto-continue again.

Each [Tool](./gpt-function.mdx) can choose **Result handling**. The default,
**Continue with LLM**, keeps the normal result-and-follow-up loop. **Return
directly** skips the follow-up provider request only when that Tool is the sole
declared Rivet tool call in the current round and its handler succeeds. The
handler's exact string becomes **Response**; its complete function-result message
remains in **All Messages**, while **Messages Sent**, usage, reasoning, request
bodies, and response-token accounting describe only provider calls that actually
occurred. A round with multiple calls always uses normal continuation, including
mixed and all-direct rounds.

**Allow parallel toolcalls** is available for OpenAI, Anthropic, and Custom providers. It permits a supported model to return more than one tool call in a response; it does not require the model to do so. OpenAI and Anthropic receive an explicit provider setting for either switch state. For a Custom OpenAI-compatible provider, Rivet sends `parallel_tool_calls: true` only when the switch is enabled. When disabled, Rivet leaves the field out so the endpoint's default applies; an explicit `parallel_tool_calls` value in **Extra provider options** is still respected. Some partially compatible Custom endpoints may ignore or reject the field when it is enabled. Google does not show this switch because its current adapter has no equivalent control.

To make tool work visible and configurable in the graph:

1. Enable **Tool use** and **Auto-continue after toolcalls run**.
2. Connect **Tool Calls** directly to the **Tool Call** input of one
   [Delegate Tool Call](./delegate-tool-call.mdx) node.
3. Configure that Delegate to auto-delegate by graph name, call registered
   external functions, or use explicit handler graphs.

That exact existing connection becomes bidirectional automatically for the
duration of auto-continuation. The LLM sends each model tool-call round to the
Delegate, receives its ordered results, and continues generating while both
nodes stay **Running**. There is no extra execution mode, reverse wire, or
waiting state to configure. Each model tool-call round appears as a separate run
page on the Delegate.

**Run per item** LLM Chat nodes are deliberately not upgraded to this connected
continuation behavior. Their Tool Calls wire remains ordinary and each split
invocation uses the internal auto-continuation path, because one shared Delegate
result cannot safely stand for multiple parallel split indexes.

If the model includes normal assistant text in a response that also contains
tool calls, the connected Delegate exposes it on **Message (fires before tool
call invocation)**. That output activates once for every tool call before
the shared batch is dispatched. Its branch and the corresponding tool handler
start in parallel. Normal
tool-result branches start after the Delegate finishes. Put a **Start Async
Branch** node immediately after the message output when that work should overlap
the tools and later LLM rounds; the async trigger returns immediately, while the
root lifecycle still owns all managed async work. In a web app, the final
foreground Chat response can be displayed before that async work finishes.
Consumers still waiting for the final LLM response or another late input remain
in ordinary post-LLM scheduling. If an already-ready pre-tool path itself enters
a cycle, Loop Controller, Race Inputs, or a foreground rejoin, Rivet reports the
unsupported branch before invoking tools. Use **Start Async Branch** as the
explicit boundary for a closed side-effect subtree that should overlap tool
work.

Rivet can converge the pre-tool message and final tool-result branches within the
same round. Consumers that also need the final LLM response or another late
input are deferred; completed continuation branch outputs are promoted after the
owning LLM finishes so the consumer runs once. Outputs from an earlier tool round
are not considered fresh inputs for the current round.

Exactly one eligible connected Delegate is required for this graph-visible
continuation. With none, LLM Chat uses its built-in internal delegation path.
With more than one, Rivet reports an ambiguity error. If auto-continuation stops
because a tool is unknown or **Max tool rounds** is reached, unresolved raw calls
remain available to the ordinary downstream graph path. A single-run Delegate
accepts one raw call; use **Run per item** on the Delegate or select one call
before it when the unresolved output can contain several calls.

### Provider Advanced

Provider Advanced contains optional Headers and Extra provider options. Built-in OpenAI, Anthropic, and Google providers use their provider-owned endpoints. For OpenAI-compatible custom hosts, choose Custom provider and use the Model section's Provider base URL.

### Outputs

| Setting                              | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Output reasoning                     | Adds a Reasoning output when the provider exposes reasoning or thinking text.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Output usage details                 | Adds a Usage output containing prompt, completion, total, cached, and reasoning tokens plus estimated cost when available.                                                                                                                                                                                                                                                                                                                                                                               |
| Output response status               | Adds a Response Status output. Retry mode returns statuses for every physical provider attempt.                                                                                                                                                                                                                                                                                                                                                                                                          |
| Output request body                  | Adds an LLM request body output. The body can contain prompts and non-secret provider options, but never authorization headers or API keys.                                                                                                                                                                                                                                                                                                                                                              |
| Stream response                      | When enabled, the provider request uses streaming so Rivet can show live response updates while the node runs. When disabled, Rivet uses a non-streaming provider request and only displays the final response. Connected downstream nodes receive only the final response in both modes. Tool-call rounds can return tool calls before a final structured response exists; in that case Rivet keeps the tool calls, and auto-continue can produce the final structured response on a later model round. |
| Cache outputs (editor only) (legacy) | Shown only for nodes that already have this legacy setting enabled. It reuses matching editor-run outputs while the Rivet app remains open; turn it off to retire it from that node.                                                                                                                                                                                                                                                                                                                     |

### Error Behavior

| Setting               | Description                                                                                                                                                                                                                                                                         |
| --------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Retry on non-200      | Retries provider requests when the Vercel AI SDK reports a non-200 HTTP status. When this is off, Rivet disables the SDK's built-in retry layer and makes one provider attempt. With an LLM Profile fallback chain, Rivet exhausts these repeats before moving to the next profile. |
| Repeat times          | Number of repeats after the initial request.                                                                                                                                                                                                                                        |
| Cooldown, ms          | Delay between retry attempts.                                                                                                                                                                                                                                                       |
| Output response error | Adds a Response Error output and converts provider request failures into excluded normal outputs. Retry mode returns errors for every physical attempt.                                                                                                                             |

</TabItem>

</Tabs>

## Custom Provider

Choose Custom provider for OpenAI-compatible APIs such as local model servers or other hosted providers.

For Custom provider, the expected node body order is:

```text
Provider: Custom
Base URL: <url>
Model: <model name>
```

The Base URL line is shown as plain configuration text in the node body, not as a clickable link.

The custom provider API key can come from, in precedence order:

- the API Key input port
- the alternative programmatic key name as a top-level run option, when set
- the alternative API key env var through `pluginEnv` or `process.env`
- the custom-provider key in Settings > LLM

The API Key input port wins when it is enabled. In configured-key mode, `Alternative programmatic key name` lets backend code pass different custom-provider secrets per node as top-level run options, while `Alternative API key env var` lets desktop or Node runs read a named environment variable. If the alternative programmatic key name is blank, Rivet skips the named top-level lookup and still checks the env var name before the shared `customAiApiKey`. The Settings > LLM custom-provider key is the app-side source of that shared key.

OpenAI, Anthropic, Google, and custom-provider configured keys are set in Settings > LLM. When running through `@valerypopoff/rivet2-node`, unset configured keys can also come from `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `GOOGLE_GENERATIVE_AI_API_KEY`, and the canonical custom-provider `CUSTOM_PROVIDER_API_KEY` env var. Legacy `CUSTOM_AI_API_KEY` is still accepted as a compatibility alias.

## Error Handling

LLM Chat normalizes common provider failures into readable errors. For example, an invalid API key should produce a provider request error that includes the HTTP status, provider, model, endpoint, and a hint to check the API key source.

In the Rivet editor, use **Inspect response** beside the normal output controls or
in the full-output toolbar to inspect how the currently selected LLM Chat
invocation ran. The inspector follows the selected graph run and process page,
including live invocations. It reports model attempts, **Provider request
retries**, **LLM profile fallbacks**, tool executions, durations, reported token
usage, and known or unknown cost state in semantic groups. It intentionally
never shows prompts, conversation
messages, generated response text, reasoning text, tool arguments/results,
retrieved documents, credentials, headers, or raw provider bodies. Frozen
outputs, cache replay, and older recordings may show a partial or unavailable
trace when no physical-call metadata exists.

Enable **Output response status** when you want machine-readable statuses, **Output response error** when provider request failures should return through an output rather than fail the node, and **Output request body** to inspect the provider body Rivet sent. With Retry on non-200 enabled, scalar-profile Response Status and Response Error become arrays so you can see each retry. With an actual LLM Profile array, Response Status is a profile-indexed Any value: one physical request stays a number and multiple attempts are an array. Response Error is compact: one profile error is a string, retries remain an array, and no request errors return `[]`. When multiple profiles include an error, the Any value retains their profile positions, for example `['Invalid URL', []]`. Setup failures stay visible in `LLM Profile Attempts`. LLM request body captures provider bodies in physical call order, including fallback candidates. `LLM Profile Summary` gives an immediately readable succeeded/failed/not-attempted outcome for each configured profile, while `LLM Profile Attempts` is the cross-profile diagnostic record; it redacts known API-key and request-header values while identifying the provider, model, profile index, retry attempt, status, and failure. If the terminal profile fails during setup before a request is made, LLM Chat returns its safe aggregate chain error instead of stale request diagnostics from an earlier profile. Request headers and Rivet-managed API key headers are not included, but prompts and request options in the actual provider body are visible. Rivet disables the Vercel AI SDK's hidden default retries, so the Retry on non-200 switch is the only retry policy for LLM Chat provider requests.

## See Also

- [Assemble Prompt Node](./assemble-prompt.mdx)
- [Prompt Node](./prompt.mdx)
- [GPT Function Node](./gpt-function.mdx)
- [Chat Node](./chat.mdx)
